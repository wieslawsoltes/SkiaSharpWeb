# Declaration and overload conformance audit

Pinned source: [b33cf54f24edc5347567c95b1924c447669c1de8](https://github.com/mono/SkiaSharp/tree/b33cf54f24edc5347567c95b1924c447669c1de8).

Every one of the **3995 pinned declarations** appears in the JSON companion and the tables below. No percentage is advertised: native pointers, C# operators, platform controls, overloaded methods, and scalar enum values have different significance.

| Status | Declarations |
| --- | ---: |
| implemented | 1936 |
| partial | 1042 |
| not-applicable | 873 |
| missing | 144 |

## Interpretation

- One row per pinned lexical declaration, retaining overload signature, source file, and line.
- Implemented means the listed managed argument shape has a concrete adapter or audited direct implementation; it is not a claim of native binary or total pixel compatibility.
- Partial means a callable/type exists with a documented difference, an untested semantic branch, or no completed signature-level audit. Method-name presence never upgrades a row to implemented.
- Not-applicable is reserved for C# language constructs, process pointers, P/Invoke, or non-web UI integration. These are not counted as implemented features.
- Missing means no matching browser type/member or an explicit unsupported overload.
- Initializer expressions and generic method suffixes are normalized to field/method identities while originalName/originalKind and stable IDs remain preserved. Lexer corrections are counted independently from implementation changes.
- This audit combines named overload adapters with evidence from native conformance, asset, font, document, surface, effect and GPU test suites. Untested semantic branches remain partial.
- JavaScript mappings: ref/out structs use mutable objects; spans use arrays/typed arrays; numeric values use JavaScript Number; overload dispatch is dynamic.
- Pinned-source corrections include System.Numerics row-vector SKMatrix44 storage, default Empty construction, midpoint-to-even checked Int32 rounding, exact value-zero SKRect.IsEmpty, degenerate rectangle intersection/union, and default warped text-on-path.
- The inventory is lexical, includes preprocessor branches and obsolete forms, and does not expand inherited members. It is not compiler-produced metadata.

## Known exactness limits

- The bundled native canvas bridge now restores singular matrices and preserves real clip classification; stock CanvasKit without the extension still cannot do this.
- Physical vendor GPUs and live Ganesh texture-adoption variants have not been exhaustively exercised. Dawn SwiftShader runs actual Graphite, including shaders, effects, fonts and image lifetimes.
- `BreakText` without an output object keeps the original JavaScript result object. Its `valueOf()` returns the count; the explicit output form returns a count and populates the width/text object.
- Warped glyph outlines use adaptive flattening with a 0.2 px target. Exact native contour ordering and native glyph-warp tolerance are not claimed.
- `GetFillPath` performs real path stroking and optional effect baking. Native fast-bounds internals and all effect combinations are not directly exposed.
- Full .NET source-level API parity is not complete. This report intentionally leaves unverified branches partial or missing.

## Release 0.4 review

Native memory tracing, singular matrix/clip accessors, and asynchronous HDR readback have additional concrete tests. Other partial branches remain partial; desktop interop is not relabeled as implemented.


## Signature rows

### SkiaSharp.Resources.ResourceProvider

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKData? Load (string resourceName)` | partial | `ResourceProvider.Load` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData? Load (string resourcePath, string resourceName)` | partial | `ResourceProvider.Load` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Resources.CachingResourceProvider

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public CachingResourceProvider (ResourceProvider resourceProvider) : base (Create (resourceProvider), true)` | partial | `CachingResourceProvider(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Resources.DataUriResourceProvider

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public DataUriResourceProvider (bool preDecode = false) : this (null, preDecode)` | partial | `DataUriResourceProvider(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public DataUriResourceProvider (ResourceProvider? fallbackProvider, bool preDecode = false) : base (Create (fallbackProvider, preDecode), true)` | partial | `DataUriResourceProvider(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Resources.FileResourceProvider

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public FileResourceProvider (string baseDirectory, bool preDecode = false) : base (Create (baseDirectory, preDecode), true)` | partial | `FileResourceProvider(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SceneGraph.InvalidationController

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public InvalidationController () : this (SceneGraphApi.sksg_invalidation_controller_new (), true)` | partial | `InvalidationController(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe void Invalidate (SKRect rect, SKMatrix matrix)` | partial | `InvalidationController.Invalidate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe SKRect Bounds` | partial | `InvalidationController.Bounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe void Begin ()` | partial | `InvalidationController.Begin` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe void End ()` | partial | `InvalidationController.End` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe void Reset ()` | partial | `InvalidationController.Reset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Skottie.Animation

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static AnimationBuilder CreateBuilder (AnimationBuilderFlags flags = AnimationBuilderFlags.None)` | partial | `Animation.CreateBuilder` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static Animation? Parse (string json)` | partial | `Animation.Parse` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool TryParse (string json, [System.Diagnostics.CodeAnalysis.NotNullWhen (true)] out Animation? animation)` | partial | `Animation.TryParse` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static Animation? Create (Stream stream)` | partial | `Animation.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool TryCreate (Stream stream, [System.Diagnostics.CodeAnalysis.NotNullWhen (true)] out Animation? animation)` | partial | `Animation.TryCreate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static Animation? Create (SKStream stream)` | partial | `Animation.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool TryCreate (SKStream stream, [System.Diagnostics.CodeAnalysis.NotNullWhen (true)] out Animation? animation)` | partial | `Animation.TryCreate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static Animation? Create (SKData data)` | partial | `Animation.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool TryCreate (SKData data, [System.Diagnostics.CodeAnalysis.NotNullWhen (true)] out Animation? animation)` | partial | `Animation.TryCreate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static Animation? Create (string path)` | partial | `Animation.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool TryCreate (string path, [System.Diagnostics.CodeAnalysis.NotNullWhen (true)] out Animation? animation)` | partial | `Animation.TryCreate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe void Render (SKCanvas canvas, SKRect dst)` | partial | `Animation.Render` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Render (SKCanvas canvas, SKRect dst, AnimationRenderFlags flags)` | partial | `Animation.Render` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Seek (double percent, InvalidationController? ic = null)` | partial | `Animation.Seek` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SeekFrame (double frame, InvalidationController? ic = null)` | partial | `Animation.SeekFrame` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SeekFrameTime (double seconds, InvalidationController? ic = null)` | partial | `Animation.SeekFrameTime` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SeekFrameTime (TimeSpan time, InvalidationController? ic = null)` | partial | `Animation.SeekFrameTime` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public TimeSpan Duration` | partial | `Animation.Duration` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public double Fps` | partial | `Animation.Fps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public double InPoint` | partial | `Animation.InPoint` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public double OutPoint` | partial | `Animation.OutPoint` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public string Version` | partial | `Animation.Version` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public unsafe SKSize Size` | partial | `Animation.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Skottie.AnimationBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public AnimationBuilder SetFontManager (SKFontManager fontManager)` | partial | `AnimationBuilder.SetFontManager` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public AnimationBuilder SetResourceProvider (ResourceProvider resourceProvider)` | partial | `AnimationBuilder.SetResourceProvider` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public AnimationBuilderStats Stats` | partial | `AnimationBuilder.Stats` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Animation? Build (Stream stream)` | partial | `AnimationBuilder.Build` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Animation? Build (SKStream stream)` | partial | `AnimationBuilder.Build` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Animation? Build (SKData data)` | partial | `AnimationBuilder.Build` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Animation? Build (string path)` | partial | `AnimationBuilder.Build` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.Skottie.AnimationBuilderStats

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public readonly TimeSpan TotalLoadTime` | partial | `AnimationBuilderStats.TotalLoadTime` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly TimeSpan JsonParseTime` | partial | `AnimationBuilderStats.JsonParseTime` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly TimeSpan SceneParseTime` | partial | `AnimationBuilderStats.SceneParseTime` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly int JsonSize` | partial | `AnimationBuilderStats.JsonSize` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly int AnimatorCount` | partial | `AnimationBuilderStats.AnimatorCount` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |

### SkiaSharp.AnimationBuilderStats

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public readonly bool Equals (AnimationBuilderStats obj)` | implemented | `AnimationBuilderStats.Equals` — Explicit value equality compares timing and count fields; deterministic JS hashing is classified separately. |
| `public readonly override bool Equals (object obj)` | implemented | `AnimationBuilderStats.Equals` — Explicit value equality compares timing and count fields; deterministic JS hashing is classified separately. |
| `public static bool operator == (AnimationBuilderStats left, AnimationBuilderStats right)` | not-applicable | `AnimationBuilderStats.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (AnimationBuilderStats left, AnimationBuilderStats right)` | not-applicable | `AnimationBuilderStats.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `AnimationBuilderStats.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.AnimationBuilderFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SKOTTIE_ANIMATION_BUILDER_FLAGS = 0 None = 0, // DEFER_IMAGE_LOADING_SKOTTIE_ANIMATION_BUILDER_FLAGS = 0x01` | implemented | `AnimationBuilderFlags.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DeferImageLoading = 1, // PREFER_EMBEDDED_FONTS_SKOTTIE_ANIMATION_BUILDER_FLAGS = 0x02` | implemented | `AnimationBuilderFlags.DeferImageLoading` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `PreferEmbeddedFonts = 2` | implemented | `AnimationBuilderFlags.PreferEmbeddedFonts` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.AnimationRenderFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SKIP_TOP_LEVEL_ISOLATION = 0x01 SkipTopLevelIsolation = 1, // DISABLE_TOP_LEVEL_CLIPPING = 0x02` | implemented | `AnimationRenderFlags.SkipTopLevelIsolation` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DisableTopLevelClipping = 2` | implemented | `AnimationRenderFlags.DisableTopLevelClipping` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFontStyleWeight

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Invisible = 0` | implemented | `SKFontStyleWeight.Invisible` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Thin = 100` | implemented | `SKFontStyleWeight.Thin` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ExtraLight = 200` | implemented | `SKFontStyleWeight.ExtraLight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Light = 300` | implemented | `SKFontStyleWeight.Light` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Normal = 400` | implemented | `SKFontStyleWeight.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Medium = 500` | implemented | `SKFontStyleWeight.Medium` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SemiBold = 600` | implemented | `SKFontStyleWeight.SemiBold` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bold = 700` | implemented | `SKFontStyleWeight.Bold` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ExtraBold = 800` | implemented | `SKFontStyleWeight.ExtraBold` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Black = 900` | implemented | `SKFontStyleWeight.Black` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ExtraBlack = 1000` | implemented | `SKFontStyleWeight.ExtraBlack` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFontStyleWidth

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `UltraCondensed = 1` | implemented | `SKFontStyleWidth.UltraCondensed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ExtraCondensed = 2` | implemented | `SKFontStyleWidth.ExtraCondensed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Condensed = 3` | implemented | `SKFontStyleWidth.Condensed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SemiCondensed = 4` | implemented | `SKFontStyleWidth.SemiCondensed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Normal = 5` | implemented | `SKFontStyleWidth.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SemiExpanded = 6` | implemented | `SKFontStyleWidth.SemiExpanded` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Expanded = 7` | implemented | `SKFontStyleWidth.Expanded` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ExtraExpanded = 8` | implemented | `SKFontStyleWidth.ExtraExpanded` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `UltraExpanded = 9` | implemented | `SKFontStyleWidth.UltraExpanded` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKColorType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Unknown = 0` | implemented | `SKColorType.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Alpha8 = 1` | implemented | `SKColorType.Alpha8` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgb565 = 2` | implemented | `SKColorType.Rgb565` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Argb4444 = 3` | implemented | `SKColorType.Argb4444` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgba8888 = 4` | implemented | `SKColorType.Rgba8888` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgb888x = 5` | implemented | `SKColorType.Rgb888x` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bgra8888 = 6` | implemented | `SKColorType.Bgra8888` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgba1010102 = 7` | implemented | `SKColorType.Rgba1010102` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgb101010x = 8` | implemented | `SKColorType.Rgb101010x` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Gray8 = 9` | implemented | `SKColorType.Gray8` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbaF16 = 10` | implemented | `SKColorType.RgbaF16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbaF16Clamped = 11` | implemented | `SKColorType.RgbaF16Clamped` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbaF32 = 12` | implemented | `SKColorType.RgbaF32` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rg88 = 13` | implemented | `SKColorType.Rg88` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `AlphaF16 = 14` | implemented | `SKColorType.AlphaF16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgF16 = 15` | implemented | `SKColorType.RgF16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Alpha16 = 16` | implemented | `SKColorType.Alpha16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rg1616 = 17` | implemented | `SKColorType.Rg1616` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgba16161616 = 18` | implemented | `SKColorType.Rgba16161616` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bgra1010102 = 19` | implemented | `SKColorType.Bgra1010102` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bgr101010x = 20` | implemented | `SKColorType.Bgr101010x` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bgr101010xXR = 21` | implemented | `SKColorType.Bgr101010xXR` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Srgba8888 = 22` | implemented | `SKColorType.Srgba8888` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `R8Unorm = 23` | implemented | `SKColorType.R8Unorm` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rgba10x6 = 24` | implemented | `SKColorType.Rgba10x6` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bgra10101010XR = 25` | implemented | `SKColorType.Bgra10101010XR` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbF16F16F16x = 26` | implemented | `SKColorType.RgbF16F16F16x` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `R16Unorm = 27` | implemented | `SKColorType.R16Unorm` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RF16 = 28` | implemented | `SKColorType.RF16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SkiaExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static bool IsBgr (this SKPixelGeometry pg)` | implemented | `SkiaExtensions.IsBgr` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static bool IsRgb (this SKPixelGeometry pg)` | implemented | `SkiaExtensions.IsRgb` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static bool IsVertical (this SKPixelGeometry pg)` | implemented | `SkiaExtensions.IsVertical` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static bool IsHorizontal (this SKPixelGeometry pg)` | implemented | `SkiaExtensions.IsHorizontal` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static int GetBytesPerPixel (this SKColorType colorType)` | implemented | `SkiaExtensions.GetBytesPerPixel` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static int GetBitShiftPerPixel (this SKColorType colorType)` | implemented | `SkiaExtensions.GetBitShiftPerPixel` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static SKAlphaType GetAlphaType (this SKColorType colorType, SKAlphaType alphaType = SKAlphaType.Premul)` | implemented | `SkiaExtensions.GetAlphaType` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static uint ToGlSizedFormat (this SKColorType colorType)` | implemented | `SkiaExtensions.ToGlSizedFormat` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |
| `public static SKSamplingOptions ToSamplingOptions (this SKFilterQuality quality)` | implemented | `SkiaExtensions.ToSamplingOptions` — Explicit pixel geometry predicates, complete native-to-managed color-type mapping, bytes/shift/alpha/GL format and filter-quality sampling adapters. |

### SkiaSharp.SKCodecOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKCodecOptions Default` | implemented | `SKCodecOptions.Default` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecOptions (SKZeroInitialized zeroInitialized)` | implemented | `SKCodecOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecOptions (SKZeroInitialized zeroInitialized, SKRectI subset)` | implemented | `SKCodecOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecOptions (SKRectI subset)` | implemented | `SKCodecOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecOptions (int frameIndex)` | implemented | `SKCodecOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecOptions (int frameIndex, int priorFrame)` | implemented | `SKCodecOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKZeroInitialized ZeroInitialized` | implemented | `SKCodecOptions.ZeroInitialized` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKRectI? Subset` | implemented | `SKCodecOptions.Subset` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool HasSubset` | implemented | `SKCodecOptions.HasSubset` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public int FrameIndex` | implemented | `SKCodecOptions.FrameIndex` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public int PriorFrame` | implemented | `SKCodecOptions.PriorFrame` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool Equals (SKCodecOptions obj)` | implemented | `SKCodecOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly override bool Equals (object obj)` | implemented | `SKCodecOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public static bool operator == (SKCodecOptions left, SKCodecOptions right)` | not-applicable | `SKCodecOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKCodecOptions left, SKCodecOptions right)` | not-applicable | `SKCodecOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKCodecOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFontMetrics

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public readonly float Top` | implemented | `SKFontMetrics.Top` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float Ascent` | implemented | `SKFontMetrics.Ascent` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float Descent` | implemented | `SKFontMetrics.Descent` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float Bottom` | implemented | `SKFontMetrics.Bottom` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float Leading` | implemented | `SKFontMetrics.Leading` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float AverageCharacterWidth` | implemented | `SKFontMetrics.AverageCharacterWidth` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float MaxCharacterWidth` | implemented | `SKFontMetrics.MaxCharacterWidth` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float XMin` | implemented | `SKFontMetrics.XMin` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float XMax` | implemented | `SKFontMetrics.XMax` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float XHeight` | implemented | `SKFontMetrics.XHeight` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float CapHeight` | implemented | `SKFontMetrics.CapHeight` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float? UnderlineThickness` | implemented | `SKFontMetrics.UnderlineThickness` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float? UnderlinePosition` | implemented | `SKFontMetrics.UnderlinePosition` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float? StrikeoutThickness` | implemented | `SKFontMetrics.StrikeoutThickness` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly float? StrikeoutPosition` | implemented | `SKFontMetrics.StrikeoutPosition` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly bool Equals (SKFontMetrics obj)` | implemented | `SKFontMetrics.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly override bool Equals (object obj)` | implemented | `SKFontMetrics.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static bool operator == (SKFontMetrics left, SKFontMetrics right)` | not-applicable | `SKFontMetrics.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKFontMetrics left, SKFontMetrics right)` | not-applicable | `SKFontMetrics.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKFontMetrics.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKLattice

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public int[] XDivs` | implemented | `SKLattice.XDivs` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public int[] YDivs` | implemented | `SKLattice.YDivs` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKLatticeRectType[] RectTypes` | implemented | `SKLattice.RectTypes` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRectI? Bounds` | implemented | `SKLattice.Bounds` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKColor[] Colors` | implemented | `SKLattice.Colors` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKLattice obj)` | implemented | `SKLattice.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKLattice.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKLattice left, SKLattice right)` | not-applicable | `SKLattice.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKLattice left, SKLattice right)` | not-applicable | `SKLattice.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKLattice.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKDocumentPdfMetadata

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public const float DefaultRasterDpi = SKDocument.DefaultRasterDpi` | implemented | `SKDocumentPdfMetadata.DefaultRasterDpi` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public const int DefaultEncodingQuality = 101` | implemented | `SKDocumentPdfMetadata.DefaultEncodingQuality` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public static readonly SKDocumentPdfMetadata Default` | implemented | `SKDocumentPdfMetadata.Default` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public SKDocumentPdfMetadata (float rasterDpi)` | implemented | `SKDocumentPdfMetadata(...)` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public SKDocumentPdfMetadata (int encodingQuality)` | implemented | `SKDocumentPdfMetadata(...)` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public SKDocumentPdfMetadata (float rasterDpi, int encodingQuality)` | implemented | `SKDocumentPdfMetadata(...)` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Title` | implemented | `SKDocumentPdfMetadata.Title` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Author` | implemented | `SKDocumentPdfMetadata.Author` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Subject` | implemented | `SKDocumentPdfMetadata.Subject` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Keywords` | implemented | `SKDocumentPdfMetadata.Keywords` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Creator` | implemented | `SKDocumentPdfMetadata.Creator` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public string Producer` | implemented | `SKDocumentPdfMetadata.Producer` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public DateTime? Creation` | implemented | `SKDocumentPdfMetadata.Creation` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public DateTime? Modified` | implemented | `SKDocumentPdfMetadata.Modified` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public float RasterDpi` | implemented | `SKDocumentPdfMetadata.RasterDpi` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public bool PdfA` | implemented | `SKDocumentPdfMetadata.PdfA` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public int EncodingQuality` | implemented | `SKDocumentPdfMetadata.EncodingQuality` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public readonly bool Equals (SKDocumentPdfMetadata obj)` | implemented | `SKDocumentPdfMetadata.Equals` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKDocumentPdfMetadata.Equals` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public static bool operator == (SKDocumentPdfMetadata left, SKDocumentPdfMetadata right)` | not-applicable | `SKDocumentPdfMetadata.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKDocumentPdfMetadata left, SKDocumentPdfMetadata right)` | not-applicable | `SKDocumentPdfMetadata.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKDocumentPdfMetadata.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKHighContrastConfig

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKHighContrastConfig Default` | partial | `SKHighContrastConfig.Default` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKHighContrastConfig (bool grayscale, SKHighContrastConfigInvertStyle invertStyle, float contrast)` | partial | `SKHighContrastConfig(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly bool IsValid` | partial | `SKHighContrastConfig.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Grayscale` | partial | `SKHighContrastConfig.Grayscale` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKHighContrastConfigInvertStyle InvertStyle` | partial | `SKHighContrastConfig.InvertStyle` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Single Contrast` | partial | `SKHighContrastConfig.Contrast` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (SKHighContrastConfig obj)` | partial | `SKHighContrastConfig.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKHighContrastConfig.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKHighContrastConfig left, SKHighContrastConfig right)` | not-applicable | `SKHighContrastConfig.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKHighContrastConfig left, SKHighContrastConfig right)` | not-applicable | `SKHighContrastConfig.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKHighContrastConfig.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPngEncoderOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKPngEncoderOptions Default` | implemented | `SKPngEncoderOptions.Default` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKPngEncoderOptions (SKPngEncoderFilterFlags filterFlags, int zLibLevel)` | implemented | `SKPngEncoderOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKPngEncoderFilterFlags FilterFlags` | implemented | `SKPngEncoderOptions.FilterFlags` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public int ZLibLevel` | implemented | `SKPngEncoderOptions.ZLibLevel` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool Equals (SKPngEncoderOptions obj)` | implemented | `SKPngEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly override bool Equals (object obj)` | implemented | `SKPngEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public static bool operator == (SKPngEncoderOptions left, SKPngEncoderOptions right)` | not-applicable | `SKPngEncoderOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKPngEncoderOptions left, SKPngEncoderOptions right)` | not-applicable | `SKPngEncoderOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKPngEncoderOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKJpegEncoderOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKJpegEncoderOptions Default` | implemented | `SKJpegEncoderOptions.Default` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKJpegEncoderOptions (int quality)` | implemented | `SKJpegEncoderOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKJpegEncoderOptions (int quality, SKJpegEncoderDownsample downsample, SKJpegEncoderAlphaOption alphaOption)` | implemented | `SKJpegEncoderOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKJpegEncoderAlphaOption AlphaOption` | implemented | `SKJpegEncoderOptions.AlphaOption` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKJpegEncoderDownsample Downsample` | implemented | `SKJpegEncoderOptions.Downsample` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public int Quality` | implemented | `SKJpegEncoderOptions.Quality` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool Equals (SKJpegEncoderOptions obj)` | implemented | `SKJpegEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly override bool Equals (object obj)` | implemented | `SKJpegEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public static bool operator == (SKJpegEncoderOptions left, SKJpegEncoderOptions right)` | not-applicable | `SKJpegEncoderOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKJpegEncoderOptions left, SKJpegEncoderOptions right)` | not-applicable | `SKJpegEncoderOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKJpegEncoderOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKWebpEncoderOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKWebpEncoderOptions Default` | implemented | `SKWebpEncoderOptions.Default` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKWebpEncoderOptions (SKWebpEncoderCompression compression, float quality)` | implemented | `SKWebpEncoderOptions(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKWebpEncoderCompression Compression` | implemented | `SKWebpEncoderOptions.Compression` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public float Quality` | implemented | `SKWebpEncoderOptions.Quality` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool Equals (SKWebpEncoderOptions obj)` | implemented | `SKWebpEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly override bool Equals (object obj)` | implemented | `SKWebpEncoderOptions.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public static bool operator == (SKWebpEncoderOptions left, SKWebpEncoderOptions right)` | not-applicable | `SKWebpEncoderOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKWebpEncoderOptions left, SKWebpEncoderOptions right)` | not-applicable | `SKWebpEncoderOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKWebpEncoderOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKWebpEncoderFrame

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKWebpEncoderFrame (SKPixmap pixmap, TimeSpan duration)` | implemented | `SKWebpEncoderFrame(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKWebpEncoderFrame (SKBitmap bitmap, TimeSpan duration)` | implemented | `SKWebpEncoderFrame(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKWebpEncoderFrame (SKImage image, TimeSpan duration)` | implemented | `SKWebpEncoderFrame(...)` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKPixmap Pixmap` | implemented | `SKWebpEncoderFrame.Pixmap` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public TimeSpan Duration` | implemented | `SKWebpEncoderFrame.Duration` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |

### SkiaSharp.SKCubicResampler

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKCubicResampler Mitchell = new (1 / 3.0f, 1 / 3.0f)` | implemented | `SKCubicResampler.Mitchell` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public static readonly SKCubicResampler CatmullRom = new (0.0f, 1 / 2.0f)` | implemented | `SKCubicResampler.CatmullRom` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public SKCubicResampler (float b, float c)` | implemented | `SKCubicResampler(...)` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly Single B` | implemented | `SKCubicResampler.B` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly Single C` | implemented | `SKCubicResampler.C` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly bool Equals (SKCubicResampler obj)` | implemented | `SKCubicResampler.Equals` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly override bool Equals (object obj)` | implemented | `SKCubicResampler.Equals` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public static bool operator == (SKCubicResampler left, SKCubicResampler right)` | not-applicable | `SKCubicResampler.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKCubicResampler left, SKCubicResampler right)` | not-applicable | `SKCubicResampler.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKCubicResampler.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKSamplingOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKSamplingOptions Default = new ()` | implemented | `SKSamplingOptions.Default` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public SKSamplingOptions (SKFilterMode filter, SKMipmapMode mipmap)` | implemented | `SKSamplingOptions(...)` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public SKSamplingOptions (SKFilterMode filter)` | implemented | `SKSamplingOptions(...)` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public SKSamplingOptions (SKCubicResampler resampler)` | implemented | `SKSamplingOptions(...)` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public SKSamplingOptions (int maxAniso)` | implemented | `SKSamplingOptions(...)` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public bool IsAniso` | implemented | `SKSamplingOptions.IsAniso` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly Int32 MaxAniso` | implemented | `SKSamplingOptions.MaxAniso` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly bool UseCubic` | implemented | `SKSamplingOptions.UseCubic` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly SKCubicResampler Cubic` | implemented | `SKSamplingOptions.Cubic` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly SKFilterMode Filter` | implemented | `SKSamplingOptions.Filter` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly SKMipmapMode Mipmap` | implemented | `SKSamplingOptions.Mipmap` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly bool Equals (SKSamplingOptions obj)` | implemented | `SKSamplingOptions.Equals` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public readonly override bool Equals (object obj)` | implemented | `SKSamplingOptions.Equals` — Sampling and cubic value constructors/default constants/equality retain native parameters; constant initializer identities are normalized separately from callable declarations. |
| `public static bool operator == (SKSamplingOptions left, SKSamplingOptions right)` | not-applicable | `SKSamplingOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKSamplingOptions left, SKSamplingOptions right)` | not-applicable | `SKSamplingOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKSamplingOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRBackend

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Metal = 0` | implemented | `GRBackend.Metal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `OpenGL = 1` | implemented | `GRBackend.OpenGL` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Vulkan = 2` | implemented | `GRBackend.Vulkan` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Dawn = 3` | implemented | `GRBackend.Dawn` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Direct3D = 4` | implemented | `GRBackend.Direct3D` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Unsupported = 5` | implemented | `GRBackend.Unsupported` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.GRBackendRenderTarget

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRBackendRenderTarget (int width, int height, int sampleCount, int stencilBits, GRGlFramebufferInfo glInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendRenderTarget(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendRenderTarget (int width, int height, int sampleCount, GRVkImageInfo vkImageInfo) : this (width, height, vkImageInfo)` | partial | `GRBackendRenderTarget(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRBackendRenderTarget (int width, int height, GRVkImageInfo vkImageInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendRenderTarget(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendRenderTarget (int width, int height, GRD3DTextureResourceInfo d3dTextureInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendRenderTarget(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendRenderTarget (int width, int height, int sampleCount, GRMtlTextureInfo mtlInfo) : this (width, height, mtlInfo)` | partial | `GRBackendRenderTarget(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRBackendRenderTarget (int width, int height, GRMtlTextureInfo mtlInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendRenderTarget(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool IsValid` | partial | `GRBackendRenderTarget.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `GRBackendRenderTarget.Width` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int Height` | partial | `GRBackendRenderTarget.Height` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int SampleCount` | partial | `GRBackendRenderTarget.SampleCount` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int StencilBits` | partial | `GRBackendRenderTarget.StencilBits` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public GRBackend Backend` | partial | `GRBackendRenderTarget.Backend` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKSizeI Size` | partial | `GRBackendRenderTarget.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRectI Rect` | partial | `GRBackendRenderTarget.Rect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRGlFramebufferInfo GetGlFramebufferInfo ()` | partial | `GRBackendRenderTarget.GetGlFramebufferInfo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetGlFramebufferInfo (out GRGlFramebufferInfo glInfo)` | partial | `GRBackendRenderTarget.GetGlFramebufferInfo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRBackendTexture

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRBackendTexture (int width, int height, bool mipmapped, GRGlTextureInfo glInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendTexture(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendTexture (int width, int height, GRVkImageInfo vkInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendTexture(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendTexture (int width, int height, GRD3DTextureResourceInfo d3dTextureInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendTexture(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRBackendTexture (int width, int height, bool mipmapped, GRMtlTextureInfo mtlInfo) : this (IntPtr.Zero, true)` | not-applicable | `GRBackendTexture(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool IsValid` | partial | `GRBackendTexture.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `GRBackendTexture.Width` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int Height` | partial | `GRBackendTexture.Height` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool HasMipMaps` | partial | `GRBackendTexture.HasMipMaps` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public GRBackend Backend` | partial | `GRBackendTexture.Backend` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKSizeI Size` | partial | `GRBackendTexture.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRectI Rect` | partial | `GRBackendTexture.Rect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRGlTextureInfo GetGlTextureInfo ()` | partial | `GRBackendTexture.GetGlTextureInfo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetGlTextureInfo (out GRGlTextureInfo glInfo)` | partial | `GRBackendTexture.GetGlTextureInfo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static GRContext CreateGl ()` | partial | `GRContext.CreateGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateGl (GRGlInterface backendContext)` | partial | `GRContext.CreateGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateGl (GRContextOptions options)` | partial | `GRContext.CreateGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateGl (GRGlInterface backendContext, GRContextOptions options)` | partial | `GRContext.CreateGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateVulkan (GRVkBackendContext backendContext)` | partial | `GRContext.CreateVulkan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateVulkan (GRVkBackendContext backendContext, GRContextOptions options)` | partial | `GRContext.CreateVulkan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateDirect3D (GRD3DBackendContext backendContext)` | partial | `GRContext.CreateDirect3D` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateDirect3D (GRD3DBackendContext backendContext, GRContextOptions options)` | partial | `GRContext.CreateDirect3D` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateMetal (GRMtlBackendContext backendContext)` | partial | `GRContext.CreateMetal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRContext CreateMetal (GRMtlBackendContext backendContext, GRContextOptions options)` | partial | `GRContext.CreateMetal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override GRBackend Backend` | partial | `GRContext.Backend` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override bool IsAbandoned` | partial | `GRContext.IsAbandoned` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AbandonContext (bool releaseResources = false)` | partial | `GRContext.AbandonContext` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long GetResourceCacheLimit ()` | partial | `GRContext.GetResourceCacheLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetResourceCacheLimit (long maxResourceBytes)` | partial | `GRContext.SetResourceCacheLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void GetResourceCacheUsage (out int maxResources, out long maxResourceBytes)` | partial | `GRContext.GetResourceCacheUsage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ResetContext (GRGlBackendState state)` | partial | `GRContext.ResetContext` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ResetContext (GRBackendState state = GRBackendState.All)` | partial | `GRContext.ResetContext` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ResetContext (uint state)` | partial | `GRContext.ResetContext` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush ()` | partial | `GRContext.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush (bool submit, bool synchronous = false)` | partial | `GRContext.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Submit (bool synchronous = false)` | partial | `GRContext.Submit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void CheckAsyncWorkCompletion ()` | partial | `GRContext.CheckAsyncWorkCompletion` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush (SKImage image)` | partial | `GRContext.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush (SKSurface surface)` | partial | `GRContext.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public new int GetMaxSurfaceSampleCount (SKColorType colorType)` | partial | `GRContext.GetMaxSurfaceSampleCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DumpMemoryStatistics (SKTraceMemoryDump dump)` | implemented | `GRContext.DumpMemoryStatistics` — Compiled native Skia memory-trace callbacks; numeric/string/backing ownership and ulong-to-BigInt behavior have unit tests. Chromium executes both graphics and Ganesh traces with nonempty native rows (browser lifecycle workflow). |
| `public void PurgeResources ()` | partial | `GRContext.PurgeResources` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void PurgeUnusedResources (long milliseconds)` | partial | `GRContext.PurgeUnusedResources` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void PurgeUnlockedResources (bool scratchResourcesOnly)` | partial | `GRContext.PurgeUnlockedResources` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void PurgeUnlockedResources (long bytesToPurge, bool preferScratchResources)` | partial | `GRContext.PurgeUnlockedResources` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRContextOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool AvoidStencilBuffers` | partial | `GRContextOptions.AvoidStencilBuffers` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int RuntimeProgramCacheSize` | partial | `GRContextOptions.RuntimeProgramCacheSize` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int GlyphCacheTextureMaximumBytes` | partial | `GRContextOptions.GlyphCacheTextureMaximumBytes` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool AllowPathMaskCaching` | partial | `GRContextOptions.AllowPathMaskCaching` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool DoManualMipmapping` | partial | `GRContextOptions.DoManualMipmapping` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int BufferMapThreshold` | partial | `GRContextOptions.BufferMapThreshold` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |

### SkiaSharp.GRD3DBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public nint Adapter` | missing | `GRD3DBackendContext.Adapter` — No exported browser type with this declaration identity. |
| `public nint Device` | missing | `GRD3DBackendContext.Device` — No exported browser type with this declaration identity. |
| `public nint Queue` | missing | `GRD3DBackendContext.Queue` — No exported browser type with this declaration identity. |
| `public bool ProtectedContext` | missing | `GRD3DBackendContext.ProtectedContext` — No exported browser type with this declaration identity. |
| `public void Dispose ()` | missing | `GRD3DBackendContext.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.GRD3DTextureResourceInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public nint Resource` | missing | `GRD3DTextureResourceInfo.Resource` — No exported browser type with this declaration identity. |
| `public uint ResourceState` | missing | `GRD3DTextureResourceInfo.ResourceState` — No exported browser type with this declaration identity. |
| `public uint Format` | missing | `GRD3DTextureResourceInfo.Format` — No exported browser type with this declaration identity. |
| `public uint SampleCount` | missing | `GRD3DTextureResourceInfo.SampleCount` — No exported browser type with this declaration identity. |
| `public uint LevelCount` | missing | `GRD3DTextureResourceInfo.LevelCount` — No exported browser type with this declaration identity. |
| `public uint SampleQualityPattern` | missing | `GRD3DTextureResourceInfo.SampleQualityPattern` — No exported browser type with this declaration identity. |
| `public bool Protected` | missing | `GRD3DTextureResourceInfo.Protected` — No exported browser type with this declaration identity. |
| `public void Dispose ()` | missing | `GRD3DTextureResourceInfo.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.GRGlBackendState

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `None = 0` | implemented | `GRGlBackendState.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RenderTarget = 1 << 0` | implemented | `GRGlBackendState.RenderTarget` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `TextureBinding = 1 << 1` | implemented | `GRGlBackendState.TextureBinding` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `View = 1 << 2, // scissor and viewport` | implemented | `GRGlBackendState.View` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Blend = 1 << 3` | implemented | `GRGlBackendState.Blend` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `MSAAEnable = 1 << 4` | implemented | `GRGlBackendState.MSAAEnable` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Vertex = 1 << 5` | implemented | `GRGlBackendState.Vertex` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Stencil = 1 << 6` | implemented | `GRGlBackendState.Stencil` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `PixelStore = 1 << 7` | implemented | `GRGlBackendState.PixelStore` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Program = 1 << 8` | implemented | `GRGlBackendState.Program` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `FixedFunction = 1 << 9` | implemented | `GRGlBackendState.FixedFunction` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Misc = 1 << 10` | implemented | `GRGlBackendState.Misc` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `PathRendering = 1 << 11` | implemented | `GRGlBackendState.PathRendering` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `All = 0xffff` | implemented | `GRGlBackendState.All` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.GRBackendState

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `None = 0` | implemented | `GRBackendState.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `All = 0xffffffff` | implemented | `GRBackendState.All` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.GRGlFramebufferInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRGlFramebufferInfo (uint fboId)` | partial | `GRGlFramebufferInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRGlFramebufferInfo (uint fboId, uint format)` | partial | `GRGlFramebufferInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public UInt32 FramebufferObjectId` | partial | `GRGlFramebufferInfo.FramebufferObjectId` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public UInt32 Format` | partial | `GRGlFramebufferInfo.Format` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool Protected` | partial | `GRGlFramebufferInfo.Protected` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (GRGlFramebufferInfo obj)` | partial | `GRGlFramebufferInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `GRGlFramebufferInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (GRGlFramebufferInfo left, GRGlFramebufferInfo right)` | not-applicable | `GRGlFramebufferInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRGlFramebufferInfo left, GRGlFramebufferInfo right)` | not-applicable | `GRGlFramebufferInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `GRGlFramebufferInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRGlTextureInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRGlTextureInfo (uint target, uint id)` | partial | `GRGlTextureInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRGlTextureInfo (uint target, uint id, uint format)` | partial | `GRGlTextureInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public UInt32 Target` | partial | `GRGlTextureInfo.Target` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public UInt32 Id` | partial | `GRGlTextureInfo.Id` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public UInt32 Format` | partial | `GRGlTextureInfo.Format` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool Protected` | partial | `GRGlTextureInfo.Protected` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (GRGlTextureInfo obj)` | partial | `GRGlTextureInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `GRGlTextureInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (GRGlTextureInfo left, GRGlTextureInfo right)` | not-applicable | `GRGlTextureInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRGlTextureInfo left, GRGlTextureInfo right)` | not-applicable | `GRGlTextureInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `GRGlTextureInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRMtlTextureInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRMtlTextureInfo (IntPtr textureHandle)` | not-applicable | `GRMtlTextureInfo(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr TextureHandle` | not-applicable | `GRMtlTextureInfo.TextureHandle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRMtlTextureInfo (Metal.IMTLTexture texture)` | missing | `GRMtlTextureInfo(...)` — No exported browser type with this declaration identity. |
| `public Metal.IMTLTexture Texture` | missing | `GRMtlTextureInfo.Texture` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (GRMtlTextureInfo obj)` | missing | `GRMtlTextureInfo.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `GRMtlTextureInfo.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (GRMtlTextureInfo left, GRMtlTextureInfo right)` | not-applicable | `GRMtlTextureInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRMtlTextureInfo left, GRMtlTextureInfo right)` | not-applicable | `GRMtlTextureInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `GRMtlTextureInfo.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.GrVkYcbcrConversionInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt32 Format` | missing | `GrVkYcbcrConversionInfo.Format` — No exported browser type with this declaration identity. |
| `public UInt64 ExternalFormat` | missing | `GrVkYcbcrConversionInfo.ExternalFormat` — No exported browser type with this declaration identity. |
| `public UInt32 YcbcrModel` | missing | `GrVkYcbcrConversionInfo.YcbcrModel` — No exported browser type with this declaration identity. |
| `public UInt32 YcbcrRange` | missing | `GrVkYcbcrConversionInfo.YcbcrRange` — No exported browser type with this declaration identity. |
| `public UInt32 XChromaOffset` | missing | `GrVkYcbcrConversionInfo.XChromaOffset` — No exported browser type with this declaration identity. |
| `public UInt32 YChromaOffset` | missing | `GrVkYcbcrConversionInfo.YChromaOffset` — No exported browser type with this declaration identity. |
| `public UInt32 ChromaFilter` | missing | `GrVkYcbcrConversionInfo.ChromaFilter` — No exported browser type with this declaration identity. |
| `public UInt32 ForceExplicitReconstruction` | missing | `GrVkYcbcrConversionInfo.ForceExplicitReconstruction` — No exported browser type with this declaration identity. |
| `public GRVkYcbcrComponents Components` | missing | `GrVkYcbcrConversionInfo.Components` — No exported browser type with this declaration identity. |
| `public UInt32 FormatFeatures` | missing | `GrVkYcbcrConversionInfo.FormatFeatures` — No exported browser type with this declaration identity. |
| `public bool SamplerFilterMustMatchChromaFilter` | missing | `GrVkYcbcrConversionInfo.SamplerFilterMustMatchChromaFilter` — No exported browser type with this declaration identity. |
| `public bool SupportsLinearFilter` | missing | `GrVkYcbcrConversionInfo.SupportsLinearFilter` — No exported browser type with this declaration identity. |
| `public static implicit operator GRVkYcbcrConversionInfo (GrVkYcbcrConversionInfo value)` | not-applicable | `new GrVkYcbcrConversionInfo(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator GrVkYcbcrConversionInfo (GRVkYcbcrConversionInfo value)` | not-applicable | `new GrVkYcbcrConversionInfo(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.GRGlInterface

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static GRGlInterface Create ()` | partial | `GRGlInterface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateAngle ()` | partial | `GRGlInterface.CreateAngle` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface Create (GRGlGetProcedureAddressDelegate get)` | partial | `GRGlInterface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateAngle (GRGlGetProcedureAddressDelegate get)` | partial | `GRGlInterface.CreateAngle` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateOpenGl (GRGlGetProcedureAddressDelegate get)` | partial | `GRGlInterface.CreateOpenGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateGles (GRGlGetProcedureAddressDelegate get)` | partial | `GRGlInterface.CreateGles` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateWebGl (GRGlGetProcedureAddressDelegate get)` | partial | `GRGlInterface.CreateWebGl` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static GRGlInterface CreateEvas (IntPtr evas)` | not-applicable | `GRGlInterface.CreateEvas` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool Validate ()` | partial | `GRGlInterface.Validate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool HasExtension (string extension)` | partial | `GRGlInterface.HasExtension` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRMtlBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public IntPtr DeviceHandle` | not-applicable | `GRMtlBackendContext.DeviceHandle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr QueueHandle` | not-applicable | `GRMtlBackendContext.QueueHandle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IMTLDevice Device` | missing | `GRMtlBackendContext.Device` — No exported browser type with this declaration identity. |
| `public IMTLCommandQueue Queue` | missing | `GRMtlBackendContext.Queue` — No exported browser type with this declaration identity. |
| `public void Dispose ()` | missing | `GRMtlBackendContext.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.GRRecordingContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public virtual GRBackend Backend` | partial | `GRRecordingContext.Backend` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public virtual bool IsAbandoned` | partial | `GRRecordingContext.IsAbandoned` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int MaxTextureSize` | partial | `GRRecordingContext.MaxTextureSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int MaxRenderTargetSize` | partial | `GRRecordingContext.MaxRenderTargetSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GetMaxSurfaceSampleCount (SKColorType colorType)` | partial | `GRRecordingContext.GetMaxSurfaceSampleCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.GRVkBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public void Dispose ()` | missing | `GRVkBackendContext.Dispose` — No exported browser type with this declaration identity. |
| `public IntPtr VkInstance` | not-applicable | `GRVkBackendContext.VkInstance` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkPhysicalDevice` | not-applicable | `GRVkBackendContext.VkPhysicalDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkDevice` | not-applicable | `GRVkBackendContext.VkDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkQueue` | not-applicable | `GRVkBackendContext.VkQueue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public UInt32 GraphicsQueueIndex` | missing | `GRVkBackendContext.GraphicsQueueIndex` — No exported browser type with this declaration identity. |
| `public UInt32 MaxAPIVersion` | missing | `GRVkBackendContext.MaxAPIVersion` — No exported browser type with this declaration identity. |
| `public GRVkExtensions Extensions` | missing | `GRVkBackendContext.Extensions` — No exported browser type with this declaration identity. |
| `public IntPtr VkPhysicalDeviceFeatures` | not-applicable | `GRVkBackendContext.VkPhysicalDeviceFeatures` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkPhysicalDeviceFeatures2` | not-applicable | `GRVkBackendContext.VkPhysicalDeviceFeatures2` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public GRVkGetProcedureAddressDelegate GetProcedureAddress` | missing | `GRVkBackendContext.GetProcedureAddress` — No exported browser type with this declaration identity. |
| `public bool ProtectedContext` | missing | `GRVkBackendContext.ProtectedContext` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public GRVkExtensions () : this (SkiaApi.gr_vk_extensions_new (), true)` | missing | `GRVkExtensions(...)` — No exported browser type with this declaration identity. |
| `public void HasExtension (string extension, int minVersion)` | missing | `GRVkExtensions.HasExtension` — No exported browser type with this declaration identity. |
| `public void Initialize (GRVkGetProcedureAddressDelegate getProc, IntPtr vkInstance, IntPtr vkPhysicalDevice)` | not-applicable | `GRVkExtensions.Initialize` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Initialize (GRVkGetProcedureAddressDelegate getProc, IntPtr vkInstance, IntPtr vkPhysicalDevice, string[] instanceExtensions, string[] deviceExtensions)` | not-applicable | `GRVkExtensions.Initialize` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static GRVkExtensions Create (GRVkGetProcedureAddressDelegate getProc, IntPtr vkInstance, IntPtr vkPhysicalDevice, string[] instanceExtensions, string[] deviceExtensions)` | not-applicable | `GRVkExtensions.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |

### SkiaSharp.SKGraphiteBackendTexture

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKGraphiteBackendTexture CreateVulkan ( int width, int height, SKGraphiteVkTextureInfo info, int imageLayout, uint queueFamilyIndex, IntPtr vkImage)` | not-applicable | `SKGraphiteBackendTexture.CreateVulkan` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKGraphiteBackendTexture CreateMetal ( int width, int height, IntPtr mtlTexture)` | not-applicable | `SKGraphiteBackendTexture.CreateMetal` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKGraphiteBackendTexture CreateDawn (IntPtr wgpuTexture)` | not-applicable | `SKGraphiteBackendTexture.CreateDawn` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool IsValid` | implemented | `SKGraphiteBackendTexture.IsValid` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public SKGraphiteBackend Backend` | implemented | `SKGraphiteBackendTexture.Backend` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public SKSizeI Dimensions` | implemented | `SKGraphiteBackendTexture.Dimensions` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |

### SkiaSharp.SKGraphiteContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static bool IsBackendAvailable (SKGraphiteBackend backend)` | partial | `SKGraphiteContext.IsBackendAvailable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateVulkan (SKGraphiteVkBackendContext backendContext)` | partial | `SKGraphiteContext.CreateVulkan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateMetal (SKGraphiteMtlBackendContext backendContext)` | partial | `SKGraphiteContext.CreateMetal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateDawn (SKGraphiteDawnBackendContext backendContext)` | partial | `SKGraphiteContext.CreateDawn` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateDawn (SKGraphiteDawnBackendContext backendContext, SKGraphiteContextOptions options)` | partial | `SKGraphiteContext.CreateDawn` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateMetal (SKGraphiteMtlBackendContext backendContext, SKGraphiteContextOptions options)` | partial | `SKGraphiteContext.CreateMetal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKGraphiteContext CreateVulkan (SKGraphiteVkBackendContext backendContext, SKGraphiteContextOptions options)` | partial | `SKGraphiteContext.CreateVulkan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKGraphiteBackend Backend` | partial | `SKGraphiteContext.Backend` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsDeviceLost` | partial | `SKGraphiteContext.IsDeviceLost` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int MaxTextureSize` | implemented | `SKGraphiteContext.MaxTextureSize` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public bool SupportsProtectedContent` | partial | `SKGraphiteContext.SupportsProtectedContent` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long CurrentBudgetedBytes` | partial | `SKGraphiteContext.CurrentBudgetedBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long MaxBudgetedBytes` | partial | `SKGraphiteContext.MaxBudgetedBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKGraphiteRecorder CreateRecorder (long recorderBudgetBytes = -1)` | implemented | `SKGraphiteContext.CreateRecorder` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public SKGraphiteRecorder CreateRecorder ( long recorderBudgetBytes, SKGraphiteFindOrCreateImageDelegate findOrCreate, Action findOrCreateDispose = null)` | partial | `SKGraphiteContext.CreateRecorder` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKGraphiteInsertStatus InsertRecording (SKGraphiteRecording recording)` | implemented | `SKGraphiteContext.InsertRecording` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public SKGraphiteInsertStatus InsertRecording (SKGraphiteInsertRecordingInfo info)` | partial | `SKGraphiteContext.InsertRecording` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Submit ()` | implemented | `SKGraphiteContext.Submit` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public bool Submit (SKGraphiteSubmitInfo submitInfo)` | partial | `SKGraphiteContext.Submit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void FreeGpuResources ()` | partial | `SKGraphiteContext.FreeGpuResources` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void PerformDeferredCleanup (TimeSpan duration)` | partial | `SKGraphiteContext.PerformDeferredCleanup` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DeleteBackendTexture (SKGraphiteBackendTexture backendTexture)` | partial | `SKGraphiteContext.DeleteBackendTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void CheckAsyncWorkCompletion ()` | partial | `SKGraphiteContext.CheckAsyncWorkCompletion` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void RequestReadPixels ( SKSurface surface, SKImageInfo dstInfo, SKRectI srcRect, SKImageRescaleGamma rescaleGamma, SKImageRescaleMode rescaleMode, Action<SKImageReadPixelsResult> callback)` | implemented | `SKGraphiteContext.RequestReadPixels` — Both callback signatures dispatch rescale gamma/mode and preserve native byte stride, color/alpha/color-space and ownership. Unit checks include failures and disposal; Chromium Graphite F16-to-F32 HDR pixels match a color-managed raster reference. JavaScript returns an awaitable Promise in addition to invoking the callback. |
| `public void RequestReadPixels ( SKSurface surface, SKImageInfo dstInfo, SKRectI srcRect, Action<SKImageReadPixelsResult> callback)` | implemented | `SKGraphiteContext.RequestReadPixels` — Both callback signatures dispatch rescale gamma/mode and preserve native byte stride, color/alpha/color-space and ownership. Unit checks include failures and disposal; Chromium Graphite F16-to-F32 HDR pixels match a color-managed raster reference. JavaScript returns an awaitable Promise in addition to invoking the callback. |

### SkiaSharp.SKGraphiteDawnBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public IntPtr WgpuInstance` | not-applicable | `SKGraphiteDawnBackendContext.WgpuInstance` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr WgpuDevice` | not-applicable | `SKGraphiteDawnBackendContext.WgpuDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr WgpuQueue` | not-applicable | `SKGraphiteDawnBackendContext.WgpuQueue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Dispose ()` | partial | `SKGraphiteDawnBackendContext.Dispose` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteImageCache

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKImage FindOrCreate (SKGraphiteRecorder recorder, SKImage image, bool mipmapped)` | implemented | `SKGraphiteImageCache.FindOrCreate` — Native Graphite texture cache tested on Dawn: independent returned ownership, same-image aliases, mipmaps, recorder isolation, LRU eviction, disposal before submission and exact GPU pixels. |
| `public void Dispose ()` | implemented | `SKGraphiteImageCache.Dispose` — Native Graphite texture cache tested on Dawn: independent returned ownership, same-image aliases, mipmaps, recorder isolation, LRU eviction, disposal before submission and exact GPU pixels. |

### SkiaSharp.SKGraphiteMtlBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public IntPtr MtlDevice` | not-applicable | `SKGraphiteMtlBackendContext.MtlDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr MtlQueue` | not-applicable | `SKGraphiteMtlBackendContext.MtlQueue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IMTLDevice Device` | missing | `SKGraphiteMtlBackendContext.Device` — No exported browser type with this declaration identity. |
| `public IMTLCommandQueue Queue` | missing | `SKGraphiteMtlBackendContext.Queue` — No exported browser type with this declaration identity. |
| `public void Dispose ()` | missing | `SKGraphiteMtlBackendContext.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.SKGraphiteRecorder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGraphiteBackend Backend` | partial | `SKGraphiteRecorder.Backend` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int MaxTextureSize` | partial | `SKGraphiteRecorder.MaxTextureSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKGraphiteRecording Snap ()` | implemented | `SKGraphiteRecorder.Snap` — Real Dawn Graphite smoke executes native recorder snap, insertion, default submit and renderer resource setup with exact readback and zero retained device/queue/texture handles. Nondefault callback/synchronous/targeted variants remain separately partial. |
| `public SKGraphiteBackendTexture CreateBackendTexture (int width, int height, SKGraphiteTextureInfo info)` | partial | `SKGraphiteRecorder.CreateBackendTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DeleteBackendTexture (SKGraphiteBackendTexture backendTexture)` | partial | `SKGraphiteRecorder.DeleteBackendTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteTextureInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKGraphiteTextureInfo CreateVulkan (SKGraphiteVkTextureInfo info)` | partial | `SKGraphiteTextureInfo.CreateVulkan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsValid` | partial | `SKGraphiteTextureInfo.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKGraphiteBackend Backend` | partial | `SKGraphiteTextureInfo.Backend` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int SampleCount` | partial | `SKGraphiteTextureInfo.SampleCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Mipmapped` | partial | `SKGraphiteTextureInfo.Mipmapped` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteVkBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public IntPtr VkInstance` | not-applicable | `SKGraphiteVkBackendContext.VkInstance` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkPhysicalDevice` | not-applicable | `SKGraphiteVkBackendContext.VkPhysicalDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkDevice` | not-applicable | `SKGraphiteVkBackendContext.VkDevice` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr VkQueue` | not-applicable | `SKGraphiteVkBackendContext.VkQueue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public uint GraphicsQueueIndex` | missing | `SKGraphiteVkBackendContext.GraphicsQueueIndex` — No exported browser type with this declaration identity. |
| `public uint MaxApiVersion` | missing | `SKGraphiteVkBackendContext.MaxApiVersion` — No exported browser type with this declaration identity. |
| `public bool ProtectedContext` | missing | `SKGraphiteVkBackendContext.ProtectedContext` — No exported browser type with this declaration identity. |
| `public SKGraphiteVkGetProcedureAddressDelegate GetProcedureAddress` | missing | `SKGraphiteVkBackendContext.GetProcedureAddress` — No exported browser type with this declaration identity. |
| `public void Dispose ()` | missing | `SKGraphiteVkBackendContext.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.SKPoint

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKPoint Empty` | implemented | `SKPoint.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPoint (float x, float y)` | implemented | `SKPoint(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKPoint.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float Length` | implemented | `SKPoint.Length` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float LengthSquared` | implemented | `SKPoint.LengthSquared` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (SKPoint p)` | implemented | `SKPoint.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (float dx, float dy)` | implemented | `SKPoint.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKPoint.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Normalize (SKPoint point)` | implemented | `SKPoint.Normalize` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static float Distance (SKPoint point, SKPoint other)` | implemented | `SKPoint.Distance` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static float DistanceSquared (SKPoint point, SKPoint other)` | implemented | `SKPoint.DistanceSquared` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Reflect (SKPoint point, SKPoint normal)` | implemented | `SKPoint.Reflect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Add (SKPoint pt, SKSizeI sz)` | implemented | `SKPoint.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Add (SKPoint pt, SKSize sz)` | implemented | `SKPoint.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Add (SKPoint pt, SKPointI sz)` | implemented | `SKPoint.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Add (SKPoint pt, SKPoint sz)` | implemented | `SKPoint.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Subtract (SKPoint pt, SKSizeI sz)` | implemented | `SKPoint.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Subtract (SKPoint pt, SKSize sz)` | implemented | `SKPoint.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Subtract (SKPoint pt, SKPointI sz)` | implemented | `SKPoint.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint Subtract (SKPoint pt, SKPoint sz)` | implemented | `SKPoint.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPoint operator + (SKPoint pt, SKSizeI sz)` | not-applicable | `SKPoint.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator + (SKPoint pt, SKSize sz)` | not-applicable | `SKPoint.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator + (SKPoint pt, SKPointI sz)` | not-applicable | `SKPoint.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator + (SKPoint pt, SKPoint sz)` | not-applicable | `SKPoint.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator - (SKPoint pt, SKSizeI sz)` | not-applicable | `SKPoint.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator - (SKPoint pt, SKSize sz)` | not-applicable | `SKPoint.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator - (SKPoint pt, SKPointI sz)` | not-applicable | `SKPoint.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint operator - (SKPoint pt, SKPoint sz)` | not-applicable | `SKPoint.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator Vector2 (SKPoint point)` | not-applicable | `new SKPoint(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKPoint (Vector2 vector)` | not-applicable | `new SKPoint(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Single X` | implemented | `SKPoint.X` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Y` | implemented | `SKPoint.Y` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKPoint obj)` | implemented | `SKPoint.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKPoint.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKPoint left, SKPoint right)` | not-applicable | `SKPoint.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKPoint left, SKPoint right)` | not-applicable | `SKPoint.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKPoint.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKPointI

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKPointI Empty` | implemented | `SKPointI.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPointI (SKSizeI sz)` | implemented | `SKPointI(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPointI (int x, int y)` | implemented | `SKPointI(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKPointI.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int Length` | implemented | `SKPointI.Length` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int LengthSquared` | implemented | `SKPointI.LengthSquared` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (SKPointI p)` | implemented | `SKPointI.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (int dx, int dy)` | implemented | `SKPointI.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKPointI.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Normalize (SKPointI point)` | partial | `SKPointI.Normalize` — Pinned integer truncation is supported; integer arithmetic overflow and zero-vector conversion need additional runtime equivalence checks. |
| `public static float Distance (SKPointI point, SKPointI other)` | implemented | `SKPointI.Distance` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static float DistanceSquared (SKPointI point, SKPointI other)` | implemented | `SKPointI.DistanceSquared` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Reflect (SKPointI point, SKPointI normal)` | implemented | `SKPointI.Reflect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Ceiling (SKPoint value)` | implemented | `SKPointI.Ceiling` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Round (SKPoint value)` | implemented | `SKPointI.Round` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Truncate (SKPoint value)` | implemented | `SKPointI.Truncate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Add (SKPointI pt, SKSizeI sz)` | implemented | `SKPointI.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Add (SKPointI pt, SKPointI sz)` | implemented | `SKPointI.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Subtract (SKPointI pt, SKSizeI sz)` | implemented | `SKPointI.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI Subtract (SKPointI pt, SKPointI sz)` | implemented | `SKPointI.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKPointI operator + (SKPointI pt, SKSizeI sz)` | not-applicable | `SKPointI.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPointI operator + (SKPointI pt, SKPointI sz)` | not-applicable | `SKPointI.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPointI operator - (SKPointI pt, SKSizeI sz)` | not-applicable | `SKPointI.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPointI operator - (SKPointI pt, SKPointI sz)` | not-applicable | `SKPointI.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator SKSizeI (SKPointI p)` | not-applicable | `new SKPointI(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKPoint (SKPointI p)` | not-applicable | `new SKPointI(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator Vector2 (SKPointI point)` | not-applicable | `new SKPointI(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Int32 X` | implemented | `SKPointI.X` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Y` | implemented | `SKPointI.Y` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKPointI obj)` | implemented | `SKPointI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKPointI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKPointI left, SKPointI right)` | not-applicable | `SKPointI.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKPointI left, SKPointI right)` | not-applicable | `SKPointI.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKPointI.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKPoint3

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKPoint3 Empty` | implemented | `SKPoint3.Empty` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKPoint3 (float x, float y, float z)` | implemented | `SKPoint3(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool IsEmpty` | implemented | `SKPoint3.IsEmpty` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly override string ToString ()` | implemented | `SKPoint3.ToString` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKPoint3 Add (SKPoint3 pt, SKPoint3 sz)` | implemented | `SKPoint3.Add` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKPoint3 Subtract (SKPoint3 pt, SKPoint3 sz)` | implemented | `SKPoint3.Subtract` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKPoint3 operator + (SKPoint3 pt, SKPoint3 sz)` | not-applicable | `SKPoint3.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKPoint3 operator - (SKPoint3 pt, SKPoint3 sz)` | not-applicable | `SKPoint3.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator Vector3 (SKPoint3 point)` | not-applicable | `new SKPoint3(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKPoint3 (Vector3 vector)` | not-applicable | `new SKPoint3(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Single X` | implemented | `SKPoint3.X` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single Y` | implemented | `SKPoint3.Y` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single Z` | implemented | `SKPoint3.Z` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool Equals (SKPoint3 obj)` | implemented | `SKPoint3.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly override bool Equals (object obj)` | implemented | `SKPoint3.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static bool operator == (SKPoint3 left, SKPoint3 right)` | not-applicable | `SKPoint3.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKPoint3 left, SKPoint3 right)` | not-applicable | `SKPoint3.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKPoint3.GetHashCode` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |

### SkiaSharp.SKSize

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKSize Empty` | implemented | `SKSize.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSize (float width, float height)` | implemented | `SKSize(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSize (SKPoint pt)` | implemented | `SKSize(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKSize.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKPoint ToPoint ()` | implemented | `SKSize.ToPoint` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKSizeI ToSizeI ()` | implemented | `SKSize.ToSizeI` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKSize.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSize Add (SKSize sz1, SKSize sz2)` | implemented | `SKSize.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSize Subtract (SKSize sz1, SKSize sz2)` | implemented | `SKSize.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSize operator + (SKSize sz1, SKSize sz2)` | not-applicable | `SKSize.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKSize operator - (SKSize sz1, SKSize sz2)` | not-applicable | `SKSize.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator SKPoint (SKSize size)` | not-applicable | `new SKSize(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKSize (SKSizeI size)` | not-applicable | `new SKSize(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Single Width` | implemented | `SKSize.Width` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Height` | implemented | `SKSize.Height` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKSize obj)` | implemented | `SKSize.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKSize.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKSize left, SKSize right)` | not-applicable | `SKSize.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKSize left, SKSize right)` | not-applicable | `SKSize.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKSize.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKSizeI

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKSizeI Empty` | implemented | `SKSizeI.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSizeI (int width, int height)` | implemented | `SKSizeI(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSizeI (SKPointI pt)` | implemented | `SKSizeI(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKSizeI.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKPointI ToPointI ()` | implemented | `SKSizeI.ToPointI` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKSizeI.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSizeI Add (SKSizeI sz1, SKSizeI sz2)` | implemented | `SKSizeI.Add` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSizeI Subtract (SKSizeI sz1, SKSizeI sz2)` | implemented | `SKSizeI.Subtract` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKSizeI operator + (SKSizeI sz1, SKSizeI sz2)` | not-applicable | `SKSizeI.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKSizeI operator - (SKSizeI sz1, SKSizeI sz2)` | not-applicable | `SKSizeI.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator SKPointI (SKSizeI size)` | not-applicable | `new SKSizeI(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Int32 Width` | implemented | `SKSizeI.Width` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Height` | implemented | `SKSizeI.Height` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKSizeI obj)` | implemented | `SKSizeI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKSizeI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKSizeI left, SKSizeI right)` | not-applicable | `SKSizeI.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKSizeI left, SKSizeI right)` | not-applicable | `SKSizeI.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKSizeI.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKRect

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKRect Empty` | implemented | `SKRect.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRect (float left, float top, float right, float bottom)` | implemented | `SKRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float MidX` | implemented | `SKRect.MidX` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float MidY` | implemented | `SKRect.MidY` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float Width` | implemented | `SKRect.Width` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly float Height` | implemented | `SKRect.Height` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKRect.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSize Size` | implemented | `SKRect.Size` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPoint Location` | implemented | `SKRect.Location` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRect Standardized` | implemented | `SKRect.Standardized` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRect AspectFit (SKSize size)` | implemented | `SKRect.AspectFit` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRect AspectFill (SKSize size)` | implemented | `SKRect.AspectFill` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Inflate (SKRect rect, float x, float y)` | implemented | `SKRect.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (SKSize size)` | implemented | `SKRect.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (float x, float y)` | implemented | `SKRect.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Intersect (SKRect a, SKRect b)` | implemented | `SKRect.Intersect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Intersect (SKRect rect)` | implemented | `SKRect.Intersect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Union (SKRect a, SKRect b)` | implemented | `SKRect.Union` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Union (SKRect rect)` | implemented | `SKRect.Union` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static implicit operator SKRect (SKRectI r)` | not-applicable | `new SKRect(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly bool Contains (float x, float y)` | implemented | `SKRect.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Contains (SKPoint pt)` | implemented | `SKRect.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Contains (SKRect rect)` | implemented | `SKRect.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IntersectsWith (SKRect rect)` | implemented | `SKRect.IntersectsWith` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IntersectsWithInclusive (SKRect rect)` | implemented | `SKRect.IntersectsWithInclusive` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (float x, float y)` | implemented | `SKRect.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (SKPoint pos)` | implemented | `SKRect.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKRect.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Create (SKPoint location, SKSize size)` | implemented | `SKRect.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Create (SKSize size)` | implemented | `SKRect.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Create (float width, float height)` | implemented | `SKRect.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRect Create (float x, float y, float width, float height)` | implemented | `SKRect.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Left` | implemented | `SKRect.Left` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Top` | implemented | `SKRect.Top` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Right` | implemented | `SKRect.Right` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Single Bottom` | implemented | `SKRect.Bottom` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKRect obj)` | implemented | `SKRect.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKRect.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKRect left, SKRect right)` | not-applicable | `SKRect.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKRect left, SKRect right)` | not-applicable | `SKRect.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKRect.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.SKRectI

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKRectI Empty` | implemented | `SKRectI.Empty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRectI (int left, int top, int right, int bottom)` | implemented | `SKRectI(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int MidX` | implemented | `SKRectI.MidX` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int MidY` | implemented | `SKRectI.MidY` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int Width` | implemented | `SKRectI.Width` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly int Height` | implemented | `SKRectI.Height` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IsEmpty` | implemented | `SKRectI.IsEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKSizeI Size` | implemented | `SKRectI.Size` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPointI Location` | implemented | `SKRectI.Location` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRectI Standardized` | implemented | `SKRectI.Standardized` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRectI AspectFit (SKSizeI size)` | implemented | `SKRectI.AspectFit` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly SKRectI AspectFill (SKSizeI size)` | implemented | `SKRectI.AspectFill` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Ceiling (SKRect value)` | implemented | `SKRectI.Ceiling` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Ceiling (SKRect value, bool outwards)` | implemented | `SKRectI.Ceiling` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Inflate (SKRectI rect, int x, int y)` | implemented | `SKRectI.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (SKSizeI size)` | implemented | `SKRectI.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (int width, int height)` | implemented | `SKRectI.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Intersect (SKRectI a, SKRectI b)` | implemented | `SKRectI.Intersect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Intersect (SKRectI rect)` | implemented | `SKRectI.Intersect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Round (SKRect value)` | implemented | `SKRectI.Round` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Floor (SKRect value)` | implemented | `SKRectI.Floor` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Floor (SKRect value, bool inwards)` | implemented | `SKRectI.Floor` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Truncate (SKRect value)` | implemented | `SKRectI.Truncate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Union (SKRectI a, SKRectI b)` | implemented | `SKRectI.Union` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Union (SKRectI rect)` | implemented | `SKRectI.Union` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Contains (int x, int y)` | implemented | `SKRectI.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Contains (SKPointI pt)` | implemented | `SKRectI.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Contains (SKRectI rect)` | implemented | `SKRectI.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IntersectsWith (SKRectI rect)` | implemented | `SKRectI.IntersectsWith` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool IntersectsWithInclusive (SKRectI rect)` | implemented | `SKRectI.IntersectsWithInclusive` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (int x, int y)` | implemented | `SKRectI.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (SKPointI pos)` | implemented | `SKRectI.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override string ToString ()` | implemented | `SKRectI.ToString` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Create (SKSizeI size)` | implemented | `SKRectI.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Create (SKPointI location, SKSizeI size)` | implemented | `SKRectI.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Create (int width, int height)` | implemented | `SKRectI.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static SKRectI Create (int x, int y, int width, int height)` | implemented | `SKRectI.Create` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Left` | implemented | `SKRectI.Left` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Top` | implemented | `SKRectI.Top` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Right` | implemented | `SKRectI.Right` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public Int32 Bottom` | implemented | `SKRectI.Bottom` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly bool Equals (SKRectI obj)` | implemented | `SKRectI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKRectI.Equals` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public static bool operator == (SKRectI left, SKRectI right)` | not-applicable | `SKRectI.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKRectI left, SKRectI right)` | not-applicable | `SKRectI.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKRectI.GetHashCode` — Available, but exact .NET hash algorithm conformance is not proven by the current tests. |

### SkiaSharp.Internals.PlatformLock

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static IPlatformLock Create ()` | missing | `Internals.PlatformLock.Create` — No exported browser type with this declaration identity. |
| `public static Func<IPlatformLock> Factory` | missing | `Internals.PlatformLock.Factory` — No exported browser type with this declaration identity. |
| `public static IPlatformLock DefaultFactory ()` | missing | `Internals.PlatformLock.DefaultFactory` — No exported browser type with this declaration identity. |
| `public void EnterReadLock ()` | missing | `Internals.PlatformLock.EnterReadLock` — No exported browser type with this declaration identity. |
| `public void ExitReadLock ()` | missing | `Internals.PlatformLock.ExitReadLock` — No exported browser type with this declaration identity. |
| `public void EnterWriteLock ()` | missing | `Internals.PlatformLock.EnterWriteLock` — No exported browser type with this declaration identity. |
| `public void ExitWriteLock ()` | missing | `Internals.PlatformLock.ExitWriteLock` — No exported browser type with this declaration identity. |
| `public void EnterUpgradeableReadLock ()` | missing | `Internals.PlatformLock.EnterUpgradeableReadLock` — No exported browser type with this declaration identity. |
| `public void ExitUpgradeableReadLock ()` | missing | `Internals.PlatformLock.ExitUpgradeableReadLock` — No exported browser type with this declaration identity. |

### SkiaSharp.SKAutoCoInitialize

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKAutoCoInitialize()` | missing | `SKAutoCoInitialize(...)` — No exported browser type with this declaration identity. |
| `public bool Initialized` | missing | `SKAutoCoInitialize.Initialized` — No exported browser type with this declaration identity. |
| `public void Uninitialize()` | missing | `SKAutoCoInitialize.Uninitialize` — No exported browser type with this declaration identity. |
| `public void Dispose()` | missing | `SKAutoCoInitialize.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.SKBitmap

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKBitmap () : this (SkiaApi.sk_bitmap_new (), true)` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (int width, int height, bool isOpaque = false) : this (width, height, SKImageInfo.PlatformColorType, isOpaque ? SKAlphaType.Opaque : SKAlphaType.Premul)` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (int width, int height, SKColorType colorType, SKAlphaType alphaType) : this (new SKImageInfo (width, height, colorType, alphaType))` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (int width, int height, SKColorType colorType, SKAlphaType alphaType, SKColorSpace colorspace) : this (new SKImageInfo (width, height, colorType, alphaType, colorspace))` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (SKImageInfo info) : this (info, info.RowBytes)` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (SKImageInfo info, int rowBytes) : this ()` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap (SKImageInfo info, SKBitmapAllocFlags flags) : this ()` | partial | `SKBitmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryAllocPixels (SKImageInfo info)` | partial | `SKBitmap.TryAllocPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryAllocPixels (SKImageInfo info, int rowBytes)` | partial | `SKBitmap.TryAllocPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryAllocPixels (SKImageInfo info, SKBitmapAllocFlags flags)` | partial | `SKBitmap.TryAllocPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Reset ()` | partial | `SKBitmap.Reset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetImmutable ()` | partial | `SKBitmap.SetImmutable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Erase (SKColor color)` | partial | `SKBitmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Erase (SKColor color, SKRectI rect)` | partial | `SKBitmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr GetAddress (int x, int y)` | not-applicable | `SKBitmap.GetAddress` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKColor GetPixel (int x, int y)` | partial | `SKBitmap.GetPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetPixel (int x, int y, SKColor color)` | partial | `SKBitmap.SetPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool CanCopyTo (SKColorType colorType)` | implemented | `SKBitmap.CanCopyTo` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKBitmap Copy ()` | partial | `SKBitmap.Copy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap Copy (SKColorType colorType)` | partial | `SKBitmap.Copy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool CopyTo (SKBitmap destination)` | partial | `SKBitmap.CopyTo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool CopyTo (SKBitmap destination, SKColorType colorType)` | partial | `SKBitmap.CopyTo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ExtractSubset (SKBitmap destination, SKRectI subset)` | partial | `SKBitmap.ExtractSubset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ExtractAlpha (SKBitmap destination)` | implemented | `SKBitmap.ExtractAlpha` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ExtractAlpha (SKBitmap destination, out SKPointI offset)` | implemented | `SKBitmap.ExtractAlpha` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ExtractAlpha (SKBitmap destination, SKPaint paint)` | implemented | `SKBitmap.ExtractAlpha` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ExtractAlpha (SKBitmap destination, SKPaint paint, out SKPointI offset)` | implemented | `SKBitmap.ExtractAlpha` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadyToDraw` | partial | `SKBitmap.ReadyToDraw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo Info` | partial | `SKBitmap.Info` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `SKBitmap.Width` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Height` | partial | `SKBitmap.Height` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorType ColorType` | partial | `SKBitmap.ColorType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKAlphaType AlphaType` | partial | `SKBitmap.AlphaType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorSpace ColorSpace` | partial | `SKBitmap.ColorSpace` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int BytesPerPixel` | partial | `SKBitmap.BytesPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int RowBytes` | partial | `SKBitmap.RowBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int ByteCount` | partial | `SKBitmap.ByteCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr GetPixels ()` | not-applicable | `SKBitmap.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public Span<byte> GetPixelSpan ()` | partial | `SKBitmap.GetPixelSpan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Span<byte> GetPixelSpan (int x, int y)` | partial | `SKBitmap.GetPixelSpan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr GetPixels (out IntPtr length)` | not-applicable | `SKBitmap.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void SetPixels (IntPtr pixels)` | not-applicable | `SKBitmap.SetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public byte[] Bytes` | partial | `SKBitmap.Bytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColor[] Pixels` | partial | `SKBitmap.Pixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsEmpty` | partial | `SKBitmap.IsEmpty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsNull` | partial | `SKBitmap.IsNull` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool DrawsNothing` | partial | `SKBitmap.DrawsNothing` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsImmutable` | partial | `SKBitmap.IsImmutable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (Stream stream)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (SKStream stream)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (SKData data)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (string filename)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (byte[] buffer)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageInfo DecodeBounds (ReadOnlySpan<byte> buffer)` | partial | `SKBitmap.DecodeBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKCodec codec)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKCodec codec, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (Stream stream)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (Stream stream, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKStream stream)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKStream stream, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKData data)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (SKData data, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (string filename)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (string filename, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (byte[] buffer)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (byte[] buffer, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (ReadOnlySpan<byte> buffer)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap Decode (ReadOnlySpan<byte> buffer, SKImageInfo bitmapInfo)` | partial | `SKBitmap.Decode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool InstallPixels (SKImageInfo info, IntPtr pixels)` | not-applicable | `SKBitmap.InstallPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool InstallPixels (SKImageInfo info, IntPtr pixels, int rowBytes)` | not-applicable | `SKBitmap.InstallPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool InstallPixels (SKImageInfo info, IntPtr pixels, int rowBytes, SKBitmapReleaseDelegate releaseProc)` | not-applicable | `SKBitmap.InstallPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool InstallPixels (SKImageInfo info, IntPtr pixels, int rowBytes, SKBitmapReleaseDelegate releaseProc, object context)` | not-applicable | `SKBitmap.InstallPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool InstallPixels (SKPixmap pixmap)` | partial | `SKBitmap.InstallPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void NotifyPixelsChanged ()` | partial | `SKBitmap.NotifyPixelsChanged` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPixmap PeekPixels ()` | partial | `SKBitmap.PeekPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool PeekPixels (SKPixmap pixmap)` | partial | `SKBitmap.PeekPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap Resize (SKImageInfo info, SKFilterQuality quality)` | partial | `SKBitmap.Resize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap Resize (SKSizeI size, SKFilterQuality quality)` | partial | `SKBitmap.Resize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap Resize (SKImageInfo info, SKSamplingOptions sampling)` | partial | `SKBitmap.Resize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap Resize (SKSizeI size, SKSamplingOptions sampling)` | partial | `SKBitmap.Resize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKBitmap destination, SKFilterQuality quality)` | partial | `SKBitmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap destination, SKFilterQuality quality)` | partial | `SKBitmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKBitmap destination, SKSamplingOptions sampling)` | partial | `SKBitmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap destination, SKSamplingOptions sampling)` | partial | `SKBitmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKBitmap FromImage (SKImage image)` | partial | `SKBitmap.FromImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData Encode (SKEncodedImageFormat format, int quality)` | partial | `SKBitmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (Stream dst, SKEncodedImageFormat format, int quality)` | partial | `SKBitmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (SKWStream dst, SKEncodedImageFormat format, int quality)` | partial | `SKBitmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader ()` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKSamplingOptions sampling)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterQuality quality)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKMatrix localMatrix)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKSamplingOptions sampling, SKMatrix localMatrix)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterQuality quality, SKMatrix localMatrix)` | partial | `SKBitmap.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKBlender

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKBlender CreateBlendMode (SKBlendMode mode)` | implemented | `SKBlender.CreateBlendMode` — Native mode blenders and a compiled SkSL arithmetic blender implement coefficients, clamping and enforce-premultiplied-color behavior. |
| `public static SKBlender CreateArithmetic (float k1, float k2, float k3, float k4, bool enforcePMColor)` | implemented | `SKBlender.CreateArithmetic` — Native mode blenders and a compiled SkSL arithmetic blender implement coefficients, clamping and enforce-premultiplied-color behavior. |

### SkiaSharp.SKCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvas (SKBitmap bitmap) : this (IntPtr.Zero, true)` | not-applicable | `SKCanvas(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Discard ()` | partial | `SKCanvas.Discard` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool QuickReject (SKRect rect)` | partial | `SKCanvas.QuickReject` — Clip bounds and conservative rejection are available, but exact native clip-stack classification and singular transform recovery are not all exposed by CanvasKit. |
| `public bool QuickReject (SKPath path)` | partial | `SKCanvas.QuickReject` — Clip bounds and conservative rejection are available, but exact native clip-stack classification and singular transform recovery are not all exposed by CanvasKit. |
| `public int Save ()` | implemented | `SKCanvas.Save` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public int SaveLayer (SKRect limit, SKPaint? paint)` | implemented | `SKCanvas.SaveLayer` — No-arg, paint, bounds+paint, and SKCanvasSaveLayerRec map to native CanvasKit saveLayer, including backdrop and flags. Opacity regression passed. |
| `public int SaveLayer (SKPaint? paint)` | implemented | `SKCanvas.SaveLayer` — No-arg, paint, bounds+paint, and SKCanvasSaveLayerRec map to native CanvasKit saveLayer, including backdrop and flags. Opacity regression passed. |
| `public int SaveLayer (in SKCanvasSaveLayerRec rec)` | implemented | `SKCanvas.SaveLayer` — No-arg, paint, bounds+paint, and SKCanvasSaveLayerRec map to native CanvasKit saveLayer, including backdrop and flags. Opacity regression passed. |
| `public int SaveLayer ()` | implemented | `SKCanvas.SaveLayer` — No-arg, paint, bounds+paint, and SKCanvasSaveLayerRec map to native CanvasKit saveLayer, including backdrop and flags. Opacity regression passed. |
| `public void DrawColor (SKColor color, SKBlendMode mode = SKBlendMode.Src)` | implemented | `SKCanvas.DrawColor` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawColor (SKColorF color, SKBlendMode mode = SKBlendMode.Src)` | implemented | `SKCanvas.DrawColor` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawLine (SKPoint p0, SKPoint p1, SKPaint paint)` | implemented | `SKCanvas.DrawLine` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawLine (float x0, float y0, float x1, float y1, SKPaint paint)` | implemented | `SKCanvas.DrawLine` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Clear ()` | implemented | `SKCanvas.Clear` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Clear (SKColor color)` | implemented | `SKCanvas.Clear` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Clear (SKColorF color)` | implemented | `SKCanvas.Clear` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Restore ()` | implemented | `SKCanvas.Restore` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void RestoreToCount (int count)` | implemented | `SKCanvas.RestoreToCount` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Translate (float dx, float dy)` | implemented | `SKCanvas.Translate` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Translate (SKPoint point)` | implemented | `SKCanvas.Translate` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Scale (float s)` | implemented | `SKCanvas.Scale` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Scale (float sx, float sy)` | implemented | `SKCanvas.Scale` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Scale (SKPoint size)` | implemented | `SKCanvas.Scale` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Scale (float sx, float sy, float px, float py)` | implemented | `SKCanvas.Scale` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void RotateDegrees (float degrees)` | implemented | `SKCanvas.RotateDegrees` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void RotateRadians (float radians)` | implemented | `SKCanvas.RotateRadians` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void RotateDegrees (float degrees, float px, float py)` | implemented | `SKCanvas.RotateDegrees` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void RotateRadians (float radians, float px, float py)` | implemented | `SKCanvas.RotateRadians` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Skew (float sx, float sy)` | implemented | `SKCanvas.Skew` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Skew (SKPoint skew)` | implemented | `SKCanvas.Skew` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Concat (in SKMatrix m)` | implemented | `SKCanvas.Concat` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void Concat (in SKMatrix44 m)` | implemented | `SKCanvas.Concat` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void ClipRect (SKRect rect, SKClipOperation operation = SKClipOperation.Intersect, bool antialias = false)` | partial | `SKCanvas.ClipRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ClipRoundRect (SKRoundRect rect, SKClipOperation operation = SKClipOperation.Intersect, bool antialias = false)` | partial | `SKCanvas.ClipRoundRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ClipPath (SKPath path, SKClipOperation operation = SKClipOperation.Intersect, bool antialias = false)` | partial | `SKCanvas.ClipPath` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void ClipRegion (SKRegion region, SKClipOperation operation = SKClipOperation.Intersect)` | partial | `SKCanvas.ClipRegion` — Clip bounds and conservative rejection are available, but exact native clip-stack classification and singular transform recovery are not all exposed by CanvasKit. |
| `public SKRect LocalClipBounds` | implemented | `SKCanvas.LocalClipBounds` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public SKRectI DeviceClipBounds` | partial | `SKCanvas.DeviceClipBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsClipEmpty` | partial | `SKCanvas.IsClipEmpty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsClipRect` | implemented | `SKCanvas.IsClipRect` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public bool GetLocalClipBounds (out SKRect bounds)` | implemented | `SKCanvas.GetLocalClipBounds` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public bool GetDeviceClipBounds (out SKRectI bounds)` | partial | `SKCanvas.GetDeviceClipBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DrawPaint (SKPaint paint)` | implemented | `SKCanvas.DrawPaint` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRegion (SKRegion region, SKPaint paint)` | partial | `SKCanvas.DrawRegion` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DrawRect (float x, float y, float w, float h, SKPaint paint)` | implemented | `SKCanvas.DrawRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRect (SKRect rect, SKPaint paint)` | implemented | `SKCanvas.DrawRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRoundRect (SKRoundRect rect, SKPaint paint)` | implemented | `SKCanvas.DrawRoundRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRoundRect (float x, float y, float w, float h, float rx, float ry, SKPaint paint)` | implemented | `SKCanvas.DrawRoundRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRoundRect (SKRect rect, float rx, float ry, SKPaint paint)` | implemented | `SKCanvas.DrawRoundRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRoundRect (SKRect rect, SKSize r, SKPaint paint)` | implemented | `SKCanvas.DrawRoundRect` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawOval (float cx, float cy, float rx, float ry, SKPaint paint)` | implemented | `SKCanvas.DrawOval` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawOval (SKPoint c, SKSize r, SKPaint paint)` | implemented | `SKCanvas.DrawOval` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawOval (SKRect rect, SKPaint paint)` | implemented | `SKCanvas.DrawOval` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawCircle (float cx, float cy, float radius, SKPaint paint)` | implemented | `SKCanvas.DrawCircle` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawCircle (SKPoint c, float radius, SKPaint paint)` | implemented | `SKCanvas.DrawCircle` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPath (SKPath path, SKPaint paint)` | implemented | `SKCanvas.DrawPath` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPoints (SKPointMode mode, SKPoint[] points, SKPaint paint)` | implemented | `SKCanvas.DrawPoints` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPoint (SKPoint p, SKPaint paint)` | implemented | `SKCanvas.DrawPoint` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPoint (float x, float y, SKPaint paint)` | implemented | `SKCanvas.DrawPoint` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPoint (SKPoint p, SKColor color)` | implemented | `SKCanvas.DrawPoint` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPoint (float x, float y, SKColor color)` | implemented | `SKCanvas.DrawPoint` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKPoint p, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKPoint p, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, float x, float y, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, float x, float y, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKRect dest, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKRect dest, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKRect source, SKRect dest, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImage (SKImage image, SKRect source, SKRect dest, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawImage` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPicture (SKPicture picture, float x, float y, SKPaint paint = null)` | implemented | `SKCanvas.DrawPicture` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPicture (SKPicture picture, SKPoint p, SKPaint paint = null)` | implemented | `SKCanvas.DrawPicture` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPicture (SKPicture picture, in SKMatrix matrix, SKPaint paint = null)` | implemented | `SKCanvas.DrawPicture` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPicture (SKPicture picture, SKPaint paint = null)` | implemented | `SKCanvas.DrawPicture` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawDrawable (SKDrawable drawable, in SKMatrix matrix)` | partial | `SKCanvas.DrawDrawable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DrawDrawable (SKDrawable drawable, float x, float y)` | partial | `SKCanvas.DrawDrawable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DrawDrawable (SKDrawable drawable, SKPoint p)` | partial | `SKCanvas.DrawDrawable` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void DrawBitmap (SKBitmap bitmap, SKPoint p, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, float x, float y, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, SKRect dest, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, SKRect source, SKRect dest, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, SKPoint p, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, float x, float y, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, SKRect dest, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmap (SKBitmap bitmap, SKRect source, SKRect dest, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmap` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawSurface (SKSurface surface, SKPoint p, SKPaint paint = null)` | implemented | `SKCanvas.DrawSurface` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawSurface (SKSurface surface, float x, float y, SKPaint paint = null)` | implemented | `SKCanvas.DrawSurface` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawSurface (SKSurface surface, SKPoint p, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawSurface` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawSurface (SKSurface surface, float x, float y, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawSurface` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (SKTextBlob text, float x, float y, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, SKPoint p, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, float x, float y, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, SKPoint p, SKFont font, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, SKPoint p, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, float x, float y, SKFont font, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawText (string text, float x, float y, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `SKCanvas.DrawText` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, float hOffset, float vOffset, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, bool warpGlyphs, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, SKTextAlign textAlign, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, float hOffset, float vOffset, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, float hOffset, float vOffset, SKTextAlign textAlign, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, bool warpGlyphs, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public void DrawTextOnPath (string text, SKPath path, SKPoint offset, bool warpGlyphs, SKTextAlign textAlign, SKFont font, SKPaint paint)` | partial | `SKCanvas.DrawTextOnPath` — All listed argument forms are accepted. Rigid glyph outlines and adaptive warped glyph outlines render; raster-only color glyphs and exact native warp tolerance remain font/backend-dependent. |
| `public SKSurface? Surface` | partial | `SKCanvas.Surface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public GRRecordingContext? Context` | partial | `SKCanvas.Context` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush ()` | implemented | `SKCanvas.Flush` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAnnotation (SKRect rect, string key, SKData value)` | implemented | `SKCanvas.DrawAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public void DrawUrlAnnotation (SKRect rect, SKData value)` | implemented | `SKCanvas.DrawUrlAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public SKData DrawUrlAnnotation (SKRect rect, string value)` | implemented | `SKCanvas.DrawUrlAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public void DrawNamedDestinationAnnotation (SKPoint point, SKData value)` | implemented | `SKCanvas.DrawNamedDestinationAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public SKData DrawNamedDestinationAnnotation (SKPoint point, string value)` | implemented | `SKCanvas.DrawNamedDestinationAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public void DrawLinkDestinationAnnotation (SKRect rect, SKData value)` | implemented | `SKCanvas.DrawLinkDestinationAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public SKData DrawLinkDestinationAnnotation (SKRect rect, string value)` | implemented | `SKCanvas.DrawLinkDestinationAnnotation` — Native SkCanvas annotation records preserve UTF-8 terminated string/data overloads and URL/named/link destination keys for picture/document consumers. Raster-only canvases retain annotations as metadata. |
| `public void DrawBitmapNinePatch (SKBitmap bitmap, SKRectI center, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapNinePatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmapNinePatch (SKBitmap bitmap, SKRectI center, SKRect dst, SKFilterMode filterMode, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapNinePatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageNinePatch (SKImage image, SKRectI center, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageNinePatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageNinePatch (SKImage image, SKRectI center, SKRect dst, SKFilterMode filterMode = SKFilterMode.Nearest, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageNinePatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmapLattice (SKBitmap bitmap, int[] xDivs, int[] yDivs, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmapLattice (SKBitmap bitmap, int[] xDivs, int[] yDivs, SKRect dst, SKFilterMode filterMode, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageLattice (SKImage image, int[] xDivs, int[] yDivs, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageLattice (SKImage image, int[] xDivs, int[] yDivs, SKRect dst, SKFilterMode filterMode, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmapLattice (SKBitmap bitmap, SKLattice lattice, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawBitmapLattice (SKBitmap bitmap, SKLattice lattice, SKRect dst, SKFilterMode filterMode, SKPaint paint = null)` | implemented | `SKCanvas.DrawBitmapLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageLattice (SKImage image, SKLattice lattice, SKRect dst, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawImageLattice (SKImage image, SKLattice lattice, SKRect dst, SKFilterMode filterMode, SKPaint paint = null)` | implemented | `SKCanvas.DrawImageLattice` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void ResetMatrix ()` | implemented | `SKCanvas.ResetMatrix` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public void SetMatrix (in SKMatrix matrix)` | implemented | `SKCanvas.SetMatrix` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public void SetMatrix (SKMatrix matrix)` | implemented | `SKCanvas.SetMatrix` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public void SetMatrix (in SKMatrix44 matrix)` | implemented | `SKCanvas.SetMatrix` — Native SkCanvas setMatrix and clip accessors, not inverse-concat approximations. Singular reset, matrix44 row-vector adaptation, transformed clip bounds, circle/rectangle classification and saved-state restoration pass native-canvas-boundary tests. |
| `public SKMatrix TotalMatrix` | partial | `SKCanvas.TotalMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKMatrix44 TotalMatrix44` | partial | `SKCanvas.TotalMatrix44` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int SaveCount` | implemented | `SKCanvas.SaveCount` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawVertices (SKVertexMode vmode, SKPoint[] vertices, SKColor[] colors, SKPaint paint)` | implemented | `SKCanvas.DrawVertices` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawVertices (SKVertexMode vmode, SKPoint[] vertices, SKPoint[] texs, SKColor[] colors, SKPaint paint)` | implemented | `SKCanvas.DrawVertices` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawVertices (SKVertexMode vmode, SKPoint[] vertices, SKPoint[] texs, SKColor[] colors, UInt16[] indices, SKPaint paint)` | implemented | `SKCanvas.DrawVertices` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawVertices (SKVertexMode vmode, SKPoint[] vertices, SKPoint[] texs, SKColor[] colors, SKBlendMode mode, UInt16[] indices, SKPaint paint)` | implemented | `SKCanvas.DrawVertices` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawVertices (SKVertices vertices, SKBlendMode mode, SKPaint paint)` | implemented | `SKCanvas.DrawVertices` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawArc (SKRect oval, float startAngle, float sweepAngle, bool useCenter, SKPaint paint)` | implemented | `SKCanvas.DrawArc` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawRoundRectDifference (SKRoundRect outer, SKRoundRect inner, SKPaint paint)` | implemented | `SKCanvas.DrawRoundRectDifference` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKColor[] colors, SKBlendMode mode, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKColor[] colors, SKBlendMode mode, SKSamplingOptions sampling, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKColor[] colors, SKBlendMode mode, SKRect cullRect, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawAtlas (SKImage atlas, SKRect[] sprites, SKRotationScaleMatrix[] transforms, SKColor[] colors, SKBlendMode mode, SKSamplingOptions sampling, SKRect cullRect, SKPaint paint = null)` | implemented | `SKCanvas.DrawAtlas` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPatch (SKPoint[] cubics, SKColor[] colors, SKPoint[] texCoords, SKPaint paint)` | implemented | `SKCanvas.DrawPatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |
| `public void DrawPatch (SKPoint[] cubics, SKColor[] colors, SKPoint[] texCoords, SKBlendMode mode, SKPaint paint)` | implemented | `SKCanvas.DrawPatch` — Signature shape explicitly dispatches to real Skia drawing/state calls; coordinates, point/rect forms, sampling, paint, and optional arguments are separated. Pixel/lattice/vertices regression cases pass. |

### SkiaSharp.SKAutoCanvasRestore

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKAutoCanvasRestore (SKCanvas canvas) : this (canvas, true)` | partial | `SKAutoCanvasRestore(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKAutoCanvasRestore (SKCanvas canvas, bool doSave)` | partial | `SKAutoCanvasRestore(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Dispose ()` | partial | `SKAutoCanvasRestore.Dispose` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Restore ()` | partial | `SKAutoCanvasRestore.Restore` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKCanvasSaveLayerRec

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRect? Bounds` | partial | `SKCanvasSaveLayerRec.Bounds` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKPaint? Paint` | partial | `SKCanvasSaveLayerRec.Paint` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKImageFilter? Backdrop` | partial | `SKCanvasSaveLayerRec.Backdrop` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKCanvasSaveLayerRecFlags Flags` | partial | `SKCanvasSaveLayerRec.Flags` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |

### SkiaSharp.SKCodec

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static int MinBufferedBytesNeeded` | partial | `SKCodec.MinBufferedBytesNeeded` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo Info` | partial | `SKCodec.Info` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKEncodedOrigin EncodedOrigin` | partial | `SKCodec.EncodedOrigin` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKEncodedImageFormat EncodedFormat` | partial | `SKCodec.EncodedFormat` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKSizeI GetScaledDimensions (float desiredScale)` | partial | `SKCodec.GetScaledDimensions` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetValidSubset (ref SKRectI desiredSubset)` | implemented | `SKCodec.GetValidSubset` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public byte[] Pixels` | partial | `SKCodec.Pixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int RepetitionCount` | partial | `SKCodec.RepetitionCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int FrameCount` | partial | `SKCodec.FrameCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCodecFrameInfo[] FrameInfo` | partial | `SKCodec.FrameInfo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetFrameInfo (int index, out SKCodecFrameInfo frameInfo)` | implemented | `SKCodec.GetFrameInfo` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKCodecResult GetPixels (out byte[] pixels)` | partial | `SKCodec.GetPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCodecResult GetPixels (SKImageInfo info, out byte[] pixels)` | partial | `SKCodec.GetPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCodecResult GetPixels (SKImageInfo info, byte[] pixels)` | partial | `SKCodec.GetPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCodecResult GetPixels (SKImageInfo info, IntPtr pixels)` | not-applicable | `SKCodec.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKCodecResult GetPixels (SKImageInfo info, IntPtr pixels, SKCodecOptions options)` | not-applicable | `SKCodec.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKCodecResult GetPixels (SKImageInfo info, IntPtr pixels, int rowBytes, SKCodecOptions options)` | not-applicable | `SKCodec.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKCodecResult StartIncrementalDecode (SKImageInfo info, IntPtr pixels, int rowBytes, SKCodecOptions options)` | not-applicable | `SKCodec.StartIncrementalDecode` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKCodecResult StartIncrementalDecode (SKImageInfo info, IntPtr pixels, int rowBytes)` | not-applicable | `SKCodec.StartIncrementalDecode` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKCodecResult IncrementalDecode (out int rowsDecoded)` | implemented | `SKCodec.IncrementalDecode` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKCodecResult IncrementalDecode ()` | implemented | `SKCodec.IncrementalDecode` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKCodecResult StartScanlineDecode (SKImageInfo info, SKCodecOptions options)` | implemented | `SKCodec.StartScanlineDecode` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKCodecResult StartScanlineDecode (SKImageInfo info)` | implemented | `SKCodec.StartScanlineDecode` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public int GetScanlines (IntPtr dst, int countLines, int rowBytes)` | not-applicable | `SKCodec.GetScanlines` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool SkipScanlines (int countLines)` | implemented | `SKCodec.SkipScanlines` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKCodecScanlineOrder ScanlineOrder` | partial | `SKCodec.ScanlineOrder` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int NextScanline` | partial | `SKCodec.NextScanline` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GetOutputScanline (int inputScanline)` | implemented | `SKCodec.GetOutputScanline` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKCodec Create (string filename)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (string filename, out SKCodecResult result)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (Stream stream)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (Stream stream, out SKCodecResult result)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (SKStream stream)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (SKStream stream, out SKCodecResult result)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKCodec Create (SKData data)` | partial | `SKCodec.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKColor

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKColor Empty` | implemented | `SKColor.Empty` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKColor (uint value)` | implemented | `SKColor(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKColor (byte red, byte green, byte blue, byte alpha)` | implemented | `SKColor(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKColor (byte red, byte green, byte blue)` | implemented | `SKColor(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColor WithRed (byte red)` | implemented | `SKColor.WithRed` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColor WithGreen (byte green)` | implemented | `SKColor.WithGreen` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColor WithBlue (byte blue)` | implemented | `SKColor.WithBlue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColor WithAlpha (byte alpha)` | implemented | `SKColor.WithAlpha` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Alpha` | implemented | `SKColor.Alpha` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Red` | implemented | `SKColor.Red` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Green` | implemented | `SKColor.Green` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Blue` | implemented | `SKColor.Blue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly float Hue` | implemented | `SKColor.Hue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColor FromHsl (float h, float s, float l, byte a = 255)` | implemented | `SKColor.FromHsl` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColor FromHsv (float h, float s, float v, byte a = 255)` | implemented | `SKColor.FromHsv` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly void ToHsl (out float h, out float s, out float l)` | implemented | `SKColor.ToHsl` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly void ToHsv (out float h, out float s, out float v)` | implemented | `SKColor.ToHsv` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly override string ToString ()` | implemented | `SKColor.ToString` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly bool Equals (SKColor obj)` | implemented | `SKColor.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly override bool Equals (object other)` | implemented | `SKColor.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static bool operator == (SKColor left, SKColor right)` | not-applicable | `SKColor.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKColor left, SKColor right)` | not-applicable | `SKColor.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKColor.GetHashCode` — Returns the signed 32-bit underlying packed value, matching the pinned implementation. |
| `public static implicit operator SKColor (uint color)` | not-applicable | `new SKColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator uint (SKColor color)` | not-applicable | `new SKColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKColor Parse (string hexString)` | implemented | `SKColor.Parse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColor Parse (ReadOnlySpan<char> hexString)` | implemented | `SKColor.Parse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static bool TryParse (string hexString, out SKColor color)` | implemented | `SKColor.TryParse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static bool TryParse (ReadOnlySpan<char> hexString, out SKColor color)` | implemented | `SKColor.TryParse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |

### SkiaSharp.SKColorF

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKColorF Empty` | implemented | `SKColorF.Empty` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKColorF (float red, float green, float blue)` | implemented | `SKColorF(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKColorF (float red, float green, float blue, float alpha)` | implemented | `SKColorF(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColorF WithRed (float red)` | implemented | `SKColorF.WithRed` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColorF WithGreen (float green)` | implemented | `SKColorF.WithGreen` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColorF WithBlue (float blue)` | implemented | `SKColorF.WithBlue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColorF WithAlpha (float alpha)` | implemented | `SKColorF.WithAlpha` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly float Hue` | implemented | `SKColorF.Hue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly SKColorF Clamp ()` | implemented | `SKColorF.Clamp` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColorF FromHsl (float h, float s, float l, float a = 1f)` | implemented | `SKColorF.FromHsl` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColorF FromHsv (float h, float s, float v, float a = 1f)` | implemented | `SKColorF.FromHsv` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly void ToHsl (out float h, out float s, out float l)` | implemented | `SKColorF.ToHsl` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly void ToHsv (out float h, out float s, out float v)` | implemented | `SKColorF.ToHsv` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly override string ToString ()` | implemented | `SKColorF.ToString` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static implicit operator SKColorF (SKColor color)` | not-applicable | `new SKColorF(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator SKColor (SKColorF color)` | not-applicable | `new SKColorF(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly Single Red` | implemented | `SKColorF.Red` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly Single Green` | implemented | `SKColorF.Green` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly Single Blue` | implemented | `SKColorF.Blue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly Single Alpha` | implemented | `SKColorF.Alpha` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly bool Equals (SKColorF obj)` | implemented | `SKColorF.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly override bool Equals (object obj)` | implemented | `SKColorF.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static bool operator == (SKColorF left, SKColorF right)` | not-applicable | `SKColorF.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKColorF left, SKColorF right)` | not-applicable | `SKColorF.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKColorF.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKColorFilter

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public const int ColorMatrixSize = 20` | partial | `SKColorFilter.ColorMatrixSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public const int TableMaxLength = 256` | partial | `SKColorFilter.TableMaxLength` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateSrgbToLinearGamma ()` | partial | `SKColorFilter.CreateSrgbToLinearGamma` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateLinearToSrgbGamma ()` | partial | `SKColorFilter.CreateLinearToSrgbGamma` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateBlendMode(SKColor c, SKBlendMode mode)` | partial | `SKColorFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateLighting(SKColor mul, SKColor add)` | implemented | `SKColorFilter.CreateLighting` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateCompose(SKColorFilter outer, SKColorFilter inner)` | partial | `SKColorFilter.CreateCompose` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateLerp(float weight, SKColorFilter filter0, SKColorFilter filter1)` | partial | `SKColorFilter.CreateLerp` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateColorMatrix(float[] matrix)` | partial | `SKColorFilter.CreateColorMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateColorMatrix(ReadOnlySpan<float> matrix)` | partial | `SKColorFilter.CreateColorMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateHslaColorMatrix(ReadOnlySpan<float> matrix)` | partial | `SKColorFilter.CreateHslaColorMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateLumaColor()` | partial | `SKColorFilter.CreateLumaColor` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKColorFilter CreateTable(byte[] table)` | implemented | `SKColorFilter.CreateTable` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateTable(ReadOnlySpan<byte> table)` | implemented | `SKColorFilter.CreateTable` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateTable(byte[] tableA, byte[] tableR, byte[] tableG, byte[] tableB)` | implemented | `SKColorFilter.CreateTable` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateTable(ReadOnlySpan<byte> tableA, ReadOnlySpan<byte> tableR, ReadOnlySpan<byte> tableG, ReadOnlySpan<byte> tableB)` | implemented | `SKColorFilter.CreateTable` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateHighContrast(SKHighContrastConfig config)` | implemented | `SKColorFilter.CreateHighContrast` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateHighContrast(bool grayscale, SKHighContrastConfigInvertStyle invertStyle, float contrast)` | implemented | `SKColorFilter.CreateHighContrast` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateOverdraw(ReadOnlySpan<SKColor> colors)` | implemented | `SKColorFilter.CreateOverdraw` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKColorFilter CreateOverdraw(SKColor[] colors)` | implemented | `SKColorFilter.CreateOverdraw` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |

### SkiaSharp.SKColorSpace

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool GammaIsCloseToSrgb` | implemented | `SKColorSpace.GammaIsCloseToSrgb` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public bool GammaIsLinear` | implemented | `SKColorSpace.GammaIsLinear` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public bool IsSrgb` | implemented | `SKColorSpace.IsSrgb` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public bool IsNumericalTransferFunction` | implemented | `SKColorSpace.IsNumericalTransferFunction` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public static bool Equal (SKColorSpace left, SKColorSpace right)` | implemented | `SKColorSpace.Equal` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public static SKColorSpace CreateSrgb ()` | implemented | `SKColorSpace.CreateSrgb` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public static SKColorSpace CreateSrgbLinear ()` | implemented | `SKColorSpace.CreateSrgbLinear` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public static SKColorSpace CreateIcc (IntPtr input, long length)` | not-applicable | `SKColorSpace.CreateIcc` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKColorSpace CreateIcc (byte[] input, long length)` | partial | `SKColorSpace.CreateIcc` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public static SKColorSpace CreateIcc (byte[] input)` | partial | `SKColorSpace.CreateIcc` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public static SKColorSpace CreateIcc (ReadOnlySpan<byte> input)` | partial | `SKColorSpace.CreateIcc` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public static SKColorSpace CreateIcc (SKData input)` | partial | `SKColorSpace.CreateIcc` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public static SKColorSpace CreateIcc (SKColorSpaceIccProfile profile)` | partial | `SKColorSpace.CreateIcc` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public static SKColorSpace CreateRgb (SKColorSpaceTransferFn transferFn, SKColorSpaceXyz toXyzD50)` | implemented | `SKColorSpace.CreateRgb` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public static SKColorSpace CreateCicp (SKColorspacePrimariesCicp colorPrimaries, SKColorspaceTransferFnCicp transferCharacteristics)` | implemented | `SKColorSpace.CreateCicp` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public SKColorSpaceTransferFn GetNumericalTransferFunction ()` | implemented | `SKColorSpace.GetNumericalTransferFunction` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public bool GetNumericalTransferFunction (out SKColorSpaceTransferFn fn)` | implemented | `SKColorSpace.GetNumericalTransferFunction` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public SKColorSpaceIccProfile ToProfile ()` | partial | `SKColorSpace.ToProfile` — Valid RGB matrix/TRC profiles roundtrip to native Skia and are rendered with color management; arbitrary LUT and HDR profile serialization need further validation. |
| `public bool ToColorSpaceXyz (out SKColorSpaceXyz toXyzD50)` | implemented | `SKColorSpace.ToColorSpaceXyz` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public SKColorSpaceXyz ToColorSpaceXyz ()` | implemented | `SKColorSpace.ToColorSpaceXyz` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public SKColorSpace ToLinearGamma ()` | implemented | `SKColorSpace.ToLinearGamma` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |
| `public SKColorSpace ToSrgbGamma ()` | implemented | `SKColorSpace.ToSrgbGamma` — Real native Skia color spaces, including RGB/CICP and linear-gamma spaces; ICC carrier fallback retains native profile color management. Float/out/matrix signatures are explicitly dispatched and native pixels verified. |

### SkiaSharp.SKColorSpacePrimaries

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKColorSpacePrimaries Empty` | implemented | `SKColorSpacePrimaries.Empty` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpacePrimaries (float[] values)` | implemented | `SKColorSpacePrimaries(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpacePrimaries (float rx, float ry, float gx, float gy, float bx, float by, float wx, float wy)` | implemented | `SKColorSpacePrimaries(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly float[] Values` | implemented | `SKColorSpacePrimaries.Values` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly bool ToColorSpaceXyz (out SKColorSpaceXyz toXyzD50)` | implemented | `SKColorSpacePrimaries.ToColorSpaceXyz` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly SKColorSpaceXyz ToColorSpaceXyz ()` | implemented | `SKColorSpacePrimaries.ToColorSpaceXyz` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single RX` | implemented | `SKColorSpacePrimaries.RX` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single RY` | implemented | `SKColorSpacePrimaries.RY` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single GX` | implemented | `SKColorSpacePrimaries.GX` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single GY` | implemented | `SKColorSpacePrimaries.GY` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single BX` | implemented | `SKColorSpacePrimaries.BX` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single BY` | implemented | `SKColorSpacePrimaries.BY` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single WX` | implemented | `SKColorSpacePrimaries.WX` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single WY` | implemented | `SKColorSpacePrimaries.WY` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly bool Equals (SKColorSpacePrimaries obj)` | implemented | `SKColorSpacePrimaries.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly override bool Equals (object obj)` | implemented | `SKColorSpacePrimaries.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static bool operator == (SKColorSpacePrimaries left, SKColorSpacePrimaries right)` | not-applicable | `SKColorSpacePrimaries.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKColorSpacePrimaries left, SKColorSpacePrimaries right)` | not-applicable | `SKColorSpacePrimaries.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKColorSpacePrimaries.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKColorSpaceTransferFn

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColorSpaceTransferFn Srgb` | implemented | `SKColorSpaceTransferFn.Srgb` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceTransferFn TwoDotTwo` | implemented | `SKColorSpaceTransferFn.TwoDotTwo` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceTransferFn Linear` | implemented | `SKColorSpaceTransferFn.Linear` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceTransferFn Rec2020` | implemented | `SKColorSpaceTransferFn.Rec2020` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceTransferFn Pq` | implemented | `SKColorSpaceTransferFn.Pq` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceTransferFn Hlg` | implemented | `SKColorSpaceTransferFn.Hlg` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static readonly SKColorSpaceTransferFn Empty` | implemented | `SKColorSpaceTransferFn.Empty` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpaceTransferFn (float[] values)` | implemented | `SKColorSpaceTransferFn(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpaceTransferFn (float g, float a, float b, float c, float d, float e, float f)` | implemented | `SKColorSpaceTransferFn(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly float[] Values` | implemented | `SKColorSpaceTransferFn.Values` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly SKColorSpaceTransferFn Invert ()` | implemented | `SKColorSpaceTransferFn.Invert` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly float Transform (float x)` | implemented | `SKColorSpaceTransferFn.Transform` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single G` | implemented | `SKColorSpaceTransferFn.G` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single A` | implemented | `SKColorSpaceTransferFn.A` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single B` | implemented | `SKColorSpaceTransferFn.B` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single C` | implemented | `SKColorSpaceTransferFn.C` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single D` | implemented | `SKColorSpaceTransferFn.D` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single E` | implemented | `SKColorSpaceTransferFn.E` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public Single F` | implemented | `SKColorSpaceTransferFn.F` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly bool Equals (SKColorSpaceTransferFn obj)` | implemented | `SKColorSpaceTransferFn.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly override bool Equals (object obj)` | implemented | `SKColorSpaceTransferFn.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static bool operator == (SKColorSpaceTransferFn left, SKColorSpaceTransferFn right)` | not-applicable | `SKColorSpaceTransferFn.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKColorSpaceTransferFn left, SKColorSpaceTransferFn right)` | not-applicable | `SKColorSpaceTransferFn.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKColorSpaceTransferFn.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKColorSpaceXyz

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColorSpaceXyz Srgb` | implemented | `SKColorSpaceXyz.Srgb` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceXyz AdobeRgb` | implemented | `SKColorSpaceXyz.AdobeRgb` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceXyz DisplayP3` | implemented | `SKColorSpaceXyz.DisplayP3` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceXyz Rec2020` | implemented | `SKColorSpaceXyz.Rec2020` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceXyz Xyz` | implemented | `SKColorSpaceXyz.Xyz` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static readonly SKColorSpaceXyz Empty` | implemented | `SKColorSpaceXyz.Empty` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly static SKColorSpaceXyz Identity = new SKColorSpaceXyz( 1, 0, 0, 0, 1, 0, 0, 0, 1)` | implemented | `SKColorSpaceXyz(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpaceXyz (float value)` | implemented | `SKColorSpaceXyz(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpaceXyz (float[] values)` | implemented | `SKColorSpaceXyz(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public SKColorSpaceXyz ( float m00, float m01, float m02, float m10, float m11, float m12, float m20, float m21, float m22)` | implemented | `SKColorSpaceXyz(...)` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public float[] Values` | implemented | `SKColorSpaceXyz.Values` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly float this[int x, int y]` | implemented | `SKColorSpaceXyz.this` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly SKColorSpaceXyz Invert ()` | implemented | `SKColorSpaceXyz.Invert` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static SKColorSpaceXyz Concat (SKColorSpaceXyz a, SKColorSpaceXyz b)` | implemented | `SKColorSpaceXyz.Concat` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly bool Equals (SKColorSpaceXyz obj)` | implemented | `SKColorSpaceXyz.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public readonly override bool Equals (object obj)` | implemented | `SKColorSpaceXyz.Equals` — Concrete float32 scalar/array constructors, properties, transforms, inversion, matrix composition and mutable out adapters. Native .NET reference curves and XYZ conversion/inversion tests verify behavior; source-compatible skcms approximation preserves scalar math. |
| `public static bool operator == (SKColorSpaceXyz left, SKColorSpaceXyz right)` | not-applicable | `SKColorSpaceXyz.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKColorSpaceXyz left, SKColorSpaceXyz right)` | not-applicable | `SKColorSpaceXyz.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKColorSpaceXyz.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKColorSpaceIccProfile

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKColorSpaceIccProfile () : this (SkiaApi.sk_colorspace_icc_profile_new (), true)` | partial | `SKColorSpaceIccProfile(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long Size` | partial | `SKColorSpaceIccProfile.Size` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public IntPtr Buffer` | not-applicable | `SKColorSpaceIccProfile.Buffer` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ToColorSpaceXyz (out SKColorSpaceXyz toXyzD50)` | partial | `SKColorSpaceIccProfile.ToColorSpaceXyz` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public SKColorSpaceXyz ToColorSpaceXyz ()` | partial | `SKColorSpaceIccProfile.ToColorSpaceXyz` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public static SKColorSpaceIccProfile Create (byte[] data)` | partial | `SKColorSpaceIccProfile.Create` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public static SKColorSpaceIccProfile Create (ReadOnlySpan<byte> data)` | partial | `SKColorSpaceIccProfile.Create` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public static SKColorSpaceIccProfile Create (SKData data)` | partial | `SKColorSpaceIccProfile.Create` — Matrix/TRC ICC parsing, buffer ownership, native decode and XYZ extraction are tested. Arbitrary LUT/multiprocess ICC manipulation is not yet exhaustively tested. |
| `public static SKColorSpaceIccProfile Create (IntPtr data, long length)` | not-applicable | `SKColorSpaceIccProfile.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |

### SkiaSharp.SKColors

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColor Empty` | implemented | `SKColors.Empty` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor AliceBlue = new SKColor (0xFFF0F8FF)` | implemented | `SKColors.AliceBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor AntiqueWhite = new SKColor (0xFFFAEBD7)` | implemented | `SKColors.AntiqueWhite` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Aqua = new SKColor (0xFF00FFFF)` | implemented | `SKColors.Aqua` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Aquamarine = new SKColor (0xFF7FFFD4)` | implemented | `SKColors.Aquamarine` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Azure = new SKColor (0xFFF0FFFF)` | implemented | `SKColors.Azure` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Beige = new SKColor (0xFFF5F5DC)` | implemented | `SKColors.Beige` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Bisque = new SKColor (0xFFFFE4C4)` | implemented | `SKColors.Bisque` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Black = new SKColor (0xFF000000)` | implemented | `SKColors.Black` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor BlanchedAlmond = new SKColor (0xFFFFEBCD)` | implemented | `SKColors.BlanchedAlmond` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Blue = new SKColor (0xFF0000FF)` | implemented | `SKColors.Blue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor BlueViolet = new SKColor (0xFF8A2BE2)` | implemented | `SKColors.BlueViolet` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Brown = new SKColor (0xFFA52A2A)` | implemented | `SKColors.Brown` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor BurlyWood = new SKColor (0xFFDEB887)` | implemented | `SKColors.BurlyWood` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor CadetBlue = new SKColor (0xFF5F9EA0)` | implemented | `SKColors.CadetBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Chartreuse = new SKColor (0xFF7FFF00)` | implemented | `SKColors.Chartreuse` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Chocolate = new SKColor (0xFFD2691E)` | implemented | `SKColors.Chocolate` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Coral = new SKColor (0xFFFF7F50)` | implemented | `SKColors.Coral` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor CornflowerBlue = new SKColor (0xFF6495ED)` | implemented | `SKColors.CornflowerBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Cornsilk = new SKColor (0xFFFFF8DC)` | implemented | `SKColors.Cornsilk` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Crimson = new SKColor (0xFFDC143C)` | implemented | `SKColors.Crimson` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Cyan = new SKColor (0xFF00FFFF)` | implemented | `SKColors.Cyan` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkBlue = new SKColor (0xFF00008B)` | implemented | `SKColors.DarkBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkCyan = new SKColor (0xFF008B8B)` | implemented | `SKColors.DarkCyan` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkGoldenrod = new SKColor (0xFFB8860B)` | implemented | `SKColors.DarkGoldenrod` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkGray = new SKColor (0xFFA9A9A9)` | implemented | `SKColors.DarkGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkGreen = new SKColor (0xFF006400)` | implemented | `SKColors.DarkGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkKhaki = new SKColor (0xFFBDB76B)` | implemented | `SKColors.DarkKhaki` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkMagenta = new SKColor (0xFF8B008B)` | implemented | `SKColors.DarkMagenta` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkOliveGreen = new SKColor (0xFF556B2F)` | implemented | `SKColors.DarkOliveGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkOrange = new SKColor (0xFFFF8C00)` | implemented | `SKColors.DarkOrange` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkOrchid = new SKColor (0xFF9932CC)` | implemented | `SKColors.DarkOrchid` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkRed = new SKColor (0xFF8B0000)` | implemented | `SKColors.DarkRed` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkSalmon = new SKColor (0xFFE9967A)` | implemented | `SKColors.DarkSalmon` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkSeaGreen = new SKColor (0xFF8FBC8B)` | implemented | `SKColors.DarkSeaGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkSlateBlue = new SKColor (0xFF483D8B)` | implemented | `SKColors.DarkSlateBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkSlateGray = new SKColor (0xFF2F4F4F)` | implemented | `SKColors.DarkSlateGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkTurquoise = new SKColor (0xFF00CED1)` | implemented | `SKColors.DarkTurquoise` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DarkViolet = new SKColor (0xFF9400D3)` | implemented | `SKColors.DarkViolet` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DeepPink = new SKColor (0xFFFF1493)` | implemented | `SKColors.DeepPink` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DeepSkyBlue = new SKColor (0xFF00BFFF)` | implemented | `SKColors.DeepSkyBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DimGray = new SKColor (0xFF696969)` | implemented | `SKColors.DimGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor DodgerBlue = new SKColor (0xFF1E90FF)` | implemented | `SKColors.DodgerBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Firebrick = new SKColor (0xFFB22222)` | implemented | `SKColors.Firebrick` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor FloralWhite = new SKColor (0xFFFFFAF0)` | implemented | `SKColors.FloralWhite` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor ForestGreen = new SKColor (0xFF228B22)` | implemented | `SKColors.ForestGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Fuchsia = new SKColor (0xFFFF00FF)` | implemented | `SKColors.Fuchsia` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Gainsboro = new SKColor (0xFFDCDCDC)` | implemented | `SKColors.Gainsboro` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor GhostWhite = new SKColor (0xFFF8F8FF)` | implemented | `SKColors.GhostWhite` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Gold = new SKColor (0xFFFFD700)` | implemented | `SKColors.Gold` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Goldenrod = new SKColor (0xFFDAA520)` | implemented | `SKColors.Goldenrod` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Gray = new SKColor (0xFF808080)` | implemented | `SKColors.Gray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Green = new SKColor (0xFF008000)` | implemented | `SKColors.Green` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor GreenYellow = new SKColor (0xFFADFF2F)` | implemented | `SKColors.GreenYellow` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Honeydew = new SKColor (0xFFF0FFF0)` | implemented | `SKColors.Honeydew` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor HotPink = new SKColor (0xFFFF69B4)` | implemented | `SKColors.HotPink` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor IndianRed = new SKColor (0xFFCD5C5C)` | implemented | `SKColors.IndianRed` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Indigo = new SKColor (0xFF4B0082)` | implemented | `SKColors.Indigo` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Ivory = new SKColor (0xFFFFFFF0)` | implemented | `SKColors.Ivory` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Khaki = new SKColor (0xFFF0E68C)` | implemented | `SKColors.Khaki` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Lavender = new SKColor (0xFFE6E6FA)` | implemented | `SKColors.Lavender` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LavenderBlush = new SKColor (0xFFFFF0F5)` | implemented | `SKColors.LavenderBlush` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LawnGreen = new SKColor (0xFF7CFC00)` | implemented | `SKColors.LawnGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LemonChiffon = new SKColor (0xFFFFFACD)` | implemented | `SKColors.LemonChiffon` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightBlue = new SKColor (0xFFADD8E6)` | implemented | `SKColors.LightBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightCoral = new SKColor (0xFFF08080)` | implemented | `SKColors.LightCoral` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightCyan = new SKColor (0xFFE0FFFF)` | implemented | `SKColors.LightCyan` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightGoldenrodYellow = new SKColor (0xFFFAFAD2)` | implemented | `SKColors.LightGoldenrodYellow` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightGray = new SKColor (0xFFD3D3D3)` | implemented | `SKColors.LightGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightGreen = new SKColor (0xFF90EE90)` | implemented | `SKColors.LightGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightPink = new SKColor (0xFFFFB6C1)` | implemented | `SKColors.LightPink` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightSalmon = new SKColor (0xFFFFA07A)` | implemented | `SKColors.LightSalmon` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightSeaGreen = new SKColor (0xFF20B2AA)` | implemented | `SKColors.LightSeaGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightSkyBlue = new SKColor (0xFF87CEFA)` | implemented | `SKColors.LightSkyBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightSlateGray = new SKColor (0xFF778899)` | implemented | `SKColors.LightSlateGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightSteelBlue = new SKColor (0xFFB0C4DE)` | implemented | `SKColors.LightSteelBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LightYellow = new SKColor (0xFFFFFFE0)` | implemented | `SKColors.LightYellow` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Lime = new SKColor (0xFF00FF00)` | implemented | `SKColors.Lime` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor LimeGreen = new SKColor (0xFF32CD32)` | implemented | `SKColors.LimeGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Linen = new SKColor (0xFFFAF0E6)` | implemented | `SKColors.Linen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Magenta = new SKColor (0xFFFF00FF)` | implemented | `SKColors.Magenta` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Maroon = new SKColor (0xFF800000)` | implemented | `SKColors.Maroon` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumAquamarine = new SKColor (0xFF66CDAA)` | implemented | `SKColors.MediumAquamarine` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumBlue = new SKColor (0xFF0000CD)` | implemented | `SKColors.MediumBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumOrchid = new SKColor (0xFFBA55D3)` | implemented | `SKColors.MediumOrchid` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumPurple = new SKColor (0xFF9370DB)` | implemented | `SKColors.MediumPurple` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumSeaGreen = new SKColor (0xFF3CB371)` | implemented | `SKColors.MediumSeaGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumSlateBlue = new SKColor (0xFF7B68EE)` | implemented | `SKColors.MediumSlateBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumSpringGreen = new SKColor (0xFF00FA9A)` | implemented | `SKColors.MediumSpringGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumTurquoise = new SKColor (0xFF48D1CC)` | implemented | `SKColors.MediumTurquoise` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MediumVioletRed = new SKColor (0xFFC71585)` | implemented | `SKColors.MediumVioletRed` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MidnightBlue = new SKColor (0xFF191970)` | implemented | `SKColors.MidnightBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MintCream = new SKColor (0xFFF5FFFA)` | implemented | `SKColors.MintCream` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor MistyRose = new SKColor (0xFFFFE4E1)` | implemented | `SKColors.MistyRose` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Moccasin = new SKColor (0xFFFFE4B5)` | implemented | `SKColors.Moccasin` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor NavajoWhite = new SKColor (0xFFFFDEAD)` | implemented | `SKColors.NavajoWhite` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Navy = new SKColor (0xFF000080)` | implemented | `SKColors.Navy` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor OldLace = new SKColor (0xFFFDF5E6)` | implemented | `SKColors.OldLace` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Olive = new SKColor (0xFF808000)` | implemented | `SKColors.Olive` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor OliveDrab = new SKColor (0xFF6B8E23)` | implemented | `SKColors.OliveDrab` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Orange = new SKColor (0xFFFFA500)` | implemented | `SKColors.Orange` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor OrangeRed = new SKColor (0xFFFF4500)` | implemented | `SKColors.OrangeRed` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Orchid = new SKColor (0xFFDA70D6)` | implemented | `SKColors.Orchid` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PaleGoldenrod = new SKColor (0xFFEEE8AA)` | implemented | `SKColors.PaleGoldenrod` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PaleGreen = new SKColor (0xFF98FB98)` | implemented | `SKColors.PaleGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PaleTurquoise = new SKColor (0xFFAFEEEE)` | implemented | `SKColors.PaleTurquoise` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PaleVioletRed = new SKColor (0xFFDB7093)` | implemented | `SKColors.PaleVioletRed` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PapayaWhip = new SKColor (0xFFFFEFD5)` | implemented | `SKColors.PapayaWhip` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PeachPuff = new SKColor (0xFFFFDAB9)` | implemented | `SKColors.PeachPuff` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Peru = new SKColor (0xFFCD853F)` | implemented | `SKColors.Peru` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Pink = new SKColor (0xFFFFC0CB)` | implemented | `SKColors.Pink` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Plum = new SKColor (0xFFDDA0DD)` | implemented | `SKColors.Plum` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor PowderBlue = new SKColor (0xFFB0E0E6)` | implemented | `SKColors.PowderBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Purple = new SKColor (0xFF800080)` | implemented | `SKColors.Purple` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Red = new SKColor (0xFFFF0000)` | implemented | `SKColors.Red` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor RosyBrown = new SKColor (0xFFBC8F8F)` | implemented | `SKColors.RosyBrown` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor RoyalBlue = new SKColor (0xFF4169E1)` | implemented | `SKColors.RoyalBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SaddleBrown = new SKColor (0xFF8B4513)` | implemented | `SKColors.SaddleBrown` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Salmon = new SKColor (0xFFFA8072)` | implemented | `SKColors.Salmon` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SandyBrown = new SKColor (0xFFF4A460)` | implemented | `SKColors.SandyBrown` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SeaGreen = new SKColor (0xFF2E8B57)` | implemented | `SKColors.SeaGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SeaShell = new SKColor (0xFFFFF5EE)` | implemented | `SKColors.SeaShell` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Sienna = new SKColor (0xFFA0522D)` | implemented | `SKColors.Sienna` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Silver = new SKColor (0xFFC0C0C0)` | implemented | `SKColors.Silver` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SkyBlue = new SKColor (0xFF87CEEB)` | implemented | `SKColors.SkyBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SlateBlue = new SKColor (0xFF6A5ACD)` | implemented | `SKColors.SlateBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SlateGray = new SKColor (0xFF708090)` | implemented | `SKColors.SlateGray` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Snow = new SKColor (0xFFFFFAFA)` | implemented | `SKColors.Snow` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SpringGreen = new SKColor (0xFF00FF7F)` | implemented | `SKColors.SpringGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor SteelBlue = new SKColor (0xFF4682B4)` | implemented | `SKColors.SteelBlue` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Tan = new SKColor (0xFFD2B48C)` | implemented | `SKColors.Tan` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Teal = new SKColor (0xFF008080)` | implemented | `SKColors.Teal` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Thistle = new SKColor (0xFFD8BFD8)` | implemented | `SKColors.Thistle` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Tomato = new SKColor (0xFFFF6347)` | implemented | `SKColors.Tomato` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Turquoise = new SKColor (0xFF40E0D0)` | implemented | `SKColors.Turquoise` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Violet = new SKColor (0xFFEE82EE)` | implemented | `SKColors.Violet` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Wheat = new SKColor (0xFFF5DEB3)` | implemented | `SKColors.Wheat` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor White = new SKColor (0xFFFFFFFF)` | implemented | `SKColors.White` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor WhiteSmoke = new SKColor (0xFFF5F5F5)` | implemented | `SKColors.WhiteSmoke` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Yellow = new SKColor (0xFFFFFF00)` | implemented | `SKColors.Yellow` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor YellowGreen = new SKColor (0xFF9ACD32)` | implemented | `SKColors.YellowGreen` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |
| `public static SKColor Transparent = new SKColor (0x00FFFFFF)` | implemented | `SKColors.Transparent` — Named-color field identity is normalized from the initializer; every pinned ARGB literal is compared with the runtime and independently reflected .NET named colors. |

### SkiaSharp.SKData

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKData Empty` | partial | `SKData.Empty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData CreateCopy (IntPtr bytes, int length)` | not-applicable | `SKData.CreateCopy` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKData CreateCopy (IntPtr bytes, long length)` | not-applicable | `SKData.CreateCopy` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKData CreateCopy (IntPtr bytes, ulong length)` | not-applicable | `SKData.CreateCopy` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKData CreateCopy (byte[] bytes)` | partial | `SKData.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData CreateCopy (byte[] bytes, ulong length)` | partial | `SKData.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData CreateCopy (ReadOnlySpan<byte> bytes)` | partial | `SKData.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (int size)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (long size)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (ulong size)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (string filename)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (Stream stream)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (Stream stream, int length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (Stream stream, ulong length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (Stream stream, long length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (SKStream stream)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (SKStream stream, int length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (SKStream stream, ulong length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (SKStream stream, long length)` | partial | `SKData.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKData Create (IntPtr address, int length)` | not-applicable | `SKData.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKData Create (IntPtr address, int length, SKDataReleaseDelegate releaseProc)` | not-applicable | `SKData.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKData Create (IntPtr address, int length, SKDataReleaseDelegate releaseProc, object context)` | not-applicable | `SKData.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKData Subset (ulong offset, ulong length)` | implemented | `SKData.Subset` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public byte[] ToArray ()` | partial | `SKData.ToArray` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsEmpty` | partial | `SKData.IsEmpty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long Size` | partial | `SKData.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr Data` | not-applicable | `SKData.Data` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public Span<byte> Span` | partial | `SKData.Span` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Stream AsStream ()` | partial | `SKData.AsStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public Stream AsStream (bool streamDisposesData)` | partial | `SKData.AsStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ReadOnlySpan<byte> AsSpan ()` | partial | `SKData.AsSpan` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SaveTo (Stream target)` | partial | `SKData.SaveTo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKDocument

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public const float DefaultRasterDpi = 72.0f` | partial | `SKDocument.DefaultRasterDpi` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Abort ()` | partial | `SKDocument.Abort` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCanvas BeginPage (float width, float height)` | partial | `SKDocument.BeginPage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCanvas BeginPage (float width, float height, SKRect content)` | partial | `SKDocument.BeginPage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void EndPage ()` | partial | `SKDocument.EndPage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Close ()` | partial | `SKDocument.Close` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (string path)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (Stream stream)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (SKWStream stream)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (string path, float dpi)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (Stream stream, float dpi)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (SKWStream stream, float dpi)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (string path, SKDocumentXpsOptions options)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (Stream stream, SKDocumentXpsOptions options)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreateXps (SKWStream stream, SKDocumentXpsOptions options)` | partial | `SKDocument.CreateXps` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (string path)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (Stream stream)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (SKWStream stream)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (string path, float dpi)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (Stream stream, float dpi)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (SKWStream stream, float dpi)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (string path, SKDocumentPdfMetadata metadata)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (Stream stream, SKDocumentPdfMetadata metadata)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKDocument CreatePdf (SKWStream stream, SKDocumentPdfMetadata metadata)` | partial | `SKDocument.CreatePdf` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKDrawable

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public uint GenerationId` | partial | `SKDrawable.GenerationId` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRect Bounds` | partial | `SKDrawable.Bounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int ApproximateBytesUsed` | partial | `SKDrawable.ApproximateBytesUsed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Draw (SKCanvas canvas, in SKMatrix matrix)` | partial | `SKDrawable.Draw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Draw (SKCanvas canvas, float x, float y)` | partial | `SKDrawable.Draw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPicture Snapshot ()` | partial | `SKDrawable.Snapshot` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void NotifyDrawingChanged ()` | partial | `SKDrawable.NotifyDrawingChanged` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFont

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFont () : this (SKTypeface.Default, DefaultSize, DefaultScaleX, DefaultSkewX)` | partial | `SKFont(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFont (SKTypeface typeface, float size = DefaultSize, float scaleX = DefaultScaleX, float skewX = DefaultSkewX) : this (SkiaApi.sk_font_new_with_values (typeface?.Handle ?? IntPtr.Zero, size, scaleX, skewX), true)` | not-applicable | `SKFont(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ForceAutoHinting` | implemented | `SKFont.ForceAutoHinting` — Compiled native SkFont flags, full metrics and glyph outline bridge; independent native font differential tests exercise flags and glyph paths. |
| `public bool EmbeddedBitmaps` | implemented | `SKFont.EmbeddedBitmaps` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool Subpixel` | implemented | `SKFont.Subpixel` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool LinearMetrics` | implemented | `SKFont.LinearMetrics` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool Embolden` | implemented | `SKFont.Embolden` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool BaselineSnap` | implemented | `SKFont.BaselineSnap` — Compiled native SkFont flags, full metrics and glyph outline bridge; independent native font differential tests exercise flags and glyph paths. |
| `public SKFontEdging Edging` | implemented | `SKFont.Edging` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKFontHinting Hinting` | implemented | `SKFont.Hinting` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKTypeface Typeface` | partial | `SKFont.Typeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float Size` | implemented | `SKFont.Size` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float ScaleX` | implemented | `SKFont.ScaleX` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float SkewX` | implemented | `SKFont.SkewX` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float Spacing` | implemented | `SKFont.Spacing` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKFontMetrics Metrics` | implemented | `SKFont.Metrics` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float GetFontMetrics (out SKFontMetrics metrics)` | implemented | `SKFont.GetFontMetrics` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort GetGlyph (int codepoint)` | implemented | `SKFont.GetGlyph` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort[] GetGlyphs (ReadOnlySpan<int> codepoints)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphs (ReadOnlySpan<int> codepoints, Span<ushort> glyphs)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort[] GetGlyphs (string text)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort[] GetGlyphs (ReadOnlySpan<char> text)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort[] GetGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public ushort[] GetGlyphs (IntPtr text, int length, SKTextEncoding encoding)` | not-applicable | `SKFont.GetGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void GetGlyphs (string text, Span<ushort> glyphs)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphs (ReadOnlySpan<char> text, Span<ushort> glyphs)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding, Span<ushort> glyphs)` | implemented | `SKFont.GetGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphs (IntPtr text, int length, SKTextEncoding encoding, Span<ushort> glyphs)` | not-applicable | `SKFont.GetGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ContainsGlyph (int codepoint)` | implemented | `SKFont.ContainsGlyph` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool ContainsGlyphs (ReadOnlySpan<int> codepoints)` | implemented | `SKFont.ContainsGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool ContainsGlyphs (string text)` | implemented | `SKFont.ContainsGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool ContainsGlyphs (ReadOnlySpan<char> text)` | implemented | `SKFont.ContainsGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool ContainsGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding)` | implemented | `SKFont.ContainsGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public bool ContainsGlyphs (IntPtr text, int length, SKTextEncoding encoding)` | not-applicable | `SKFont.ContainsGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int CountGlyphs (string text)` | implemented | `SKFont.CountGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public int CountGlyphs (ReadOnlySpan<char> text)` | implemented | `SKFont.CountGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public int CountGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding)` | implemented | `SKFont.CountGlyphs` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public int CountGlyphs (IntPtr text, int length, SKTextEncoding encoding)` | not-applicable | `SKFont.CountGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (string text, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (ReadOnlySpan<char> text, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (IntPtr text, int length, SKTextEncoding encoding, SKPaint paint = null)` | not-applicable | `SKFont.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (string text, out SKRect bounds, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (ReadOnlySpan<char> text, out SKRect bounds, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (ReadOnlySpan<byte> text, SKTextEncoding encoding, out SKRect bounds, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (IntPtr text, int length, SKTextEncoding encoding, out SKRect bounds, SKPaint paint = null)` | not-applicable | `SKFont.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (ReadOnlySpan<ushort> glyphs, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float MeasureText (ReadOnlySpan<ushort> glyphs, out SKRect bounds, SKPaint paint = null)` | implemented | `SKFont.MeasureText` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public int BreakText (string text, float maxWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (string text, float maxWidth, out float measuredWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (ReadOnlySpan<char> text, float maxWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (ReadOnlySpan<char> text, float maxWidth, out float measuredWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (ReadOnlySpan<byte> text, SKTextEncoding encoding, float maxWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (ReadOnlySpan<byte> text, SKTextEncoding encoding, float maxWidth, out float measuredWidth, SKPaint paint = null)` | partial | `SKFont.BreakText` — String and explicit UTF-8/16/32 forms support mutable measured-width outputs. Calls without an output retain the v0.1 {Count,MeasuredWidth,Text} result; numeric coercion returns Count. This is an intentional JavaScript return-shape difference. |
| `public int BreakText (IntPtr text, int length, SKTextEncoding encoding, float maxWidth, SKPaint paint = null)` | not-applicable | `SKFont.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int BreakText (IntPtr text, int length, SKTextEncoding encoding, float maxWidth, out float measuredWidth, SKPaint paint = null)` | not-applicable | `SKFont.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPoint[] GetGlyphPositions (string text, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPoint[] GetGlyphPositions (ReadOnlySpan<char> text, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPoint[] GetGlyphPositions (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPoint[] GetGlyphPositions (IntPtr text, int length, SKTextEncoding encoding, SKPoint origin = default)` | not-applicable | `SKFont.GetGlyphPositions` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void GetGlyphPositions (string text, Span<SKPoint> offsets, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphPositions (ReadOnlySpan<char> text, Span<SKPoint> offsets, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphPositions (ReadOnlySpan<byte> text, SKTextEncoding encoding, Span<SKPoint> offsets, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphPositions (IntPtr text, int length, SKTextEncoding encoding, Span<SKPoint> offsets, SKPoint origin = default)` | not-applicable | `SKFont.GetGlyphPositions` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPoint[] GetGlyphPositions (ReadOnlySpan<ushort> glyphs, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphPositions (ReadOnlySpan<ushort> glyphs, Span<SKPoint> positions, SKPoint origin = default)` | implemented | `SKFont.GetGlyphPositions` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphOffsets (string text, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphOffsets (ReadOnlySpan<char> text, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphOffsets (ReadOnlySpan<byte> text, SKTextEncoding encoding, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphOffsets (IntPtr text, int length, SKTextEncoding encoding, float origin = 0f)` | not-applicable | `SKFont.GetGlyphOffsets` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void GetGlyphOffsets (string text, Span<float> offsets, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphOffsets (ReadOnlySpan<char> text, Span<float> offsets, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphOffsets (ReadOnlySpan<byte> text, SKTextEncoding encoding, Span<float> offsets, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphOffsets (IntPtr text, int length, SKTextEncoding encoding, Span<float> offsets, float origin = 0f)` | not-applicable | `SKFont.GetGlyphOffsets` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphOffsets (ReadOnlySpan<ushort> glyphs, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphOffsets (ReadOnlySpan<ushort> glyphs, Span<float> offsets, float origin = 0f)` | implemented | `SKFont.GetGlyphOffsets` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (string text, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (ReadOnlySpan<char> text, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (IntPtr text, int length, SKTextEncoding encoding, SKPaint paint = null)` | not-applicable | `SKFont.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (string text, out SKRect[] bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (ReadOnlySpan<char> text, out SKRect[] bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (ReadOnlySpan<byte> text, SKTextEncoding encoding, out SKRect[] bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (IntPtr text, int length, SKTextEncoding encoding, out SKRect[] bounds, SKPaint paint = null)` | not-applicable | `SKFont.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void GetGlyphWidths (string text, Span<float> widths, Span<SKRect> bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphWidths (ReadOnlySpan<char> text, Span<float> widths, Span<SKRect> bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphWidths (ReadOnlySpan<byte> text, SKTextEncoding encoding, Span<float> widths, Span<SKRect> bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphWidths (IntPtr text, int length, SKTextEncoding encoding, Span<float> widths, Span<SKRect> bounds, SKPaint paint = null)` | not-applicable | `SKFont.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (ReadOnlySpan<ushort> glyphs, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public float[] GetGlyphWidths (ReadOnlySpan<ushort> glyphs, out SKRect[] bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public void GetGlyphWidths (ReadOnlySpan<ushort> glyphs, Span<float> widths, Span<SKRect> bounds, SKPaint paint = null)` | implemented | `SKFont.GetGlyphWidths` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetGlyphPath (ushort glyph)` | implemented | `SKFont.GetGlyphPath` — Compiled native SkFont flags, full metrics and glyph outline bridge; independent native font differential tests exercise flags and glyph paths. |
| `public SKPath GetTextPath (string text, SKPoint origin = default)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (ReadOnlySpan<char> text, SKPoint origin = default)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKPoint origin = default)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (IntPtr text, int length, SKTextEncoding encoding, SKPoint origin = default)` | not-applicable | `SKFont.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (string text, ReadOnlySpan<SKPoint> positions)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (ReadOnlySpan<char> text, ReadOnlySpan<SKPoint> positions)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (ReadOnlySpan<byte> text, SKTextEncoding encoding, ReadOnlySpan<SKPoint> positions)` | implemented | `SKFont.GetTextPath` — Managed strings, encoded byte buffers, glyph spans, destination arrays, optional paint, origin and bounds overloads are explicitly dispatched; native font metrics/glyph widths and UTF-8/16 buffers are regression-tested. |
| `public SKPath GetTextPath (IntPtr text, int length, SKTextEncoding encoding, ReadOnlySpan<SKPoint> positions)` | not-applicable | `SKFont.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void GetGlyphPaths (ReadOnlySpan<ushort> glyphs, SKGlyphPathDelegate glyphPathDelegate)` | partial | `SKFont.GetGlyphPaths` — Callbacks receive glyph outline and SKMatrix as required; geometry is pretransformed and callback transform is Identity. Exact native normalization/transform decomposition is not claimed. |
| `public SKPath GetTextPathOnPath (string text, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | partial | `SKFont.GetTextPathOnPath` — Listed managed text/glyph forms render warped outlines, including supplied widths/positions. Adaptive curve flattening uses a 0.2 px target rather than native Skia warp internals. |
| `public SKPath GetTextPathOnPath (ReadOnlySpan<char> text, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | partial | `SKFont.GetTextPathOnPath` — Listed managed text/glyph forms render warped outlines, including supplied widths/positions. Adaptive curve flattening uses a 0.2 px target rather than native Skia warp internals. |
| `public SKPath GetTextPathOnPath (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | partial | `SKFont.GetTextPathOnPath` — Listed managed text/glyph forms render warped outlines, including supplied widths/positions. Adaptive curve flattening uses a 0.2 px target rather than native Skia warp internals. |
| `public SKPath GetTextPathOnPath (IntPtr text, int length, SKTextEncoding encoding, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | not-applicable | `SKFont.GetTextPathOnPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPathOnPath (ReadOnlySpan<ushort> glyphs, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | partial | `SKFont.GetTextPathOnPath` — Listed managed text/glyph forms render warped outlines, including supplied widths/positions. Adaptive curve flattening uses a 0.2 px target rather than native Skia warp internals. |
| `public SKPath GetTextPathOnPath (ReadOnlySpan<ushort> glyphs, ReadOnlySpan<float> glyphWidths, ReadOnlySpan<SKPoint> glyphPositions, SKPath path, SKTextAlign textAlign = SKTextAlign.Left)` | partial | `SKFont.GetTextPathOnPath` — Listed managed text/glyph forms render warped outlines, including supplied widths/positions. Adaptive curve flattening uses a 0.2 px target rather than native Skia warp internals. |

### SkiaSharp.SKFontArguments

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public ReadOnlySpan<SKFontVariationPositionCoordinate> VariationDesignPosition` | implemented | `SKFontArguments.VariationDesignPosition` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public int CollectionIndex` | implemented | `SKFontArguments.CollectionIndex` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public int PaletteIndex` | implemented | `SKFontArguments.PaletteIndex` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public ReadOnlySpan<SKFontPaletteOverride> PaletteOverrides` | implemented | `SKFontArguments.PaletteOverrides` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |

### SkiaSharp.SKFontManager

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKFontManager Default` | partial | `SKFontManager.Default` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int FontFamilyCount` | partial | `SKFontManager.FontFamilyCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IEnumerable<string> FontFamilies` | partial | `SKFontManager.FontFamilies` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public string GetFamilyName (int index)` | implemented | `SKFontManager.GetFamilyName` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public string[] GetFontFamilies ()` | partial | `SKFontManager.GetFontFamilies` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFontStyleSet GetFontStyles (int index)` | implemented | `SKFontManager.GetFontStyles` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKFontStyleSet GetFontStyles (string familyName)` | implemented | `SKFontManager.GetFontStyles` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchFamily (string familyName)` | partial | `SKFontManager.MatchFamily` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface MatchFamily (string familyName, SKFontStyle style)` | partial | `SKFontManager.MatchFamily` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (string path, int index = 0)` | partial | `SKFontManager.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (Stream stream, int index = 0)` | partial | `SKFontManager.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (SKStreamAsset stream, int index = 0)` | partial | `SKFontManager.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (SKData data, int index = 0)` | partial | `SKFontManager.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface MatchCharacter (char character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, char character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, string[] bcp47, char character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, string[] bcp47, int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, SKFontStyleWeight weight, SKFontStyleWidth width, SKFontStyleSlant slant, string[] bcp47, char character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, SKFontStyleWeight weight, SKFontStyleWidth width, SKFontStyleSlant slant, string[] bcp47, int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, int weight, int width, SKFontStyleSlant slant, string[] bcp47, int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public SKTypeface MatchCharacter (string familyName, SKFontStyle style, string[] bcp47, int character)` | implemented | `SKFontManager.MatchCharacter` — Indexed/style/language/character overloads use registered family data and native locale matching; font manager API tests pass. |
| `public static SKFontManager CreateDefault ()` | partial | `SKFontManager.CreateDefault` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFontStyle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFontStyle () : this (SKFontStyleWeight.Normal, SKFontStyleWidth.Normal, SKFontStyleSlant.Upright)` | implemented | `SKFontStyle(...)` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public SKFontStyle (SKFontStyleWeight weight, SKFontStyleWidth width, SKFontStyleSlant slant) : this ((int)weight, (int)width, slant)` | implemented | `SKFontStyle(...)` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public SKFontStyle (int weight, int width, SKFontStyleSlant slant) : this (SkiaApi.sk_fontstyle_new (weight, width, slant), true)` | implemented | `SKFontStyle(...)` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public int Weight` | implemented | `SKFontStyle.Weight` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public int Width` | implemented | `SKFontStyle.Width` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public SKFontStyleSlant Slant` | implemented | `SKFontStyle.Slant` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static SKFontStyle Normal` | implemented | `SKFontStyle.Normal` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static SKFontStyle Bold` | implemented | `SKFontStyle.Bold` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static SKFontStyle Italic` | implemented | `SKFontStyle.Italic` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static SKFontStyle BoldItalic` | implemented | `SKFontStyle.BoldItalic` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |

### SkiaSharp.SKFontStyleSet

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFontStyleSet () : this (SkiaApi.sk_fontstyleset_create_empty (), true)` | implemented | `SKFontStyleSet(...)` — Empty and populated native style-set indexing/enumeration are explicit and font tests cover zero-count behavior. |
| `public int Count` | implemented | `SKFontStyleSet.Count` — Empty and populated native style-set indexing/enumeration are explicit and font tests cover zero-count behavior. |
| `public SKFontStyle this[int index]` | partial | `SKFontStyleSet.this` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public string GetStyleName (int index)` | partial | `SKFontStyleSet.GetStyleName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (int index)` | partial | `SKFontStyleSet.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface CreateTypeface (SKFontStyle style)` | partial | `SKFontStyleSet.CreateTypeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IEnumerator<SKFontStyle> GetEnumerator ()` | implemented | `SKFontStyleSet.GetEnumerator` — Empty and populated native style-set indexing/enumeration are explicit and font tests cover zero-count behavior. |

### SkiaSharp.SKFourByteTag

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFourByteTag (uint value)` | implemented | `SKFourByteTag(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public SKFourByteTag (char c1, char c2, char c3, char c4)` | implemented | `SKFourByteTag(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKFourByteTag Parse (string? tag)` | implemented | `SKFourByteTag.Parse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKFourByteTag Parse (ReadOnlySpan<char> tag)` | implemented | `SKFourByteTag.Parse` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public override unsafe string ToString ()` | implemented | `SKFourByteTag.ToString` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static implicit operator uint (SKFourByteTag tag)` | not-applicable | `new SKFourByteTag(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKFourByteTag (uint tag)` | not-applicable | `new SKFourByteTag(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public override bool Equals (object? obj)` | implemented | `SKFourByteTag.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public bool Equals (SKFourByteTag other)` | implemented | `SKFourByteTag.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public override int GetHashCode ()` | implemented | `SKFourByteTag.GetHashCode` — Returns the signed 32-bit underlying packed value, matching the pinned implementation. |
| `public static bool operator == (SKFourByteTag left, SKFourByteTag right)` | not-applicable | `SKFourByteTag.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKFourByteTag left, SKFourByteTag right)` | not-applicable | `SKFourByteTag.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.SKFrontBufferedManagedStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFrontBufferedManagedStream (Stream managedStream, int bufferSize) : this (managedStream, bufferSize, false)` | implemented | `SKFrontBufferedManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKFrontBufferedManagedStream (Stream managedStream, int bufferSize, bool disposeUnderlyingStream) : this (new SKManagedStream (managedStream, disposeUnderlyingStream), bufferSize, true)` | implemented | `SKFrontBufferedManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKFrontBufferedManagedStream (SKStream nativeStream, int bufferSize) : this (nativeStream, bufferSize, false)` | implemented | `SKFrontBufferedManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKFrontBufferedManagedStream (SKStream nativeStream, int bufferSize, bool disposeUnderlyingStream)` | implemented | `SKFrontBufferedManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKFrontBufferedStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public const int DefaultBufferSize = 4096` | partial | `SKFrontBufferedStream.DefaultBufferSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFrontBufferedStream(Stream stream) : this(stream, DefaultBufferSize, false)` | partial | `SKFrontBufferedStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFrontBufferedStream(Stream stream, long bufferSize) : this(stream, bufferSize, false)` | partial | `SKFrontBufferedStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFrontBufferedStream(Stream stream, bool disposeUnderlyingStream) : this(stream, DefaultBufferSize, disposeUnderlyingStream)` | partial | `SKFrontBufferedStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFrontBufferedStream(Stream stream, long bufferSize, bool disposeUnderlyingStream)` | partial | `SKFrontBufferedStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override bool CanRead` | partial | `SKFrontBufferedStream.CanRead` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override bool CanSeek` | partial | `SKFrontBufferedStream.CanSeek` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override bool CanWrite` | partial | `SKFrontBufferedStream.CanWrite` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override long Length` | partial | `SKFrontBufferedStream.Length` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override long Position` | partial | `SKFrontBufferedStream.Position` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override void Flush()` | partial | `SKFrontBufferedStream.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override int Read(byte[] buffer, int offset, int count)` | partial | `SKFrontBufferedStream.Read` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override long Seek(long offset, SeekOrigin origin)` | partial | `SKFrontBufferedStream.Seek` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override void SetLength(long value)` | partial | `SKFrontBufferedStream.SetLength` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public override void Write(byte[] buffer, int offset, int count)` | partial | `SKFrontBufferedStream.Write` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphics

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void Init ()` | partial | `SKGraphics.Init` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static void PurgeFontCache ()` | partial | `SKGraphics.PurgeFontCache` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static void PurgeResourceCache ()` | partial | `SKGraphics.PurgeResourceCache` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static void PurgeAllCaches ()` | partial | `SKGraphics.PurgeAllCaches` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long GetFontCacheUsed ()` | partial | `SKGraphics.GetFontCacheUsed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long GetFontCacheLimit ()` | partial | `SKGraphics.GetFontCacheLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long SetFontCacheLimit (long bytes)` | partial | `SKGraphics.SetFontCacheLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static int GetFontCacheCountUsed ()` | partial | `SKGraphics.GetFontCacheCountUsed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static int GetFontCacheCountLimit ()` | partial | `SKGraphics.GetFontCacheCountLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static int SetFontCacheCountLimit (int count)` | partial | `SKGraphics.SetFontCacheCountLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long GetResourceCacheTotalBytesUsed ()` | partial | `SKGraphics.GetResourceCacheTotalBytesUsed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long GetResourceCacheTotalByteLimit ()` | partial | `SKGraphics.GetResourceCacheTotalByteLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long SetResourceCacheTotalByteLimit (long bytes)` | partial | `SKGraphics.SetResourceCacheTotalByteLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long GetResourceCacheSingleAllocationByteLimit ()` | partial | `SKGraphics.GetResourceCacheSingleAllocationByteLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static long SetResourceCacheSingleAllocationByteLimit (long bytes)` | partial | `SKGraphics.SetResourceCacheSingleAllocationByteLimit` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static void DumpMemoryStatistics (SKTraceMemoryDump dump)` | implemented | `SKGraphics.DumpMemoryStatistics` — Compiled native Skia memory-trace callbacks; numeric/string/backing ownership and ulong-to-BigInt behavior have unit tests. Chromium executes both graphics and Ganesh traces with nonempty native rows (browser lifecycle workflow). |

### SkiaSharp.SKImage

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKImage Create (SKImageInfo info)` | implemented | `SKImage.Create` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKImage FromPixelCopy (SKImageInfo info, SKStream pixels)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, SKStream pixels, int rowBytes)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, Stream pixels)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, Stream pixels, int rowBytes)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, byte[] pixels)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, byte[] pixels, int rowBytes)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, IntPtr pixels)` | not-applicable | `SKImage.FromPixelCopy` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKImage FromPixelCopy (SKImageInfo info, IntPtr pixels, int rowBytes)` | not-applicable | `SKImage.FromPixelCopy` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKImage FromPixelCopy (SKPixmap pixmap)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, ReadOnlySpan<byte> pixels)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixelCopy (SKImageInfo info, ReadOnlySpan<byte> pixels, int rowBytes)` | partial | `SKImage.FromPixelCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixels (SKImageInfo info, SKData data)` | partial | `SKImage.FromPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixels (SKImageInfo info, SKData data, int rowBytes)` | partial | `SKImage.FromPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixels (SKImageInfo info, IntPtr pixels)` | not-applicable | `SKImage.FromPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKImage FromPixels (SKImageInfo info, IntPtr pixels, int rowBytes)` | not-applicable | `SKImage.FromPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKImage FromPixels (SKPixmap pixmap)` | partial | `SKImage.FromPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixels (SKPixmap pixmap, SKImageRasterReleaseDelegate releaseProc)` | partial | `SKImage.FromPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromPixels (SKPixmap pixmap, SKImageRasterReleaseDelegate releaseProc, object releaseContext)` | partial | `SKImage.FromPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (SKData data, SKRectI subset)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (SKData data)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (ReadOnlySpan<byte> data)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (byte[] data)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKAlphaType alphaType)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKAlphaType alphaType, SKColorSpace colorSpace)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKAlphaType alphaType, SKColorSpace colorSpace, SKGraphiteReleaseDelegate releaseProc)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (SKStream data)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (Stream data)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromEncodedData (string filename)` | partial | `SKImage.FromEncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromBitmap (SKBitmap bitmap)` | partial | `SKImage.FromBitmap` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace, SKImageTextureReleaseDelegate releaseProc)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace, SKImageTextureReleaseDelegate releaseProc, object releaseContext)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace, SKImageTextureReleaseDelegate releaseProc)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace, SKImageTextureReleaseDelegate releaseProc, object releaseContext)` | partial | `SKImage.FromTexture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImage FromAdoptedTexture (GRContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRRecordingContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromAdoptedTexture (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKAlphaType alpha, SKColorSpace colorspace)` | partial | `SKImage.FromAdoptedTexture` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public static SKImage FromPicture (SKPicture picture, SKSizeI dimensions)` | implemented | `SKImage.FromPicture` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKImage FromPicture (SKPicture picture, SKSizeI dimensions, SKMatrix matrix)` | implemented | `SKImage.FromPicture` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKImage FromPicture (SKPicture picture, SKSizeI dimensions, SKPaint paint)` | implemented | `SKImage.FromPicture` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKImage FromPicture (SKPicture picture, SKSizeI dimensions, SKMatrix matrix, SKPaint paint)` | implemented | `SKImage.FromPicture` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKData Encode ()` | partial | `SKImage.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData Encode (SKEncodedImageFormat format, int quality)` | partial | `SKImage.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `SKImage.Width` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Height` | partial | `SKImage.Height` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public uint UniqueId` | partial | `SKImage.UniqueId` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKAlphaType AlphaType` | partial | `SKImage.AlphaType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorType ColorType` | partial | `SKImage.ColorType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorSpace ColorSpace` | partial | `SKImage.ColorSpace` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsAlphaOnly` | partial | `SKImage.IsAlphaOnly` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData EncodedData` | partial | `SKImage.EncodedData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo Info` | partial | `SKImage.Info` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader ()` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKMatrix localMatrix)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKSamplingOptions sampling)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKFilterQuality quality)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKSamplingOptions sampling, SKMatrix localMatrix)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKFilterQuality quality, SKMatrix localMatrix)` | partial | `SKImage.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToRawShader ()` | implemented | `SKImage.ToRawShader` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKShader ToRawShader (SKShaderTileMode tileX, SKShaderTileMode tileY)` | implemented | `SKImage.ToRawShader` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKShader ToRawShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKMatrix localMatrix)` | implemented | `SKImage.ToRawShader` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKShader ToRawShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKSamplingOptions sampling)` | implemented | `SKImage.ToRawShader` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKShader ToRawShader (SKShaderTileMode tileX, SKShaderTileMode tileY, SKSamplingOptions sampling, SKMatrix localMatrix)` | implemented | `SKImage.ToRawShader` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool PeekPixels (SKPixmap pixmap)` | partial | `SKImage.PeekPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPixmap PeekPixels ()` | partial | `SKImage.PeekPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsTextureBacked` | partial | `SKImage.IsTextureBacked` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsLazyGenerated` | partial | `SKImage.IsLazyGenerated` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsValid (GRContext context)` | implemented | `SKImage.IsValid` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool IsValid (GRRecordingContext context)` | implemented | `SKImage.IsValid` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels)` | not-applicable | `SKImage.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes)` | not-applicable | `SKImage.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes, int srcX, int srcY)` | not-applicable | `SKImage.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes, int srcX, int srcY, SKImageCachingHint cachingHint)` | not-applicable | `SKImage.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKPixmap pixmap)` | partial | `SKImage.ReadPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ReadPixels (SKPixmap pixmap, int srcX, int srcY)` | partial | `SKImage.ReadPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ReadPixels (SKPixmap pixmap, int srcX, int srcY, SKImageCachingHint cachingHint)` | partial | `SKImage.ReadPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void RequestReadPixels (SKImageInfo info, SKRectI srcRect, Action<SKImageReadPixelsResult> callback)` | implemented | `SKImage.RequestReadPixels` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void RequestReadPixels (SKImageInfo info, SKRectI srcRect, SKImageRescaleGamma rescaleGamma, SKImageRescaleMode rescaleMode, Action<SKImageReadPixelsResult> callback)` | implemented | `SKImage.RequestReadPixels` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ScalePixels (SKPixmap dst, SKFilterQuality quality)` | partial | `SKImage.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap dst, SKFilterQuality quality, SKImageCachingHint cachingHint)` | partial | `SKImage.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap dst, SKSamplingOptions sampling)` | partial | `SKImage.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap dst, SKSamplingOptions sampling, SKImageCachingHint cachingHint)` | partial | `SKImage.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage Subset (SKRectI subset)` | partial | `SKImage.Subset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage Subset (GRRecordingContext context, SKRectI subset)` | partial | `SKImage.Subset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage ToRasterImage ()` | partial | `SKImage.ToRasterImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage ToRasterImage (bool ensurePixelData)` | partial | `SKImage.ToRasterImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage ToTextureImage (GRContext context)` | partial | `SKImage.ToTextureImage` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public SKImage ToTextureImage (GRContext context, bool mipmapped)` | partial | `SKImage.ToTextureImage` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public SKImage ToTextureImage (GRContext context, bool mipmapped, bool budgeted)` | partial | `SKImage.ToTextureImage` — Native GPU implementation is compiled and resource-lifetime wrappers tested; these Ganesh/adoption signatures still lack live WebGL/GPU execution evidence. |
| `public SKImage ToTextureImage (SKGraphiteRecorder recorder)` | implemented | `SKImage.ToTextureImage` — Native SkImages::TextureFromImage using Dawn Graphite recorder: SwiftShader WebGPU exact-pixel test covers source/image disposal before submission and zero native handles after cleanup (gpu-image-validation.json). Physical vendor GPUs remain untested. |
| `public SKImage ToTextureImage (SKGraphiteRecorder recorder, bool mipmapped)` | implemented | `SKImage.ToTextureImage` — Native SkImages::TextureFromImage using Dawn Graphite recorder: SwiftShader WebGPU exact-pixel test covers source/image disposal before submission and zero native handles after cleanup (gpu-image-validation.json). Physical vendor GPUs remain untested. |
| `public SKImage ApplyImageFilter (SKImageFilter filter, SKRectI subset, SKRectI clipBounds, out SKRectI outSubset, out SKPoint outOffset)` | implemented | `SKImage.ApplyImageFilter` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKImage ApplyImageFilter (SKImageFilter filter, SKRectI subset, SKRectI clipBounds, out SKRectI outSubset, out SKPointI outOffset)` | implemented | `SKImage.ApplyImageFilter` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKImage ApplyImageFilter (GRContext context, SKImageFilter filter, SKRectI subset, SKRectI clipBounds, out SKRectI outSubset, out SKPointI outOffset)` | implemented | `SKImage.ApplyImageFilter` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKImage ApplyImageFilter (GRRecordingContext context, SKImageFilter filter, SKRectI subset, SKRectI clipBounds, out SKRectI outSubset, out SKPointI outOffset)` | implemented | `SKImage.ApplyImageFilter` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKImageFilter

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKImageFilter CreateMatrix (SKMatrix matrix)` | partial | `SKImageFilter.CreateMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMatrix (SKMatrix matrix, SKFilterQuality quality, SKImageFilter? input)` | partial | `SKImageFilter.CreateMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMatrix (in SKMatrix matrix)` | partial | `SKImageFilter.CreateMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMatrix (in SKMatrix matrix, SKSamplingOptions sampling)` | partial | `SKImageFilter.CreateMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMatrix (in SKMatrix matrix, SKSamplingOptions sampling, SKImageFilter? input)` | partial | `SKImageFilter.CreateMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY, SKImageFilter? input)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY, SKShaderTileMode tileMode)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY, SKShaderTileMode tileMode, SKImageFilter? input)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlur (float sigmaX, float sigmaY, SKShaderTileMode tileMode, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateBlur` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateColorFilter (SKColorFilter cf)` | partial | `SKImageFilter.CreateColorFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateColorFilter (SKColorFilter cf, SKImageFilter? input)` | partial | `SKImageFilter.CreateColorFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateColorFilter (SKColorFilter cf, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateColorFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateCompose (SKImageFilter outer, SKImageFilter inner)` | partial | `SKImageFilter.CreateCompose` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateCrop (SKRect rect)` | implemented | `SKImageFilter.CreateCrop` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateCrop (SKRect rect, SKShaderTileMode tileMode)` | implemented | `SKImageFilter.CreateCrop` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateCrop (SKRect rect, SKShaderTileMode tileMode, SKImageFilter? input)` | implemented | `SKImageFilter.CreateCrop` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDisplacementMapEffect (SKColorChannel xChannelSelector, SKColorChannel yChannelSelector, float scale, SKImageFilter displacement)` | partial | `SKImageFilter.CreateDisplacementMapEffect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDisplacementMapEffect (SKColorChannel xChannelSelector, SKColorChannel yChannelSelector, float scale, SKImageFilter displacement, SKImageFilter? input)` | partial | `SKImageFilter.CreateDisplacementMapEffect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDisplacementMapEffect (SKColorChannel xChannelSelector, SKColorChannel yChannelSelector, float scale, SKImageFilter displacement, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateDisplacementMapEffect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadow (float dx, float dy, float sigmaX, float sigmaY, SKColor color)` | partial | `SKImageFilter.CreateDropShadow` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadow (float dx, float dy, float sigmaX, float sigmaY, SKColor color, SKImageFilter? input)` | partial | `SKImageFilter.CreateDropShadow` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadow (float dx, float dy, float sigmaX, float sigmaY, SKColor color, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateDropShadow` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadowOnly (float dx, float dy, float sigmaX, float sigmaY, SKColor color)` | partial | `SKImageFilter.CreateDropShadowOnly` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadowOnly (float dx, float dy, float sigmaX, float sigmaY, SKColor color, SKImageFilter? input)` | partial | `SKImageFilter.CreateDropShadowOnly` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDropShadowOnly (float dx, float dy, float sigmaX, float sigmaY, SKColor color, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateDropShadowOnly` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateEmpty ()` | partial | `SKImageFilter.CreateEmpty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDistantLitDiffuse (SKPoint3 direction, SKColor lightColor, float surfaceScale, float kd)` | implemented | `SKImageFilter.CreateDistantLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDistantLitDiffuse (SKPoint3 direction, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input)` | implemented | `SKImageFilter.CreateDistantLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDistantLitDiffuse (SKPoint3 direction, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateDistantLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitDiffuse (SKPoint3 location, SKColor lightColor, float surfaceScale, float kd)` | implemented | `SKImageFilter.CreatePointLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitDiffuse (SKPoint3 location, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input)` | implemented | `SKImageFilter.CreatePointLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitDiffuse (SKPoint3 location, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreatePointLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitDiffuse (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float kd)` | implemented | `SKImageFilter.CreateSpotLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitDiffuse (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input)` | implemented | `SKImageFilter.CreateSpotLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitDiffuse (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float kd, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateSpotLitDiffuse` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDistantLitSpecular (SKPoint3 direction, SKColor lightColor, float surfaceScale, float ks, float shininess)` | implemented | `SKImageFilter.CreateDistantLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDistantLitSpecular (SKPoint3 direction, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input)` | implemented | `SKImageFilter.CreateDistantLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateDistantLitSpecular (SKPoint3 direction, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateDistantLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitSpecular (SKPoint3 location, SKColor lightColor, float surfaceScale, float ks, float shininess)` | implemented | `SKImageFilter.CreatePointLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitSpecular (SKPoint3 location, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input)` | implemented | `SKImageFilter.CreatePointLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePointLitSpecular (SKPoint3 location, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreatePointLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitSpecular (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float ks, float shininess)` | implemented | `SKImageFilter.CreateSpotLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitSpecular (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input)` | implemented | `SKImageFilter.CreateSpotLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateSpotLitSpecular (SKPoint3 location, SKPoint3 target, float specularExponent, float cutoffAngle, SKColor lightColor, float surfaceScale, float ks, float shininess, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateSpotLitSpecular` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMatrixConvolution (SKSizeI kernelSize, ReadOnlySpan<float> kernel, float gain, float bias, SKPointI kernelOffset, SKShaderTileMode tileMode, bool convolveAlpha)` | implemented | `SKImageFilter.CreateMatrixConvolution` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMatrixConvolution (SKSizeI kernelSize, ReadOnlySpan<float> kernel, float gain, float bias, SKPointI kernelOffset, SKShaderTileMode tileMode, bool convolveAlpha, SKImageFilter? input)` | implemented | `SKImageFilter.CreateMatrixConvolution` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMatrixConvolution (SKSizeI kernelSize, ReadOnlySpan<float> kernel, float gain, float bias, SKPointI kernelOffset, SKShaderTileMode tileMode, bool convolveAlpha, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateMatrixConvolution` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMerge (SKImageFilter? first, SKImageFilter? second)` | partial | `SKImageFilter.CreateMerge` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMerge (SKImageFilter? first, SKImageFilter? second, SKRect cropRect)` | partial | `SKImageFilter.CreateMerge` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMerge (ReadOnlySpan<SKImageFilter> filters)` | partial | `SKImageFilter.CreateMerge` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMerge (ReadOnlySpan<SKImageFilter> filters, SKRect cropRect)` | partial | `SKImageFilter.CreateMerge` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMerge (ReadOnlySpan<SKImageFilter> filters, SKRect* cropRect)` | partial | `SKImageFilter.CreateMerge` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDilate (float radiusX, float radiusY)` | partial | `SKImageFilter.CreateDilate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDilate (float radiusX, float radiusY, SKImageFilter? input)` | partial | `SKImageFilter.CreateDilate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateDilate (float radiusX, float radiusY, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateDilate` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateErode (float radiusX, float radiusY)` | partial | `SKImageFilter.CreateErode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateErode (float radiusX, float radiusY, SKImageFilter? input)` | partial | `SKImageFilter.CreateErode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateErode (float radiusX, float radiusY, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateErode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateOffset (float radiusX, float radiusY)` | partial | `SKImageFilter.CreateOffset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateOffset (float radiusX, float radiusY, SKImageFilter? input)` | partial | `SKImageFilter.CreateOffset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateOffset (float radiusX, float radiusY, SKImageFilter? input, SKRect cropRect)` | partial | `SKImageFilter.CreateOffset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreatePicture (SKPicture picture)` | partial | `SKImageFilter.CreatePicture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreatePicture (SKPicture picture, SKRect cropRect)` | partial | `SKImageFilter.CreatePicture` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateTile (SKRect src, SKRect dst)` | partial | `SKImageFilter.CreateTile` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateTile (SKRect src, SKRect dst, SKImageFilter? input)` | partial | `SKImageFilter.CreateTile` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlendMode mode, SKImageFilter? background)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlendMode mode, SKImageFilter? background, SKImageFilter? foreground)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlendMode mode, SKImageFilter? background, SKImageFilter? foreground, SKRect cropRect)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlender blender, SKImageFilter? background)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlender blender, SKImageFilter? background, SKImageFilter? foreground)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateBlendMode (SKBlender blender, SKImageFilter? background, SKImageFilter? foreground, SKRect cropRect)` | partial | `SKImageFilter.CreateBlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateArithmetic (float k1, float k2, float k3, float k4, bool enforcePMColor, SKImageFilter? background)` | implemented | `SKImageFilter.CreateArithmetic` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateArithmetic (float k1, float k2, float k3, float k4, bool enforcePMColor, SKImageFilter? background, SKImageFilter? foreground)` | implemented | `SKImageFilter.CreateArithmetic` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateArithmetic (float k1, float k2, float k3, float k4, bool enforcePMColor, SKImageFilter? background, SKImageFilter? foreground, SKRect cropRect)` | implemented | `SKImageFilter.CreateArithmetic` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateImage (SKImage image)` | partial | `SKImageFilter.CreateImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateImage (SKImage image, SKSamplingOptions sampling)` | partial | `SKImageFilter.CreateImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateImage (SKImage image, SKRect src, SKRect dst, SKSamplingOptions sampling)` | partial | `SKImageFilter.CreateImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateImage (SKImage image, SKRect src, SKRect dst, SKFilterQuality filterQuality)` | partial | `SKImageFilter.CreateImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateMagnifier (SKRect lensBounds, float zoomAmount, float inset, SKSamplingOptions sampling)` | implemented | `SKImageFilter.CreateMagnifier` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMagnifier (SKRect lensBounds, float zoomAmount, float inset, SKSamplingOptions sampling, SKImageFilter? input)` | implemented | `SKImageFilter.CreateMagnifier` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreateMagnifier (SKRect lensBounds, float zoomAmount, float inset, SKSamplingOptions sampling, SKImageFilter? input, SKRect cropRect)` | implemented | `SKImageFilter.CreateMagnifier` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKImageFilter CreatePaint (SKPaint paint)` | partial | `SKImageFilter.CreatePaint` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreatePaint (SKPaint paint, SKRect cropRect)` | partial | `SKImageFilter.CreatePaint` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateShader (SKShader? shader)` | partial | `SKImageFilter.CreateShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateShader (SKShader? shader, bool dither)` | partial | `SKImageFilter.CreateShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKImageFilter CreateShader (SKShader? shader, bool dither, SKRect cropRect)` | partial | `SKImageFilter.CreateShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKImageInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKImageInfo Empty` | partial | `SKImageInfo.Empty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static readonly SKColorType PlatformColorType` | partial | `SKImageInfo.PlatformColorType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static readonly int PlatformColorAlphaShift` | partial | `SKImageInfo.PlatformColorAlphaShift` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static readonly int PlatformColorRedShift` | partial | `SKImageInfo.PlatformColorRedShift` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static readonly int PlatformColorGreenShift` | partial | `SKImageInfo.PlatformColorGreenShift` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static readonly int PlatformColorBlueShift` | partial | `SKImageInfo.PlatformColorBlueShift` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `SKImageInfo.Width` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int Height` | partial | `SKImageInfo.Height` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKColorType ColorType` | partial | `SKImageInfo.ColorType` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKAlphaType AlphaType` | partial | `SKImageInfo.AlphaType` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKColorSpace ColorSpace` | partial | `SKImageInfo.ColorSpace` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKImageInfo (int width, int height)` | partial | `SKImageInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo (int width, int height, SKColorType colorType)` | partial | `SKImageInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo (int width, int height, SKColorType colorType, SKAlphaType alphaType)` | partial | `SKImageInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageInfo (int width, int height, SKColorType colorType, SKAlphaType alphaType, SKColorSpace colorspace)` | partial | `SKImageInfo(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly int BytesPerPixel` | partial | `SKImageInfo.BytesPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly int BitShiftPerPixel` | partial | `SKImageInfo.BitShiftPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly int BitsPerPixel` | partial | `SKImageInfo.BitsPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly int BytesSize` | partial | `SKImageInfo.BytesSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly long BytesSize64` | partial | `SKImageInfo.BytesSize64` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly int RowBytes` | partial | `SKImageInfo.RowBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly long RowBytes64` | partial | `SKImageInfo.RowBytes64` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly bool IsEmpty` | partial | `SKImageInfo.IsEmpty` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly bool IsOpaque` | partial | `SKImageInfo.IsOpaque` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKSizeI Size` | partial | `SKImageInfo.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKRectI Rect` | partial | `SKImageInfo.Rect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKImageInfo WithSize (SKSizeI size)` | partial | `SKImageInfo.WithSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKImageInfo WithSize (int width, int height)` | partial | `SKImageInfo.WithSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKImageInfo WithColorType (SKColorType newColorType)` | partial | `SKImageInfo.WithColorType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKImageInfo WithColorSpace (SKColorSpace newColorSpace)` | partial | `SKImageInfo.WithColorSpace` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly SKImageInfo WithAlphaType (SKAlphaType newAlphaType)` | partial | `SKImageInfo.WithAlphaType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly bool Equals (SKImageInfo obj)` | partial | `SKImageInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKImageInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKImageInfo left, SKImageInfo right)` | not-applicable | `SKImageInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKImageInfo left, SKImageInfo right)` | not-applicable | `SKImageInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKImageInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKImageReadPixelsResult

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public int PlaneCount` | partial | `SKImageReadPixelsResult.PlaneCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GetPlaneRowBytes (int planeIndex)` | partial | `SKImageReadPixelsResult.GetPlaneRowBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ReadOnlySpan<byte> GetPlaneData (int planeIndex)` | partial | `SKImageReadPixelsResult.GetPlaneData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void CopyPlaneTo (int planeIndex, Span<byte> destination)` | partial | `SKImageReadPixelsResult.CopyPlaneTo` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public byte[] ToArray (int planeIndex = 0)` | partial | `SKImageReadPixelsResult.ToArray` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage ToImage ()` | partial | `SKImageReadPixelsResult.ToImage` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBitmap ToBitmap ()` | partial | `SKImageReadPixelsResult.ToBitmap` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Dispose ()` | partial | `SKImageReadPixelsResult.Dispose` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKManagedStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKManagedStream (Stream managedStream) : this (managedStream, false)` | implemented | `SKManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKManagedStream (Stream managedStream, bool disposeManagedStream) : base (true)` | implemented | `SKManagedStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public int CopyTo (SKWStream destination)` | implemented | `SKManagedStream.CopyTo` — Pinned stream-copy semantics preserve current position, Int32 read count, destination flush and independently owned remainder; chunking, short reads, failed writes and exceptions are tested. |
| `public SKStreamAsset ToMemoryStream ()` | implemented | `SKManagedStream.ToMemoryStream` — Pinned stream-copy semantics preserve current position, Int32 read count, destination flush and independently owned remainder; chunking, short reads, failed writes and exceptions are tested. |

### SkiaSharp.SKManagedWStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKManagedWStream (Stream managedStream) : this (managedStream, false)` | implemented | `SKManagedWStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKManagedWStream (Stream managedStream, bool disposeManagedStream) : this (managedStream, disposeManagedStream, true)` | implemented | `SKManagedWStream(...)` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKMaskFilter

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public const int TableMaxLength = 256` | implemented | `SKMaskFilter.TableMaxLength` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static float ConvertRadiusToSigma (float radius)` | implemented | `SKMaskFilter.ConvertRadiusToSigma` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static float ConvertSigmaToRadius (float sigma)` | implemented | `SKMaskFilter.ConvertSigmaToRadius` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateBlur (SKBlurStyle blurStyle, float sigma)` | implemented | `SKMaskFilter.CreateBlur` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateBlur (SKBlurStyle blurStyle, float sigma, bool respectCTM)` | implemented | `SKMaskFilter.CreateBlur` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateTable (byte[] table)` | implemented | `SKMaskFilter.CreateTable` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateGamma (float gamma)` | implemented | `SKMaskFilter.CreateGamma` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateClip (byte min, byte max)` | implemented | `SKMaskFilter.CreateClip` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |
| `public static SKMaskFilter CreateShader (SKShader shader)` | implemented | `SKMaskFilter.CreateShader` — Native SkMaskFilter table/gamma/clip/shader and blur factories, table constant and radius/sigma functions are exercised by native pixel tests. |

### SkiaSharp.SKMatrix

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public readonly static SKMatrix Empty` | implemented | `SKMatrix.Empty` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly static SKMatrix Identity = new SKMatrix` | implemented | `SKMatrix.Identity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix (float[] values)` | implemented | `SKMatrix(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix ( float scaleX, float skewX, float transX, float skewY, float scaleY, float transY, float persp0, float persp1, float persp2)` | implemented | `SKMatrix(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool IsIdentity` | implemented | `SKMatrix.IsIdentity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public float[] Values` | implemented | `SKMatrix.Values` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void GetValues (float[] values)` | implemented | `SKMatrix.GetValues` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateIdentity ()` | implemented | `SKMatrix.CreateIdentity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateTranslation (float x, float y)` | implemented | `SKMatrix.CreateTranslation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateScale (float x, float y)` | implemented | `SKMatrix.CreateScale` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateScale (float x, float y, float pivotX, float pivotY)` | implemented | `SKMatrix.CreateScale` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateRotation (float radians)` | implemented | `SKMatrix.CreateRotation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateRotation (float radians, float pivotX, float pivotY)` | implemented | `SKMatrix.CreateRotation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateRotationDegrees (float degrees)` | implemented | `SKMatrix.CreateRotationDegrees` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateRotationDegrees (float degrees, float pivotX, float pivotY)` | implemented | `SKMatrix.CreateRotationDegrees` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateSkew (float x, float y)` | implemented | `SKMatrix.CreateSkew` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix CreateScaleTranslation (float sx, float sy, float tx, float ty)` | implemented | `SKMatrix.CreateScaleTranslation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool IsInvertible` | implemented | `SKMatrix.IsInvertible` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool TryInvert (out SKMatrix inverse)` | implemented | `SKMatrix.TryInvert` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix Invert ()` | implemented | `SKMatrix.Invert` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix Concat (SKMatrix first, SKMatrix second)` | implemented | `SKMatrix.Concat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix PreConcat (SKMatrix matrix)` | implemented | `SKMatrix.PreConcat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix PostConcat (SKMatrix matrix)` | implemented | `SKMatrix.PostConcat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static void Concat (ref SKMatrix target, SKMatrix first, SKMatrix second)` | implemented | `SKMatrix.Concat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKRect MapRect (SKRect source)` | implemented | `SKMatrix.MapRect` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapPoint (SKPoint point)` | implemented | `SKMatrix.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapPoint (float x, float y)` | implemented | `SKMatrix.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void MapPoints (Span<SKPoint> result, ReadOnlySpan<SKPoint> points)` | implemented | `SKMatrix.MapPoints` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void MapPoints (SKPoint[] result, SKPoint[] points)` | implemented | `SKMatrix.MapPoints` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint[] MapPoints (SKPoint[] points)` | implemented | `SKMatrix.MapPoints` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapVector (SKPoint vector)` | implemented | `SKMatrix.MapVector` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapVector (float x, float y)` | implemented | `SKMatrix.MapVector` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void MapVectors (Span<SKPoint> result, ReadOnlySpan<SKPoint> vectors)` | implemented | `SKMatrix.MapVectors` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void MapVectors (SKPoint[] result, SKPoint[] vectors)` | implemented | `SKMatrix.MapVectors` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint[] MapVectors (SKPoint[] vectors)` | implemented | `SKMatrix.MapVectors` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly float MapRadius (float radius)` | implemented | `SKMatrix.MapRadius` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single ScaleX` | implemented | `SKMatrix.ScaleX` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single SkewX` | implemented | `SKMatrix.SkewX` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single TransX` | implemented | `SKMatrix.TransX` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single SkewY` | implemented | `SKMatrix.SkewY` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single ScaleY` | implemented | `SKMatrix.ScaleY` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single TransY` | implemented | `SKMatrix.TransY` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single Persp0` | implemented | `SKMatrix.Persp0` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single Persp1` | implemented | `SKMatrix.Persp1` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single Persp2` | implemented | `SKMatrix.Persp2` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool Equals (SKMatrix obj)` | implemented | `SKMatrix.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly override bool Equals (object obj)` | implemented | `SKMatrix.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static bool operator == (SKMatrix left, SKMatrix right)` | not-applicable | `SKMatrix.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKMatrix left, SKMatrix right)` | not-applicable | `SKMatrix.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKMatrix.GetHashCode` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |

### SkiaSharp.SKMatrix44

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public readonly static SKMatrix44 Empty` | implemented | `SKMatrix44.Empty` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly static SKMatrix44 Identity = Matrix4x4.Identity` | implemented | `SKMatrix44.Identity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix44 ()` | implemented | `SKMatrix44(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix44 (SKMatrix src)` | implemented | `SKMatrix44(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix44 (SKMatrix44 src)` | implemented | `SKMatrix44(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKMatrix44 ( float m00, float m01, float m02, float m03, float m10, float m11, float m12, float m13, float m20, float m21, float m22, float m23, float m30, float m31, float m32, float m33)` | implemented | `SKMatrix44(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateIdentity ()` | implemented | `SKMatrix44.CreateIdentity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateTranslation (float x, float y, float z)` | implemented | `SKMatrix44.CreateTranslation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateScale (float x, float y, float z)` | implemented | `SKMatrix44.CreateScale` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateScale (float x, float y, float z, float pivotX, float pivotY, float pivotZ)` | implemented | `SKMatrix44.CreateScale` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateRotation (float x, float y, float z, float radians)` | implemented | `SKMatrix44.CreateRotation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 CreateRotationDegrees (float x, float y, float z, float degrees)` | implemented | `SKMatrix44.CreateRotationDegrees` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 FromRowMajor (ReadOnlySpan<float> src)` | implemented | `SKMatrix44.FromRowMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 FromColumnMajor (ReadOnlySpan<float> src)` | implemented | `SKMatrix44.FromColumnMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly float[] ToRowMajor ()` | implemented | `SKMatrix44.ToRowMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly float[] ToColumnMajor ()` | implemented | `SKMatrix44.ToColumnMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void ToRowMajor (Span<float> dst)` | implemented | `SKMatrix44.ToRowMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly void ToColumnMajor (Span<float> dst)` | implemented | `SKMatrix44.ToColumnMajor` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool IsInvertible` | implemented | `SKMatrix44.IsInvertible` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool TryInvert (out SKMatrix44 inverse)` | implemented | `SKMatrix44.TryInvert` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix44 Invert ()` | implemented | `SKMatrix44.Invert` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix44 Transpose ()` | implemented | `SKMatrix44.Transpose` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly float Determinant ()` | implemented | `SKMatrix44.Determinant` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapPoint (SKPoint point)` | implemented | `SKMatrix44.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint3 MapPoint (SKPoint3 point)` | implemented | `SKMatrix44.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint MapPoint (float x, float y)` | implemented | `SKMatrix44.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKPoint3 MapPoint (float x, float y, float z)` | implemented | `SKMatrix44.MapPoint` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Concat (SKMatrix44 first, SKMatrix44 second)` | implemented | `SKMatrix44.Concat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix44 PreConcat (SKMatrix44 matrix)` | implemented | `SKMatrix44.PreConcat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix44 PostConcat (SKMatrix44 matrix)` | implemented | `SKMatrix44.PostConcat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static void Concat (ref SKMatrix44 target, SKMatrix44 first, SKMatrix44 second)` | implemented | `SKMatrix44.Concat` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Negate (SKMatrix44 value)` | implemented | `SKMatrix44.Negate` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Add (SKMatrix44 value1, SKMatrix44 value2)` | implemented | `SKMatrix44.Add` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Subtract (SKMatrix44 value1, SKMatrix44 value2)` | implemented | `SKMatrix44.Subtract` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Multiply (SKMatrix44 value1, SKMatrix44 value2)` | implemented | `SKMatrix44.Multiply` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 Multiply (SKMatrix44 value1, float value2)` | implemented | `SKMatrix44.Multiply` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKMatrix44 operator - (SKMatrix44 value)` | not-applicable | `SKMatrix44.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKMatrix44 operator + (SKMatrix44 value1, SKMatrix44 value2)` | not-applicable | `SKMatrix44.Add(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKMatrix44 operator - (SKMatrix44 value1, SKMatrix44 value2)` | not-applicable | `SKMatrix44.Subtract(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKMatrix44 operator * (SKMatrix44 value1, SKMatrix44 value2)` | not-applicable | `SKMatrix44.Multiply(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static SKMatrix44 operator * (SKMatrix44 value1, float value2)` | not-applicable | `SKMatrix44.Multiply(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public SKMatrix Matrix` | implemented | `SKMatrix44.Matrix` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public float this[int row, int column]` | implemented | `SKMatrix44.Get(row,column) / Set(row,column,value)` — JavaScript indexers use explicit Get(row,column) and Set(row,column,value). |
| `public static implicit operator SKMatrix44 (SKMatrix matrix)` | not-applicable | `new SKMatrix44(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator Matrix4x4 (SKMatrix44 matrix)` | not-applicable | `new SKMatrix44(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKMatrix44 (Matrix4x4 matrix)` | not-applicable | `new SKMatrix44(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public Single M00` | implemented | `SKMatrix44.M00` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M01` | implemented | `SKMatrix44.M01` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M02` | implemented | `SKMatrix44.M02` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M03` | implemented | `SKMatrix44.M03` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M10` | implemented | `SKMatrix44.M10` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M11` | implemented | `SKMatrix44.M11` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M12` | implemented | `SKMatrix44.M12` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M13` | implemented | `SKMatrix44.M13` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M20` | implemented | `SKMatrix44.M20` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M21` | implemented | `SKMatrix44.M21` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M22` | implemented | `SKMatrix44.M22` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M23` | implemented | `SKMatrix44.M23` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M30` | implemented | `SKMatrix44.M30` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M31` | implemented | `SKMatrix44.M31` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M32` | implemented | `SKMatrix44.M32` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single M33` | implemented | `SKMatrix44.M33` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool Equals (SKMatrix44 obj)` | implemented | `SKMatrix44.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly override bool Equals (object obj)` | implemented | `SKMatrix44.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static bool operator == (SKMatrix44 left, SKMatrix44 right)` | not-applicable | `SKMatrix44.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKMatrix44 left, SKMatrix44 right)` | not-applicable | `SKMatrix44.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKMatrix44.GetHashCode` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |

### SkiaSharp.SKNWayCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKNWayCanvas (int width, int height) : this (IntPtr.Zero, true)` | not-applicable | `SKNWayCanvas(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void AddCanvas (SKCanvas canvas)` | partial | `SKNWayCanvas.AddCanvas` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void RemoveCanvas (SKCanvas canvas)` | partial | `SKNWayCanvas.RemoveCanvas` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void RemoveAll ()` | partial | `SKNWayCanvas.RemoveAll` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKNoDrawCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKNoDrawCanvas (int width, int height) : this (IntPtr.Zero, true)` | not-applicable | `SKNoDrawCanvas(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |

### SkiaSharp.SKObject

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public override IntPtr Handle` | not-applicable | `SKObject.Handle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |

### SkiaSharp.SKNativeObject

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public virtual IntPtr Handle` | not-applicable | `SKNativeObject.Handle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Dispose ()` | missing | `SKNativeObject.Dispose` — No exported browser type with this declaration identity. |

### SkiaSharp.SKOverdrawCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKOverdrawCanvas (SKCanvas canvas) : this (IntPtr.Zero, true)` | not-applicable | `SKOverdrawCanvas(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |

### SkiaSharp.SKPMColor

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPMColor (uint value)` | implemented | `SKPMColor(...)` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Alpha` | implemented | `SKPMColor.Alpha` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Red` | implemented | `SKPMColor.Red` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Green` | implemented | `SKPMColor.Green` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly byte Blue` | implemented | `SKPMColor.Blue` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKPMColor PreMultiply (SKColor color)` | implemented | `SKPMColor.PreMultiply` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKPMColor[] PreMultiply (SKColor[] colors)` | implemented | `SKPMColor.PreMultiply` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColor UnPreMultiply (SKPMColor pmcolor)` | implemented | `SKPMColor.UnPreMultiply` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static SKColor[] UnPreMultiply (SKPMColor[] pmcolors)` | implemented | `SKPMColor.UnPreMultiply` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static explicit operator SKPMColor (SKColor color)` | not-applicable | `new SKPMColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator SKColor (SKPMColor color)` | not-applicable | `new SKPMColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override string ToString ()` | implemented | `SKPMColor.ToString` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly bool Equals (SKPMColor obj)` | implemented | `SKPMColor.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public readonly override bool Equals (object other)` | implemented | `SKPMColor.Equals` — Explicit value/array/span/out overload adapters. Independent .NET reference fixtures verify named colors, 104 HSL/HSV results, all 256 alpha premultiplication/unpremultiplication cases, and float/out/tag behavior. |
| `public static bool operator == (SKPMColor left, SKPMColor right)` | not-applicable | `SKPMColor.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKPMColor left, SKPMColor right)` | not-applicable | `SKPMColor.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKPMColor.GetHashCode` — Returns the signed 32-bit underlying packed value, matching the pinned implementation. |
| `public static implicit operator SKPMColor (uint color)` | not-applicable | `new SKPMColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static explicit operator uint (SKPMColor color)` | not-applicable | `new SKPMColor(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.SKPaintHinting

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `NoHinting = 0` | implemented | `SKPaintHinting.NoHinting` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Slight = 1` | implemented | `SKPaintHinting.Slight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Normal = 2` | implemented | `SKPaintHinting.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Full = 3` | implemented | `SKPaintHinting.Full` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFilterQuality

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `None = 0` | implemented | `SKFilterQuality.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Low = 1` | implemented | `SKFilterQuality.Low` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Medium = 2` | implemented | `SKFilterQuality.Medium` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `High = 3` | implemented | `SKFilterQuality.High` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPaint

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaint () : this (SkiaApi.sk_compatpaint_new_with_font (DefaultFont.Handle), true)` | partial | `SKPaint(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPaint (SKFont font) : this (IntPtr.Zero, true)` | not-applicable | `SKPaint(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Reset ()` | partial | `SKPaint.Reset` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsAntialias` | implemented | `SKPaint.IsAntialias` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool IsDither` | implemented | `SKPaint.IsDither` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool IsLinearText` | partial | `SKPaint.IsLinearText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public bool SubpixelText` | partial | `SKPaint.SubpixelText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public bool LcdRenderText` | partial | `SKPaint.LcdRenderText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public bool IsEmbeddedBitmapText` | partial | `SKPaint.IsEmbeddedBitmapText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public bool IsAutohinted` | partial | `SKPaint.IsAutohinted` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public SKPaintHinting HintingLevel` | partial | `SKPaint.HintingLevel` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public bool FakeBoldText` | implemented | `SKPaint.FakeBoldText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool IsStroke` | implemented | `SKPaint.IsStroke` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPaintStyle Style` | partial | `SKPaint.Style` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColor Color` | implemented | `SKPaint.Color` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKColorF ColorF` | implemented | `SKPaint.ColorF` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public void SetColor (SKColorF color, SKColorSpace colorspace)` | implemented | `SKPaint.SetColor` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float StrokeWidth` | implemented | `SKPaint.StrokeWidth` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float StrokeMiter` | implemented | `SKPaint.StrokeMiter` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKStrokeCap StrokeCap` | implemented | `SKPaint.StrokeCap` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKStrokeJoin StrokeJoin` | implemented | `SKPaint.StrokeJoin` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKShader Shader` | partial | `SKPaint.Shader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKMaskFilter MaskFilter` | partial | `SKPaint.MaskFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorFilter ColorFilter` | partial | `SKPaint.ColorFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImageFilter ImageFilter` | partial | `SKPaint.ImageFilter` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBlendMode BlendMode` | partial | `SKPaint.BlendMode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKBlender Blender` | partial | `SKPaint.Blender` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFilterQuality FilterQuality` | partial | `SKPaint.FilterQuality` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface Typeface` | partial | `SKPaint.Typeface` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float TextSize` | implemented | `SKPaint.TextSize` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKTextAlign TextAlign` | implemented | `SKPaint.TextAlign` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKTextEncoding TextEncoding` | implemented | `SKPaint.TextEncoding` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float TextScaleX` | implemented | `SKPaint.TextScaleX` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float TextSkewX` | implemented | `SKPaint.TextSkewX` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPathEffect PathEffect` | partial | `SKPaint.PathEffect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float FontSpacing` | implemented | `SKPaint.FontSpacing` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKFontMetrics FontMetrics` | implemented | `SKPaint.FontMetrics` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float GetFontMetrics (out SKFontMetrics metrics)` | implemented | `SKPaint.GetFontMetrics` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPaint Clone ()` | partial | `SKPaint.Clone` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float MeasureText (string text)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (ReadOnlySpan<char> text)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (byte[] text)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (ReadOnlySpan<byte> text)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (IntPtr buffer, int length)` | not-applicable | `SKPaint.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (IntPtr buffer, IntPtr length)` | not-applicable | `SKPaint.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (string text, ref SKRect bounds)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (ReadOnlySpan<char> text, ref SKRect bounds)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (byte[] text, ref SKRect bounds)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (ReadOnlySpan<byte> text, ref SKRect bounds)` | implemented | `SKPaint.MeasureText` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float MeasureText (IntPtr buffer, int length, ref SKRect bounds)` | not-applicable | `SKPaint.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float MeasureText (IntPtr buffer, IntPtr length, ref SKRect bounds)` | not-applicable | `SKPaint.MeasureText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public long BreakText (string text, float maxWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (string text, float maxWidth, out float measuredWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (string text, float maxWidth, out float measuredWidth, out string measuredText)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (ReadOnlySpan<char> text, float maxWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (ReadOnlySpan<char> text, float maxWidth, out float measuredWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (byte[] text, float maxWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (byte[] text, float maxWidth, out float measuredWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (ReadOnlySpan<byte> text, float maxWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (ReadOnlySpan<byte> text, float maxWidth, out float measuredWidth)` | partial | `SKPaint.BreakText` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public long BreakText (IntPtr buffer, int length, float maxWidth)` | not-applicable | `SKPaint.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public long BreakText (IntPtr buffer, int length, float maxWidth, out float measuredWidth)` | not-applicable | `SKPaint.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public long BreakText (IntPtr buffer, IntPtr length, float maxWidth)` | not-applicable | `SKPaint.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public long BreakText (IntPtr buffer, IntPtr length, float maxWidth, out float measuredWidth)` | not-applicable | `SKPaint.BreakText` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (string text, float x, float y)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (ReadOnlySpan<char> text, float x, float y)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (byte[] text, float x, float y)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (ReadOnlySpan<byte> text, float x, float y)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (IntPtr buffer, int length, float x, float y)` | not-applicable | `SKPaint.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (IntPtr buffer, IntPtr length, float x, float y)` | not-applicable | `SKPaint.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (string text, SKPoint[] points)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (ReadOnlySpan<char> text, ReadOnlySpan<SKPoint> points)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (byte[] text, SKPoint[] points)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (ReadOnlySpan<byte> text, ReadOnlySpan<SKPoint> points)` | implemented | `SKPaint.GetTextPath` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPath GetTextPath (IntPtr buffer, int length, SKPoint[] points)` | not-applicable | `SKPaint.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (IntPtr buffer, int length, ReadOnlySpan<SKPoint> points)` | not-applicable | `SKPaint.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetTextPath (IntPtr buffer, IntPtr length, SKPoint[] points)` | not-applicable | `SKPaint.GetTextPath` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath GetFillPath (SKPath src)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public SKPath GetFillPath (SKPath src, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public SKPath GetFillPath (SKPath src, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public SKPath GetFillPath (SKPath src, SKRect cullRect)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public SKPath GetFillPath (SKPath src, SKRect cullRect, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public SKPath GetFillPath (SKPath src, SKRect cullRect, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst, SKRect cullRect)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst, SKRect cullRect, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPath dst, SKRect cullRect, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst, SKRect cullRect)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst, SKRect cullRect, float resScale)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFillPath (SKPath src, SKPathBuilder dst, SKRect cullRect, SKMatrix matrix)` | partial | `SKPaint.GetFillPath` — Fill/stroke/hairline and destination forms are implemented using native stroke geometry; custom effect baking depends on effect metadata. Cull and matrix arguments affect resolution hints, not a native getFillPath entrypoint. |
| `public bool GetFastBounds (SKRect bounds, out SKRect fastBounds)` | partial | `SKPaint.GetFastBounds` — Conservative fill/stroke bounds are returned; effect-bearing paints report false instead of guessing filter expansion. |
| `public int CountGlyphs (string text)` | implemented | `SKPaint.CountGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public int CountGlyphs (ReadOnlySpan<char> text)` | implemented | `SKPaint.CountGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public int CountGlyphs (byte[] text)` | implemented | `SKPaint.CountGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public int CountGlyphs (ReadOnlySpan<byte> text)` | implemented | `SKPaint.CountGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public int CountGlyphs (IntPtr text, int length)` | not-applicable | `SKPaint.CountGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int CountGlyphs (IntPtr text, IntPtr length)` | not-applicable | `SKPaint.CountGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public ushort[] GetGlyphs (string text)` | implemented | `SKPaint.GetGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public ushort[] GetGlyphs (ReadOnlySpan<char> text)` | implemented | `SKPaint.GetGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public ushort[] GetGlyphs (byte[] text)` | implemented | `SKPaint.GetGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public ushort[] GetGlyphs (ReadOnlySpan<byte> text)` | implemented | `SKPaint.GetGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public ushort[] GetGlyphs (IntPtr text, int length)` | not-applicable | `SKPaint.GetGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public ushort[] GetGlyphs (IntPtr text, IntPtr length)` | not-applicable | `SKPaint.GetGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ContainsGlyphs (string text)` | implemented | `SKPaint.ContainsGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool ContainsGlyphs (ReadOnlySpan<char> text)` | implemented | `SKPaint.ContainsGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool ContainsGlyphs (byte[] text)` | implemented | `SKPaint.ContainsGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool ContainsGlyphs (ReadOnlySpan<byte> text)` | implemented | `SKPaint.ContainsGlyphs` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public bool ContainsGlyphs (IntPtr text, int length)` | not-applicable | `SKPaint.ContainsGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ContainsGlyphs (IntPtr text, IntPtr length)` | not-applicable | `SKPaint.ContainsGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPoint[] GetGlyphPositions (string text, SKPoint origin = default)` | implemented | `SKPaint.GetGlyphPositions` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPoint[] GetGlyphPositions (ReadOnlySpan<char> text, SKPoint origin = default)` | implemented | `SKPaint.GetGlyphPositions` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPoint[] GetGlyphPositions (ReadOnlySpan<byte> text, SKPoint origin = default)` | implemented | `SKPaint.GetGlyphPositions` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public SKPoint[] GetGlyphPositions (IntPtr text, int length, SKPoint origin = default)` | not-applicable | `SKPaint.GetGlyphPositions` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphOffsets (string text, float origin = 0f)` | implemented | `SKPaint.GetGlyphOffsets` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphOffsets (ReadOnlySpan<char> text, float origin = 0f)` | implemented | `SKPaint.GetGlyphOffsets` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphOffsets (ReadOnlySpan<byte> text, float origin = 0f)` | implemented | `SKPaint.GetGlyphOffsets` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphOffsets (IntPtr text, int length, float origin = 0f)` | not-applicable | `SKPaint.GetGlyphOffsets` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (string text)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (ReadOnlySpan<char> text)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (byte[] text)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (ReadOnlySpan<byte> text)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (IntPtr text, int length)` | not-applicable | `SKPaint.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (IntPtr text, IntPtr length)` | not-applicable | `SKPaint.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (string text, out SKRect[] bounds)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (ReadOnlySpan<char> text, out SKRect[] bounds)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (byte[] text, out SKRect[] bounds)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (ReadOnlySpan<byte> text, out SKRect[] bounds)` | implemented | `SKPaint.GetGlyphWidths` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |
| `public float[] GetGlyphWidths (IntPtr text, int length, out SKRect[] bounds)` | not-applicable | `SKPaint.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetGlyphWidths (IntPtr text, IntPtr length, out SKRect[] bounds)` | not-applicable | `SKPaint.GetGlyphWidths` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetTextIntercepts (string text, float x, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetTextIntercepts (ReadOnlySpan<char> text, float x, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetTextIntercepts (byte[] text, float x, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetTextIntercepts (ReadOnlySpan<byte> text, float x, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetTextIntercepts (IntPtr text, IntPtr length, float x, float y, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetTextIntercepts (IntPtr text, int length, float x, float y, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetTextIntercepts (SKTextBlob text, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetPositionedTextIntercepts (string text, SKPoint[] positions, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetPositionedTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetPositionedTextIntercepts (ReadOnlySpan<char> text, ReadOnlySpan<SKPoint> positions, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetPositionedTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetPositionedTextIntercepts (byte[] text, SKPoint[] positions, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetPositionedTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetPositionedTextIntercepts (ReadOnlySpan<byte> text, ReadOnlySpan<SKPoint> positions, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetPositionedTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetPositionedTextIntercepts (IntPtr text, int length, SKPoint[] positions, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetPositionedTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetPositionedTextIntercepts (IntPtr text, IntPtr length, SKPoint[] positions, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetPositionedTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetHorizontalTextIntercepts (string text, float[] xpositions, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetHorizontalTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetHorizontalTextIntercepts (ReadOnlySpan<char> text, ReadOnlySpan<float> xpositions, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetHorizontalTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetHorizontalTextIntercepts (byte[] text, float[] xpositions, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetHorizontalTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetHorizontalTextIntercepts (ReadOnlySpan<byte> text, ReadOnlySpan<float> xpositions, float y, float upperBounds, float lowerBounds)` | partial | `SKPaint.GetHorizontalTextIntercepts` — Managed compatibility property/method is present. Some native font flags or historical return/byte-count conventions are not identical or not fully exercised. |
| `public float[] GetHorizontalTextIntercepts (IntPtr text, int length, float[] xpositions, float y, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetHorizontalTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float[] GetHorizontalTextIntercepts (IntPtr text, IntPtr length, float[] xpositions, float y, float upperBounds, float lowerBounds)` | not-applicable | `SKPaint.GetHorizontalTextIntercepts` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKFont ToFont ()` | implemented | `SKPaint.ToFont` — Compatibility paint creates/configures a real SKFont and dispatches text/glyph/encoding/span overloads explicitly; metrics and width equivalence tests pass. |

### SkiaSharp.SKPathConvexity

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Unknown = 0` | implemented | `SKPathConvexity.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Convex = 1` | implemented | `SKPathConvexity.Convex` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Concave = 2` | implemented | `SKPathConvexity.Concave` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPath

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public override IntPtr Handle` | not-applicable | `SKPath.Handle` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPath () : this (SkiaApi.sk_path_new (), true)` | partial | `SKPath(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPath (SKPath path) : this (SkiaApi.sk_path_clone (path.Handle), true)` | partial | `SKPath(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathFillType FillType` | implemented | `SKPath.FillType` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPathConvexity Convexity` | implemented | `SKPath.Convexity` — Compiled SkPath convexity/rectangle classification avoids geometric sampling; GetRect supports separate mutable closed/direction outputs. |
| `public bool IsConvex` | implemented | `SKPath.IsConvex` — Compiled SkPath convexity/rectangle classification avoids geometric sampling; GetRect supports separate mutable closed/direction outputs. |
| `public bool IsConcave` | implemented | `SKPath.IsConcave` — Compiled SkPath convexity/rectangle classification avoids geometric sampling; GetRect supports separate mutable closed/direction outputs. |
| `public bool IsEmpty` | implemented | `SKPath.IsEmpty` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool IsOval` | partial | `SKPath.IsOval` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsRoundRect` | partial | `SKPath.IsRoundRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsLine` | partial | `SKPath.IsLine` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsRect` | partial | `SKPath.IsRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathSegmentMask SegmentMasks` | implemented | `SKPath.SegmentMasks` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public int VerbCount` | implemented | `SKPath.VerbCount` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public int PointCount` | implemented | `SKPath.PointCount` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPoint this[int index]` | partial | `SKPath.this` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKPoint[] Points` | implemented | `SKPath.Points` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPoint LastPoint` | implemented | `SKPath.LastPoint` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKRect Bounds` | implemented | `SKPath.Bounds` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKRect TightBounds` | implemented | `SKPath.TightBounds` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKRect GetOvalBounds ()` | partial | `SKPath.GetOvalBounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRoundRect GetRoundRect ()` | partial | `SKPath.GetRoundRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPoint[] GetLine ()` | partial | `SKPath.GetLine` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRect GetRect ()` | implemented | `SKPath.GetRect` — Compiled SkPath convexity/rectangle classification avoids geometric sampling; GetRect supports separate mutable closed/direction outputs. |
| `public SKRect GetRect (out bool isClosed, out SKPathDirection direction)` | implemented | `SKPath.GetRect` — Compiled SkPath convexity/rectangle classification avoids geometric sampling; GetRect supports separate mutable closed/direction outputs. |
| `public SKPoint GetPoint (int index)` | implemented | `SKPath.GetPoint` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPoint[] GetPoints (int max)` | implemented | `SKPath.GetPoints` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public int GetPoints (SKPoint[] points, int max)` | implemented | `SKPath.GetPoints` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool Contains (float x, float y)` | implemented | `SKPath.Contains` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Offset (SKPoint offset)` | implemented | `SKPath.Offset` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Offset (float dx, float dy)` | implemented | `SKPath.Offset` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Reset ()` | implemented | `SKPath.Reset` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool GetBounds (out SKRect rect)` | implemented | `SKPath.GetBounds` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKRect ComputeTightBounds ()` | implemented | `SKPath.ComputeTightBounds` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Transform (in SKMatrix matrix)` | implemented | `SKPath.Transform` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Transform (in SKMatrix matrix, SKPath destination)` | implemented | `SKPath.Transform` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Transform (SKMatrix matrix)` | implemented | `SKPath.Transform` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Transform (SKMatrix matrix, SKPath destination)` | implemented | `SKPath.Transform` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public Iterator CreateIterator (bool forceClose)` | partial | `SKPath.CreateIterator` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public RawIterator CreateRawIterator ()` | partial | `SKPath.CreateRawIterator` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Op (SKPath other, SKPathOp op, SKPath result)` | implemented | `SKPath.Op` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPath Op (SKPath other, SKPathOp op)` | implemented | `SKPath.Op` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool Simplify (SKPath result)` | implemented | `SKPath.Simplify` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPath Simplify ()` | implemented | `SKPath.Simplify` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool GetTightBounds (out SKRect result)` | implemented | `SKPath.GetTightBounds` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public bool ToWinding (SKPath result)` | implemented | `SKPath.ToWinding` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public SKPath ToWinding ()` | implemented | `SKPath.ToWinding` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public string ToSvgPathData ()` | implemented | `SKPath.ToSvgPathData` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public static SKPath ParseSvgPathData (string svgPath)` | implemented | `SKPath.ParseSvgPathData` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public static SKPoint[] ConvertConicToQuads (SKPoint p0, SKPoint p1, SKPoint p2, float w, int pow2)` | partial | `SKPath.ConvertConicToQuads` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static int ConvertConicToQuads (SKPoint p0, SKPoint p1, SKPoint p2, float w, out SKPoint[] pts, int pow2)` | partial | `SKPath.ConvertConicToQuads` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static int ConvertConicToQuads (SKPoint p0, SKPoint p1, SKPoint p2, float w, SKPoint[] pts, int pow2)` | partial | `SKPath.ConvertConicToQuads` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void MoveTo (SKPoint point)` | implemented | `SKPath.MoveTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void MoveTo (float x, float y)` | implemented | `SKPath.MoveTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RMoveTo (SKPoint point)` | implemented | `SKPath.RMoveTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RMoveTo (float dx, float dy)` | implemented | `SKPath.RMoveTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void LineTo (SKPoint point)` | implemented | `SKPath.LineTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void LineTo (float x, float y)` | implemented | `SKPath.LineTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RLineTo (SKPoint point)` | implemented | `SKPath.RLineTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RLineTo (float dx, float dy)` | implemented | `SKPath.RLineTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void QuadTo (SKPoint point0, SKPoint point1)` | implemented | `SKPath.QuadTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void QuadTo (float x0, float y0, float x1, float y1)` | implemented | `SKPath.QuadTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RQuadTo (SKPoint point0, SKPoint point1)` | implemented | `SKPath.RQuadTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RQuadTo (float dx0, float dy0, float dx1, float dy1)` | implemented | `SKPath.RQuadTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ConicTo (SKPoint point0, SKPoint point1, float w)` | implemented | `SKPath.ConicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ConicTo (float x0, float y0, float x1, float y1, float w)` | implemented | `SKPath.ConicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RConicTo (SKPoint point0, SKPoint point1, float w)` | implemented | `SKPath.RConicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RConicTo (float dx0, float dy0, float dx1, float dy1, float w)` | implemented | `SKPath.RConicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void CubicTo (SKPoint point0, SKPoint point1, SKPoint point2)` | implemented | `SKPath.CubicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void CubicTo (float x0, float y0, float x1, float y1, float x2, float y2)` | implemented | `SKPath.CubicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RCubicTo (SKPoint point0, SKPoint point1, SKPoint point2)` | implemented | `SKPath.RCubicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RCubicTo (float dx0, float dy0, float dx1, float dy1, float dx2, float dy2)` | implemented | `SKPath.RCubicTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ArcTo (SKPoint r, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, SKPoint xy)` | implemented | `SKPath.ArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ArcTo (float rx, float ry, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, float x, float y)` | implemented | `SKPath.ArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ArcTo (SKRect oval, float startAngle, float sweepAngle, bool forceMoveTo)` | implemented | `SKPath.ArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ArcTo (SKPoint point1, SKPoint point2, float radius)` | implemented | `SKPath.ArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void ArcTo (float x1, float y1, float x2, float y2, float radius)` | implemented | `SKPath.ArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RArcTo (SKPoint r, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, SKPoint xy)` | implemented | `SKPath.RArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void RArcTo (float rx, float ry, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, float x, float y)` | implemented | `SKPath.RArcTo` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Close ()` | implemented | `SKPath.Close` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void Rewind ()` | implemented | `SKPath.Rewind` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddRect (SKRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | implemented | `SKPath.AddRect` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddRect (SKRect rect, SKPathDirection direction, uint startIndex)` | implemented | `SKPath.AddRect` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddRoundRect (SKRoundRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | partial | `SKPath.AddRoundRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddRoundRect (SKRoundRect rect, SKPathDirection direction, uint startIndex)` | implemented | `SKPath.AddRoundRect` — All eight rounded-rectangle start indices and both contour directions emit real conic/line paths; equal rendered pixels across all sixteen variants are verified. |
| `public void AddRoundRect (SKRect rect, float rx, float ry, SKPathDirection dir = SKPathDirection.Clockwise)` | partial | `SKPath.AddRoundRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddOval (SKRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | implemented | `SKPath.AddOval` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddArc (SKRect oval, float startAngle, float sweepAngle)` | implemented | `SKPath.AddArc` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddCircle (float x, float y, float radius, SKPathDirection dir = SKPathDirection.Clockwise)` | implemented | `SKPath.AddCircle` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPoly (SKPoint[] points, bool close = true)` | implemented | `SKPath.AddPoly` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPoly (ReadOnlySpan<SKPoint> points, bool close = true)` | implemented | `SKPath.AddPoly` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPath (SKPath other, float dx, float dy, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPath.AddPath` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPath (SKPath other, in SKMatrix matrix, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPath.AddPath` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPath (SKPath other, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPath.AddPath` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |
| `public void AddPathReverse (SKPath other)` | implemented | `SKPath.AddPathReverse` — Explicit existing path argument dispatch uses native Skia path builders/operations; copy/destination and relative/absolute coordinate forms are supported. Existing paths.test.mjs validates these families. |

### SkiaSharp.SKPath.Iterator

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPathVerb Next (SKPoint[] points)` | partial | `SKPath.Iterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathVerb Next (Span<SKPoint> points)` | partial | `SKPath.Iterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float ConicWeight ()` | partial | `SKPath.Iterator.ConicWeight` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsCloseLine ()` | partial | `SKPath.Iterator.IsCloseLine` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsCloseContour ()` | partial | `SKPath.Iterator.IsCloseContour` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPath.RawIterator

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPathVerb Next (SKPoint[] points)` | partial | `SKPath.RawIterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathVerb Next (Span<SKPoint> points)` | partial | `SKPath.RawIterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public float ConicWeight ()` | partial | `SKPath.RawIterator.ConicWeight` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathVerb Peek ()` | partial | `SKPath.RawIterator.Peek` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPath.OpBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public OpBuilder () : base (SkiaApi.sk_opbuilder_new (), true)` | partial | `SKPath.OpBuilder(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Add (SKPath path, SKPathOp op)` | partial | `SKPath.OpBuilder.Add` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Resolve (SKPath result)` | partial | `SKPath.OpBuilder.Resolve` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPathBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPathBuilder () : this (SkiaApi.sk_pathbuilder_new (), true)` | implemented | `SKPathBuilder(...)` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public SKPathBuilder (SKPath path) : this (SkiaApi.sk_pathbuilder_new_from_path (path?.Handle ?? throw new ArgumentNullException (nameof (path))), true)` | implemented | `SKPathBuilder(...)` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public SKPathFillType FillType` | implemented | `SKPathBuilder.FillType` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public SKPath Detach ()` | implemented | `SKPathBuilder.Detach` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public SKPath Snapshot ()` | implemented | `SKPathBuilder.Snapshot` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void Reset ()` | implemented | `SKPathBuilder.Reset` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void MoveTo (SKPoint point)` | implemented | `SKPathBuilder.MoveTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void MoveTo (float x, float y)` | implemented | `SKPathBuilder.MoveTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RMoveTo (SKPoint point)` | implemented | `SKPathBuilder.RMoveTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RMoveTo (float dx, float dy)` | implemented | `SKPathBuilder.RMoveTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void LineTo (SKPoint point)` | implemented | `SKPathBuilder.LineTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void LineTo (float x, float y)` | implemented | `SKPathBuilder.LineTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RLineTo (SKPoint point)` | implemented | `SKPathBuilder.RLineTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RLineTo (float dx, float dy)` | implemented | `SKPathBuilder.RLineTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void QuadTo (SKPoint point0, SKPoint point1)` | implemented | `SKPathBuilder.QuadTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void QuadTo (float x0, float y0, float x1, float y1)` | implemented | `SKPathBuilder.QuadTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RQuadTo (SKPoint point0, SKPoint point1)` | implemented | `SKPathBuilder.RQuadTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RQuadTo (float dx0, float dy0, float dx1, float dy1)` | implemented | `SKPathBuilder.RQuadTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ConicTo (SKPoint point0, SKPoint point1, float w)` | implemented | `SKPathBuilder.ConicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ConicTo (float x0, float y0, float x1, float y1, float w)` | implemented | `SKPathBuilder.ConicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RConicTo (SKPoint point0, SKPoint point1, float w)` | implemented | `SKPathBuilder.RConicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RConicTo (float dx0, float dy0, float dx1, float dy1, float w)` | implemented | `SKPathBuilder.RConicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void CubicTo (SKPoint point0, SKPoint point1, SKPoint point2)` | implemented | `SKPathBuilder.CubicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void CubicTo (float x0, float y0, float x1, float y1, float x2, float y2)` | implemented | `SKPathBuilder.CubicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RCubicTo (SKPoint point0, SKPoint point1, SKPoint point2)` | implemented | `SKPathBuilder.RCubicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RCubicTo (float dx0, float dy0, float dx1, float dy1, float dx2, float dy2)` | implemented | `SKPathBuilder.RCubicTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ArcTo (SKPoint r, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, SKPoint xy)` | implemented | `SKPathBuilder.ArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ArcTo (float rx, float ry, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, float x, float y)` | implemented | `SKPathBuilder.ArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ArcTo (SKRect oval, float startAngle, float sweepAngle, bool forceMoveTo)` | implemented | `SKPathBuilder.ArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ArcTo (SKPoint point1, SKPoint point2, float radius)` | implemented | `SKPathBuilder.ArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ArcTo (float x1, float y1, float x2, float y2, float radius)` | implemented | `SKPathBuilder.ArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RArcTo (SKPoint r, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, SKPoint xy)` | implemented | `SKPathBuilder.RArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void RArcTo (float rx, float ry, float xAxisRotate, SKPathArcSize largeArc, SKPathDirection sweep, float x, float y)` | implemented | `SKPathBuilder.RArcTo` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void Close ()` | implemented | `SKPathBuilder.Close` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddRect (SKRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | implemented | `SKPathBuilder.AddRect` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddRect (SKRect rect, SKPathDirection direction, uint startIndex)` | implemented | `SKPathBuilder.AddRect` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddRoundRect (SKRoundRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | implemented | `SKPathBuilder.AddRoundRect` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddRoundRect (SKRoundRect rect, SKPathDirection direction, uint startIndex)` | implemented | `SKPathBuilder.AddRoundRect` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddOval (SKRect rect, SKPathDirection direction = SKPathDirection.Clockwise)` | implemented | `SKPathBuilder.AddOval` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddArc (SKRect oval, float startAngle, float sweepAngle)` | implemented | `SKPathBuilder.AddArc` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddRoundRect (SKRect rect, float rx, float ry, SKPathDirection dir = SKPathDirection.Clockwise)` | implemented | `SKPathBuilder.AddRoundRect` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddCircle (float x, float y, float radius, SKPathDirection dir = SKPathDirection.Clockwise)` | implemented | `SKPathBuilder.AddCircle` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddPoly (ReadOnlySpan<SKPoint> points, bool close = true)` | implemented | `SKPathBuilder.AddPoly` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddPoly (SKPoint[] points, bool close = true)` | implemented | `SKPathBuilder.AddPoly` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddPath (SKPath other, float dx, float dy, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPathBuilder.AddPath` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddPath (SKPath other, in SKMatrix matrix, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPathBuilder.AddPath` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void AddPath (SKPath other, SKPathAddMode mode = SKPathAddMode.Append)` | implemented | `SKPathBuilder.AddPath` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |
| `public void ReverseAddPath (SKPath other)` | implemented | `SKPathBuilder.ReverseAddPath` — Native SkPathBuilder storage shares the explicitly dispatched mutable path point/scalar/span/matrix adapters. Snapshot/detach ownership and contour geometry are checked in path tests. |

### SkiaSharp.SKPathEffect

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPathEffect CreateCompose(SKPathEffect outer, SKPathEffect inner)` | implemented | `SKPathEffect.CreateCompose` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect CreateSum(SKPathEffect first, SKPathEffect second)` | implemented | `SKPathEffect.CreateSum` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect CreateDiscrete(float segLength, float deviation, UInt32 seedAssist = 0)` | implemented | `SKPathEffect.CreateDiscrete` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect CreateCorner(float radius)` | implemented | `SKPathEffect.CreateCorner` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect Create1DPath(SKPath path, float advance, float phase, SKPath1DPathEffectStyle style)` | implemented | `SKPathEffect.Create1DPath` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect Create2DLine(float width, SKMatrix matrix)` | implemented | `SKPathEffect.Create2DLine` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect Create2DPath(SKMatrix matrix, SKPath path)` | implemented | `SKPathEffect.Create2DPath` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect CreateDash(float[] intervals, float phase)` | partial | `SKPathEffect.CreateDash` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKPathEffect CreateTrim(float start, float stop)` | implemented | `SKPathEffect.CreateTrim` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |
| `public static SKPathEffect CreateTrim(float start, float stop, SKTrimPathEffectMode mode)` | implemented | `SKPathEffect.CreateTrim` — Concrete compiled native Skia effect factory plus portable fallback. Independent native/.NET fixtures validate specialized effect pixels/path geometry; not a method-name-only certification. |

### SkiaSharp.SKPathMeasure

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPathMeasure () : this (SkiaApi.sk_pathmeasure_new (), true)` | partial | `SKPathMeasure(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPathMeasure (SKPath path, bool forceClosed = false, float resScale = 1) : this (IntPtr.Zero, true)` | not-applicable | `SKPathMeasure(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public float Length` | partial | `SKPathMeasure.Length` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsClosed` | partial | `SKPathMeasure.IsClosed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetPath (SKPath path)` | partial | `SKPathMeasure.SetPath` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetPath (SKPath path, bool forceClosed)` | partial | `SKPathMeasure.SetPath` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetPositionAndTangent (float distance, out SKPoint position, out SKPoint tangent)` | partial | `SKPathMeasure.GetPositionAndTangent` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPoint GetPosition (float distance)` | partial | `SKPathMeasure.GetPosition` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetPosition (float distance, out SKPoint position)` | partial | `SKPathMeasure.GetPosition` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPoint GetTangent (float distance)` | partial | `SKPathMeasure.GetTangent` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetTangent (float distance, out SKPoint tangent)` | partial | `SKPathMeasure.GetTangent` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKMatrix GetMatrix (float distance, SKPathMeasureMatrixFlags flags)` | partial | `SKPathMeasure.GetMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetMatrix (float distance, out SKMatrix matrix, SKPathMeasureMatrixFlags flags)` | partial | `SKPathMeasure.GetMatrix` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetSegment (float start, float stop, SKPathBuilder dst, bool startWithMoveTo)` | partial | `SKPathMeasure.GetSegment` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetSegment (float start, float stop, SKPath dst, bool startWithMoveTo)` | partial | `SKPathMeasure.GetSegment` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPath GetSegment (float start, float stop, bool startWithMoveTo)` | partial | `SKPathMeasure.GetSegment` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool NextContour ()` | partial | `SKPathMeasure.NextContour` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPicture

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public uint UniqueId` | partial | `SKPicture.UniqueId` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKRect CullRect` | partial | `SKPicture.CullRect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int ApproximateBytesUsed` | partial | `SKPicture.ApproximateBytesUsed` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int ApproximateOperationCount` | implemented | `SKPicture.ApproximateOperationCount` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public int GetApproximateOperationCount(bool includeNested)` | implemented | `SKPicture.GetApproximateOperationCount` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public SKData Serialize ()` | partial | `SKPicture.Serialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Serialize (Stream stream)` | partial | `SKPicture.Serialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Serialize (SKWStream stream)` | partial | `SKPicture.Serialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Playback (SKCanvas canvas)` | partial | `SKPicture.Playback` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader ()` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKRect tile)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode, SKRect tile)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKMatrix localMatrix, SKRect tile)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKShader ToShader (SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode, SKMatrix localMatrix, SKRect tile)` | partial | `SKPicture.ToShader` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKPicture Deserialize (IntPtr data, int length)` | not-applicable | `SKPicture.Deserialize` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKPicture Deserialize (ReadOnlySpan<byte> data)` | partial | `SKPicture.Deserialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKPicture Deserialize (SKData data)` | partial | `SKPicture.Deserialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKPicture Deserialize (Stream stream)` | partial | `SKPicture.Deserialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKPicture Deserialize (SKStream stream)` | partial | `SKPicture.Deserialize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPictureRecorder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPictureRecorder () : this (SkiaApi.sk_picture_recorder_new (), true)` | partial | `SKPictureRecorder(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCanvas BeginRecording (SKRect cullRect)` | partial | `SKPictureRecorder.BeginRecording` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKCanvas BeginRecording (SKRect cullRect, bool useRTree)` | partial | `SKPictureRecorder.BeginRecording` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPicture EndRecording ()` | partial | `SKPictureRecorder.EndRecording` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKDrawable EndRecordingAsDrawable ()` | implemented | `SKPictureRecorder.EndRecordingAsDrawable` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public SKCanvas RecordingCanvas` | partial | `SKPictureRecorder.RecordingCanvas` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |

### SkiaSharp.SKPixmap

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPixmap () : this (SkiaApi.sk_pixmap_new (), true)` | partial | `SKPixmap(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPixmap (SKImageInfo info, IntPtr addr) : this (info, addr, info.RowBytes)` | not-applicable | `SKPixmap(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKPixmap (SKImageInfo info, IntPtr addr, int rowBytes) : this (IntPtr.Zero, true)` | not-applicable | `SKPixmap(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void Reset ()` | implemented | `SKPixmap.Reset` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void Reset (SKImageInfo info, IntPtr addr, int rowBytes)` | not-applicable | `SKPixmap.Reset` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKImageInfo Info` | partial | `SKPixmap.Info` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Width` | partial | `SKPixmap.Width` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Height` | partial | `SKPixmap.Height` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKSizeI Size` | partial | `SKPixmap.Size` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRectI Rect` | partial | `SKPixmap.Rect` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorType ColorType` | partial | `SKPixmap.ColorType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKAlphaType AlphaType` | partial | `SKPixmap.AlphaType` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorSpace? ColorSpace` | partial | `SKPixmap.ColorSpace` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int BytesPerPixel` | partial | `SKPixmap.BytesPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int BitShiftPerPixel` | partial | `SKPixmap.BitShiftPerPixel` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int RowBytes` | partial | `SKPixmap.RowBytes` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int BytesSize` | partial | `SKPixmap.BytesSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public long BytesSize64` | partial | `SKPixmap.BytesSize64` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr GetPixels ()` | not-applicable | `SKPixmap.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public IntPtr GetPixels (int x, int y)` | not-applicable | `SKPixmap.GetPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public Span<byte> GetPixelSpan ()` | implemented | `SKPixmap.GetPixelSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<byte> GetPixelSpan (int x, int y)` | implemented | `SKPixmap.GetPixelSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public unsafe Span<T> GetPixelSpan<T> () where T : unmanaged` | implemented | `SKPixmap.GetPixelSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public unsafe Span<T> GetPixelSpan<T> (int x, int y) where T : unmanaged` | implemented | `SKPixmap.GetPixelSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKColor GetPixelColor (int x, int y)` | partial | `SKPixmap.GetPixelColor` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKColorF GetPixelColorF (int x, int y)` | implemented | `SKPixmap.GetPixelColorF` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public float GetPixelAlpha (int x, int y)` | implemented | `SKPixmap.GetPixelAlpha` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ScalePixels (SKPixmap destination, SKFilterQuality quality)` | partial | `SKPixmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap destination)` | partial | `SKPixmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ScalePixels (SKPixmap destination, SKSamplingOptions sampling)` | partial | `SKPixmap.ScalePixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes, int srcX, int srcY)` | not-applicable | `SKPixmap.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes)` | not-applicable | `SKPixmap.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ReadPixels (SKPixmap pixmap, int srcX, int srcY)` | partial | `SKPixmap.ReadPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ReadPixels (SKPixmap pixmap)` | partial | `SKPixmap.ReadPixels` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData? Encode (SKEncodedImageFormat encoder, int quality)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (Stream dst, SKEncodedImageFormat encoder, int quality)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (SKWStream dst, SKEncodedImageFormat encoder, int quality)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData? Encode (SKWebpEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (Stream dst, SKWebpEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (SKWStream dst, SKWebpEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData? Encode (SKJpegEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (Stream dst, SKJpegEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (SKWStream dst, SKJpegEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData? Encode (SKPngEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (Stream dst, SKPngEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Encode (SKWStream dst, SKPngEncoderOptions options)` | partial | `SKPixmap.Encode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPixmap? ExtractSubset (SKRectI subset)` | implemented | `SKPixmap.ExtractSubset` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ExtractSubset (SKPixmap result, SKRectI subset)` | implemented | `SKPixmap.ExtractSubset` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool Erase (SKColor color)` | partial | `SKPixmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Erase (SKColor color, SKRectI subset)` | partial | `SKPixmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Erase (SKColorF color)` | partial | `SKPixmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Erase (SKColorF color, SKRectI subset)` | partial | `SKPixmap.Erase` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ComputeIsOpaque ()` | implemented | `SKPixmap.ComputeIsOpaque` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKPixmap WithColorType (SKColorType newColorType)` | implemented | `SKPixmap.WithColorType` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKPixmap WithColorSpace (SKColorSpace newColorSpace)` | implemented | `SKPixmap.WithColorSpace` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKPixmap WithAlphaType (SKAlphaType newAlphaType)` | implemented | `SKPixmap.WithAlphaType` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKRegion

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRegion () : this (SkiaApi.sk_region_new (), true)` | implemented | `SKRegion(...)` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SKRegion (SKRegion region) : this ()` | implemented | `SKRegion(...)` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SKRegion (SKRectI rect) : this ()` | implemented | `SKRegion(...)` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SKRegion (SKPath path) : this ()` | implemented | `SKRegion(...)` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool IsEmpty` | implemented | `SKRegion.IsEmpty` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool IsRect` | implemented | `SKRegion.IsRect` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool IsComplex` | implemented | `SKRegion.IsComplex` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SKRectI Bounds` | implemented | `SKRegion.Bounds` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SKPath GetBoundaryPath ()` | implemented | `SKRegion.GetBoundaryPath` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Contains (SKPath path)` | implemented | `SKRegion.Contains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Contains (SKRegion src)` | implemented | `SKRegion.Contains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Contains (SKPointI xy)` | implemented | `SKRegion.Contains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Contains (int x, int y)` | implemented | `SKRegion.Contains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Contains (SKRectI rect)` | implemented | `SKRegion.Contains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool QuickContains (SKRectI rect)` | implemented | `SKRegion.QuickContains` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool QuickReject (SKRectI rect)` | implemented | `SKRegion.QuickReject` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool QuickReject (SKRegion region)` | implemented | `SKRegion.QuickReject` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool QuickReject (SKPath path)` | implemented | `SKRegion.QuickReject` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Intersects (SKPath path)` | implemented | `SKRegion.Intersects` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Intersects (SKRegion region)` | implemented | `SKRegion.Intersects` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Intersects (SKRectI rect)` | implemented | `SKRegion.Intersects` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public void SetEmpty ()` | implemented | `SKRegion.SetEmpty` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool SetRegion (SKRegion region)` | implemented | `SKRegion.SetRegion` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool SetRect (SKRectI rect)` | implemented | `SKRegion.SetRect` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool SetRects (ReadOnlySpan<SKRectI> rects)` | implemented | `SKRegion.SetRects` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool SetPath (SKPath path, SKRegion clip)` | implemented | `SKRegion.SetPath` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool SetPath (SKPath path)` | implemented | `SKRegion.SetPath` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public void Translate (int x, int y)` | implemented | `SKRegion.Translate` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Op (SKRectI rect, SKRegionOperation op)` | implemented | `SKRegion.Op` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Op (int left, int top, int right, int bottom, SKRegionOperation op)` | implemented | `SKRegion.Op` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Op (SKRegion region, SKRegionOperation op)` | implemented | `SKRegion.Op` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public bool Op (SKPath path, SKRegionOperation op)` | implemented | `SKRegion.Op` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public RectIterator CreateRectIterator ()` | implemented | `SKRegion.CreateRectIterator` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public ClipIterator CreateClipIterator (SKRectI clip)` | implemented | `SKRegion.CreateClipIterator` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |
| `public SpanIterator CreateSpanIterator (int y, int left, int right)` | implemented | `SKRegion.CreateSpanIterator` — Integer band/run region operations and exact native scan conversion/serialization are exercised against 36 independent Skia reference regions, including Boolean algebra, iterator boundaries and native binary roundtrips. |

### SkiaSharp.SKRegion.RectIterator

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool Next (out SKRectI rect)` | partial | `SKRegion.RectIterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKRegion.ClipIterator

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool Next (out SKRectI rect)` | partial | `SKRegion.ClipIterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKRegion.SpanIterator

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool Next (out int left, out int right)` | partial | `SKRegion.SpanIterator.Next` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKRotationScaleMatrix

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly SKRotationScaleMatrix Empty` | implemented | `SKRotationScaleMatrix.Empty` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static readonly SKRotationScaleMatrix Identity = new SKRotationScaleMatrix (1, 0, 0, 0)` | implemented | `SKRotationScaleMatrix(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public SKRotationScaleMatrix (float scos, float ssin, float tx, float ty)` | implemented | `SKRotationScaleMatrix(...)` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly SKMatrix ToMatrix ()` | implemented | `SKRotationScaleMatrix.ToMatrix` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateDegrees (float scale, float degrees, float tx, float ty, float anchorX, float anchorY)` | implemented | `SKRotationScaleMatrix.CreateDegrees` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix Create (float scale, float radians, float tx, float ty, float anchorX, float anchorY)` | implemented | `SKRotationScaleMatrix.Create` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateIdentity ()` | implemented | `SKRotationScaleMatrix.CreateIdentity` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateTranslation (float x, float y)` | implemented | `SKRotationScaleMatrix.CreateTranslation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateScale (float s)` | implemented | `SKRotationScaleMatrix.CreateScale` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateRotation (float radians, float anchorX, float anchorY)` | implemented | `SKRotationScaleMatrix.CreateRotation` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static SKRotationScaleMatrix CreateRotationDegrees (float degrees, float anchorX, float anchorY)` | implemented | `SKRotationScaleMatrix.CreateRotationDegrees` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single SCos` | implemented | `SKRotationScaleMatrix.SCos` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single SSin` | implemented | `SKRotationScaleMatrix.SSin` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single TX` | implemented | `SKRotationScaleMatrix.TX` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public Single TY` | implemented | `SKRotationScaleMatrix.TY` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly bool Equals (SKRotationScaleMatrix obj)` | implemented | `SKRotationScaleMatrix.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public readonly override bool Equals (object obj)` | implemented | `SKRotationScaleMatrix.Equals` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |
| `public static bool operator == (SKRotationScaleMatrix left, SKRotationScaleMatrix right)` | not-applicable | `SKRotationScaleMatrix.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKRotationScaleMatrix left, SKRotationScaleMatrix right)` | not-applicable | `SKRotationScaleMatrix.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | implemented | `SKRotationScaleMatrix.GetHashCode` — Explicit value overload adapter: System.Numerics row-vector fields, row/column arrays, constructor forms, immutable matrix arithmetic, mutable destination outputs, and point conversion. See overloads.test.mjs matrix cases. |

### SkiaSharp.SKRoundRect

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRoundRect () : this (SkiaApi.sk_rrect_new (), true)` | implemented | `SKRoundRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRoundRect (SKRect rect) : this (SkiaApi.sk_rrect_new (), true)` | implemented | `SKRoundRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRoundRect (SKRect rect, float radius) : this (rect, radius, radius)` | implemented | `SKRoundRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRoundRect (SKRect rect, float xRadius, float yRadius) : this (SkiaApi.sk_rrect_new (), true)` | implemented | `SKRoundRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRoundRect (SKRoundRect rrect) : this (SkiaApi.sk_rrect_new_copy (rrect.Handle), true)` | implemented | `SKRoundRect(...)` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRect Rect` | implemented | `SKRoundRect.Rect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPoint[] Radii` | implemented | `SKRoundRect.Radii` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKRoundRectType Type` | implemented | `SKRoundRect.Type` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public float Width` | implemented | `SKRoundRect.Width` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public float Height` | implemented | `SKRoundRect.Height` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public bool IsValid` | implemented | `SKRoundRect.IsValid` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public bool AllCornersCircular` | implemented | `SKRoundRect.AllCornersCircular` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public bool CheckAllCornersCircular (float tolerance)` | implemented | `SKRoundRect.CheckAllCornersCircular` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetEmpty ()` | implemented | `SKRoundRect.SetEmpty` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetRect (SKRect rect)` | implemented | `SKRoundRect.SetRect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetRect (SKRect rect, float xRadius, float yRadius)` | implemented | `SKRoundRect.SetRect` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetOval (SKRect rect)` | implemented | `SKRoundRect.SetOval` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetNinePatch (SKRect rect, float leftRadius, float topRadius, float rightRadius, float bottomRadius)` | implemented | `SKRoundRect.SetNinePatch` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetRectRadii (SKRect rect, SKPoint[] radii)` | implemented | `SKRoundRect.SetRectRadii` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void SetRectRadii (SKRect rect, ReadOnlySpan<SKPoint> radii)` | implemented | `SKRoundRect.SetRectRadii` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public bool Contains (SKRect rect)` | implemented | `SKRoundRect.Contains` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public SKPoint GetRadii (SKRoundRectCorner corner)` | implemented | `SKRoundRect.GetRadii` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Deflate (SKSize size)` | implemented | `SKRoundRect.Deflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Deflate (float dx, float dy)` | implemented | `SKRoundRect.Deflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (SKSize size)` | implemented | `SKRoundRect.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Inflate (float dx, float dy)` | implemented | `SKRoundRect.Inflate` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (SKPoint pos)` | implemented | `SKRoundRect.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public void Offset (float dx, float dy)` | implemented | `SKRoundRect.Offset` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |
| `public bool TryTransform (SKMatrix matrix, out SKRoundRect transformed)` | implemented | `SKRoundRect.TryTransform` — Supports translation, scale/reflection, and axis-swapping rotations; returns false for transforms that cannot remain an axis-aligned rounded rectangle. Mutable output or {Success,Transformed} convenience form. |
| `public SKRoundRect Transform (SKMatrix matrix)` | implemented | `SKRoundRect.Transform` — Explicit point/size/rectangle constructor and overload dispatch, or unchanged scalar property. Geometry and rounded-rectangle transformation regression cases pass. |

### SkiaSharp.SKRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public int Size` | implemented | `SKRunBuffer.Size` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<ushort> Glyphs` | implemented | `SKRunBuffer.Glyphs` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetGlyphs (ReadOnlySpan<ushort> glyphs)` | implemented | `SKRunBuffer.SetGlyphs` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<ushort> GetGlyphSpan ()` | implemented | `SKRunBuffer.GetGlyphSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKHorizontalRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<float> Positions` | implemented | `SKHorizontalRunBuffer.Positions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetPositions (ReadOnlySpan<float> positions)` | implemented | `SKHorizontalRunBuffer.SetPositions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<float> GetPositionSpan ()` | implemented | `SKHorizontalRunBuffer.GetPositionSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKPositionedRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<SKPoint> Positions` | implemented | `SKPositionedRunBuffer.Positions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetPositions (ReadOnlySpan<SKPoint> positions)` | implemented | `SKPositionedRunBuffer.SetPositions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<SKPoint> GetPositionSpan ()` | implemented | `SKPositionedRunBuffer.GetPositionSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKRotationScaleRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<SKRotationScaleMatrix> Positions` | implemented | `SKRotationScaleRunBuffer.Positions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetPositions (ReadOnlySpan<SKRotationScaleMatrix> positions)` | implemented | `SKRotationScaleRunBuffer.SetPositions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<SKRotationScaleMatrix> GetRotationScaleSpan ()` | implemented | `SKRotationScaleRunBuffer.GetRotationScaleSpan` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetRotationScale (ReadOnlySpan<SKRotationScaleMatrix> positions)` | implemented | `SKRotationScaleRunBuffer.SetRotationScale` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKTextRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public int TextSize` | partial | `SKTextRunBuffer.TextSize` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Span<byte> Text` | implemented | `SKTextRunBuffer.Text` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<uint> Clusters` | implemented | `SKTextRunBuffer.Clusters` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetText (ReadOnlySpan<byte> text)` | implemented | `SKTextRunBuffer.SetText` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void SetClusters (ReadOnlySpan<uint> clusters)` | implemented | `SKTextRunBuffer.SetClusters` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKHorizontalTextRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<float> Positions` | partial | `SKHorizontalTextRunBuffer.Positions` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public void SetPositions (ReadOnlySpan<float> positions)` | partial | `SKHorizontalTextRunBuffer.SetPositions` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKPositionedTextRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<SKPoint> Positions` | partial | `SKPositionedTextRunBuffer.Positions` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public void SetPositions (ReadOnlySpan<SKPoint> positions)` | partial | `SKPositionedTextRunBuffer.SetPositions` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKRotationScaleTextRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<SKRotationScaleMatrix> Positions` | partial | `SKRotationScaleTextRunBuffer.Positions` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public void SetPositions (ReadOnlySpan<SKRotationScaleMatrix> positions)` | partial | `SKRotationScaleTextRunBuffer.SetPositions` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKRawRunBuffer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Span<ushort> Glyphs` | implemented | `SKRawRunBuffer.Glyphs` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<T> Positions` | implemented | `SKRawRunBuffer.Positions` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<byte> Text` | implemented | `SKRawRunBuffer.Text` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Span<uint> Clusters` | implemented | `SKRawRunBuffer.Clusters` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKRuntimeEffect

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKRuntimeEffect CreateShader (string sksl, out string errors)` | implemented | `SKRuntimeEffect.CreateShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public static SKRuntimeEffect CreateColorFilter (string sksl, out string errors)` | implemented | `SKRuntimeEffect.CreateColorFilter` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public static SKRuntimeEffect CreateBlender (string sksl, out string errors)` | implemented | `SKRuntimeEffect.CreateBlender` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public static SKRuntimeShaderBuilder BuildShader (string sksl)` | implemented | `SKRuntimeEffect.BuildShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public static SKRuntimeColorFilterBuilder BuildColorFilter (string sksl)` | implemented | `SKRuntimeEffect.BuildColorFilter` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public static SKRuntimeBlenderBuilder BuildBlender (string sksl)` | implemented | `SKRuntimeEffect.BuildBlender` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public int UniformSize` | implemented | `SKRuntimeEffect.UniformSize` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public IReadOnlyList<string> Children` | implemented | `SKRuntimeEffect.Children` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public IReadOnlyList<string> Uniforms` | implemented | `SKRuntimeEffect.Uniforms` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKShader ToShader ()` | implemented | `SKRuntimeEffect.ToShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKShader ToShader (SKRuntimeEffectUniforms uniforms)` | implemented | `SKRuntimeEffect.ToShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKShader ToShader (SKRuntimeEffectUniforms uniforms, SKRuntimeEffectChildren children)` | implemented | `SKRuntimeEffect.ToShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKShader ToShader (SKRuntimeEffectUniforms uniforms, SKRuntimeEffectChildren children, SKMatrix localMatrix)` | implemented | `SKRuntimeEffect.ToShader` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKColorFilter ToColorFilter ()` | implemented | `SKRuntimeEffect.ToColorFilter` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKColorFilter ToColorFilter (SKRuntimeEffectUniforms uniforms)` | implemented | `SKRuntimeEffect.ToColorFilter` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKColorFilter ToColorFilter (SKRuntimeEffectUniforms uniforms, SKRuntimeEffectChildren children)` | implemented | `SKRuntimeEffect.ToColorFilter` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKBlender ToBlender ()` | implemented | `SKRuntimeEffect.ToBlender` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKBlender ToBlender (SKRuntimeEffectUniforms uniforms)` | implemented | `SKRuntimeEffect.ToBlender` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |
| `public SKBlender ToBlender (SKRuntimeEffectUniforms uniforms, SKRuntimeEffectChildren children)` | implemented | `SKRuntimeEffect.ToBlender` — Compiled native shader/color-filter/blender creation and raw uniform buffers support all three child kinds without float32 integer precision loss. Compilation errors, child order, matrix and uniform return APIs are covered by native conformance tests. |

### SkiaSharp.SKRuntimeEffectUniforms

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeEffectUniforms (SKRuntimeEffect effect)` | implemented | `SKRuntimeEffectUniforms(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public IReadOnlyList<string> Names` | implemented | `SKRuntimeEffectUniforms.Names` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public int Count` | implemented | `SKRuntimeEffectUniforms.Count` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public int Size` | implemented | `SKRuntimeEffectUniforms.Size` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Reset ()` | implemented | `SKRuntimeEffectUniforms.Reset` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public bool Contains (string name)` | implemented | `SKRuntimeEffectUniforms.Contains` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectUniform this[string name]` | implemented | `SKRuntimeEffectUniforms.this` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Add (string name, SKRuntimeEffectUniform value)` | implemented | `SKRuntimeEffectUniforms.Add` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKData ToData ()` | implemented | `SKRuntimeEffectUniforms.ToData` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public IEnumerator<string> GetEnumerator ()` | implemented | `SKRuntimeEffectUniforms.GetEnumerator` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Dispose ()` | implemented | `SKRuntimeEffectUniforms.Dispose` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeEffectChildren

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeEffectChildren (SKRuntimeEffect effect)` | implemented | `SKRuntimeEffectChildren(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public IReadOnlyList<string> Names` | implemented | `SKRuntimeEffectChildren.Names` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public int Count` | implemented | `SKRuntimeEffectChildren.Count` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Reset ()` | implemented | `SKRuntimeEffectChildren.Reset` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public bool Contains (string name)` | implemented | `SKRuntimeEffectChildren.Contains` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectChild? this[string name]` | implemented | `SKRuntimeEffectChildren.this` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Add (string name, SKRuntimeEffectChild? value)` | implemented | `SKRuntimeEffectChildren.Add` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKObject[] ToArray ()` | implemented | `SKRuntimeEffectChildren.ToArray` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public IEnumerator<string> GetEnumerator ()` | implemented | `SKRuntimeEffectChildren.GetEnumerator` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Dispose ()` | implemented | `SKRuntimeEffectChildren.Dispose` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeEffectUniform

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKRuntimeEffectUniform Empty` | implemented | `SKRuntimeEffectUniform.Empty` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public bool IsEmpty` | implemented | `SKRuntimeEffectUniform.IsEmpty` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public int Size` | implemented | `SKRuntimeEffectUniform.Size` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public static implicit operator SKRuntimeEffectUniform (float value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (float[] value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (Span<float> value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (ReadOnlySpan<float> value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKPoint value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKSize value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKPoint3 value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (int value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (int[] value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (Span<int> value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (ReadOnlySpan<int> value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKPointI value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKSizeI value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKColor value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKColorF value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (float[][] value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectUniform (SKMatrix value)` | not-applicable | `new SKRuntimeEffectUniform(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public void WriteTo (Span<byte> data)` | implemented | `SKRuntimeEffectUniform.WriteTo` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeEffectChild

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeEffectChild (SKShader shader)` | implemented | `SKRuntimeEffectChild(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectChild (SKColorFilter colorFilter)` | implemented | `SKRuntimeEffectChild(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectChild (SKBlender blender)` | implemented | `SKRuntimeEffectChild(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKObject Value` | implemented | `SKRuntimeEffectChild.Value` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKShader Shader` | implemented | `SKRuntimeEffectChild.Shader` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKColorFilter ColorFilter` | implemented | `SKRuntimeEffectChild.ColorFilter` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKBlender Blender` | implemented | `SKRuntimeEffectChild.Blender` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public static implicit operator SKRuntimeEffectChild (SKShader shader)` | not-applicable | `new SKRuntimeEffectChild(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectChild (SKColorFilter colorFilter)` | not-applicable | `new SKRuntimeEffectChild(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKRuntimeEffectChild (SKBlender blender)` | not-applicable | `new SKRuntimeEffectChild(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.SKRuntimeEffectBuilderException

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeEffectBuilderException (string message) : base (message)` | implemented | `SKRuntimeEffectBuilderException(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeEffectBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeEffectBuilder (SKRuntimeEffect effect)` | implemented | `SKRuntimeEffectBuilder(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffect Effect` | implemented | `SKRuntimeEffectBuilder.Effect` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectUniforms Uniforms` | implemented | `SKRuntimeEffectBuilder.Uniforms` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKRuntimeEffectChildren Children` | implemented | `SKRuntimeEffectBuilder.Children` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public void Dispose ()` | implemented | `SKRuntimeEffectBuilder.Dispose` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeShaderBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeShaderBuilder (SKRuntimeEffect effect) : base (effect)` | implemented | `SKRuntimeShaderBuilder(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKShader Build ()` | implemented | `SKRuntimeShaderBuilder.Build` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKShader Build (SKMatrix localMatrix)` | implemented | `SKRuntimeShaderBuilder.Build` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeColorFilterBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeColorFilterBuilder (SKRuntimeEffect effect) : base (effect)` | implemented | `SKRuntimeColorFilterBuilder(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKColorFilter Build ()` | implemented | `SKRuntimeColorFilterBuilder.Build` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKRuntimeBlenderBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRuntimeBlenderBuilder (SKRuntimeEffect effect) : base (effect)` | implemented | `SKRuntimeBlenderBuilder(...)` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |
| `public SKBlender Build ()` | implemented | `SKRuntimeBlenderBuilder.Build` — Concrete typed uniform/child/value/builder implementation with validated names/types, source declaration child ordering, exact integer byte buffers, mutable indexers, enumeration, reset and native retained ownership. C# indexers map to Set/Get or named JavaScript properties. |

### SkiaSharp.SKSvgCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKCanvas Create (SKRect bounds, Stream stream)` | partial | `SKSvgCanvas.Create` — Native vector SVG canvas and both output stream forms operate; the current native build converts text to outline paths rather than retaining the upstream default SVG text nodes. |
| `public static SKCanvas Create (SKRect bounds, SKWStream stream)` | partial | `SKSvgCanvas.Create` — Native vector SVG canvas and both output stream forms operate; the current native build converts text to outline paths rather than retaining the upstream default SVG text nodes. |

### SkiaSharp.SKShader

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKShader WithColorFilter (SKColorFilter filter)` | implemented | `SKShader.WithColorFilter` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |
| `public SKShader WithLocalMatrix (SKMatrix localMatrix)` | implemented | `SKShader.WithLocalMatrix` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |
| `public static SKShader CreateEmpty ()` | implemented | `SKShader.CreateEmpty` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateColor (SKColor color)` | implemented | `SKShader.CreateColor` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateColor (SKColorF color, SKColorSpace colorspace)` | implemented | `SKShader.CreateColor` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateBitmap (SKBitmap src)` | implemented | `SKShader.CreateBitmap` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateBitmap (SKBitmap src, SKShaderTileMode tmx, SKShaderTileMode tmy)` | implemented | `SKShader.CreateBitmap` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateBitmap (SKBitmap src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKMatrix localMatrix)` | implemented | `SKShader.CreateBitmap` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKSamplingOptions sampling)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterQuality quality)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKMatrix localMatrix)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKSamplingOptions sampling, SKMatrix localMatrix)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateImage (SKImage src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterQuality quality, SKMatrix localMatrix)` | implemented | `SKShader.CreateImage` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKRect tile)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode, SKRect tile)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKMatrix localMatrix, SKRect tile)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePicture (SKPicture src, SKShaderTileMode tmx, SKShaderTileMode tmy, SKFilterMode filterMode, SKMatrix localMatrix, SKRect tile)` | implemented | `SKShader.CreatePicture` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColor[] colors, SKShaderTileMode mode)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColor[] colors, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColor[] colors, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColorF[] colors, SKColorSpace colorspace, SKShaderTileMode mode)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateLinearGradient (SKPoint start, SKPoint end, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateLinearGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColor[] colors, SKShaderTileMode mode)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColor[] colors, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColor[] colors, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColorF[] colors, SKColorSpace colorspace, SKShaderTileMode mode)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateRadialGradient (SKPoint center, float radius, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateRadialGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors, float[] colorPos)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors, float[] colorPos, SKMatrix localMatrix)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors, SKShaderTileMode tileMode, float startAngle, float endAngle)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors, float[] colorPos, SKShaderTileMode tileMode, float startAngle, float endAngle)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColor[] colors, float[] colorPos, SKShaderTileMode tileMode, float startAngle, float endAngle, SKMatrix localMatrix)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKMatrix localMatrix)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace, SKShaderTileMode tileMode, float startAngle, float endAngle)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode tileMode, float startAngle, float endAngle)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateSweepGradient (SKPoint center, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode tileMode, float startAngle, float endAngle, SKMatrix localMatrix)` | implemented | `SKShader.CreateSweepGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColor[] colors, SKShaderTileMode mode)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColor[] colors, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColor[] colors, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColorF[] colors, SKColorSpace colorspace, SKShaderTileMode mode)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateTwoPointConicalGradient (SKPoint start, float startRadius, SKPoint end, float endRadius, SKColorF[] colors, SKColorSpace colorspace, float[] colorPos, SKShaderTileMode mode, SKMatrix localMatrix)` | implemented | `SKShader.CreateTwoPointConicalGradient` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseFractalNoise (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed)` | implemented | `SKShader.CreatePerlinNoiseFractalNoise` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseFractalNoise (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed, SKPointI tileSize)` | implemented | `SKShader.CreatePerlinNoiseFractalNoise` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseFractalNoise (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed, SKSizeI tileSize)` | implemented | `SKShader.CreatePerlinNoiseFractalNoise` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseTurbulence (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed)` | implemented | `SKShader.CreatePerlinNoiseTurbulence` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseTurbulence (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed, SKPointI tileSize)` | implemented | `SKShader.CreatePerlinNoiseTurbulence` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreatePerlinNoiseTurbulence (float baseFrequencyX, float baseFrequencyY, int numOctaves, float seed, SKSizeI tileSize)` | implemented | `SKShader.CreatePerlinNoiseTurbulence` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateCompose (SKShader shaderA, SKShader shaderB)` | implemented | `SKShader.CreateCompose` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateCompose (SKShader shaderA, SKShader shaderB, SKBlendMode mode)` | implemented | `SKShader.CreateCompose` — Gradient overloads consume colorspace/positions/tile/matrix/angle separately; native wide-gamut and local-matrix pixels, bitmap/image/picture shader forms and lifecycle are verified. |
| `public static SKShader CreateBlend (SKBlendMode mode, SKShader shaderA, SKShader shaderB)` | implemented | `SKShader.CreateBlend` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |
| `public static SKShader CreateBlend (SKBlender blender, SKShader shaderA, SKShader shaderB)` | implemented | `SKShader.CreateBlend` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |
| `public static SKShader CreateColorFilter (SKShader shader, SKColorFilter filter)` | implemented | `SKShader.CreateColorFilter` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |
| `public static SKShader CreateLocalMatrix (SKShader shader, SKMatrix localMatrix)` | implemented | `SKShader.CreateLocalMatrix` — Compiled native SkShader local-matrix/color-filter/custom-blender binding preserves native child ownership; stock CanvasKit supports local-matrix shaders but needs native support for remaining variants. |

### SkiaSharp.SKStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool IsAtEnd` | partial | `SKStream.IsAtEnd` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SByte ReadSByte ()` | implemented | `SKStream.ReadSByte` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Int16 ReadInt16 ()` | implemented | `SKStream.ReadInt16` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Int32 ReadInt32 ()` | implemented | `SKStream.ReadInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public Byte ReadByte ()` | implemented | `SKStream.ReadByte` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public UInt16 ReadUInt16 ()` | implemented | `SKStream.ReadUInt16` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public UInt32 ReadUInt32 ()` | implemented | `SKStream.ReadUInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadBool ()` | implemented | `SKStream.ReadBool` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadSByte (out SByte buffer)` | implemented | `SKStream.ReadSByte` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadInt16 (out Int16 buffer)` | implemented | `SKStream.ReadInt16` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadInt32 (out Int32 buffer)` | implemented | `SKStream.ReadInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadByte (out Byte buffer)` | implemented | `SKStream.ReadByte` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadUInt16 (out UInt16 buffer)` | implemented | `SKStream.ReadUInt16` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadUInt32 (out UInt32 buffer)` | implemented | `SKStream.ReadUInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool ReadBool (out Boolean buffer)` | implemented | `SKStream.ReadBool` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public int Read (byte[] buffer, int size)` | partial | `SKStream.Read` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Read (IntPtr buffer, int size)` | not-applicable | `SKStream.Read` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int Peek (IntPtr buffer, int size)` | not-applicable | `SKStream.Peek` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int Skip (int size)` | implemented | `SKStream.Skip` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool Rewind ()` | partial | `SKStream.Rewind` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Seek (int position)` | partial | `SKStream.Seek` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Move (long offset)` | partial | `SKStream.Move` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Move (int offset)` | partial | `SKStream.Move` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public IntPtr GetMemoryBase ()` | not-applicable | `SKStream.GetMemoryBase` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKData GetData ()` | implemented | `SKStream.GetData` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool HasPosition` | partial | `SKStream.HasPosition` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Position` | partial | `SKStream.Position` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool HasLength` | partial | `SKStream.HasLength` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int Length` | partial | `SKStream.Length` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFileStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFileStream (string path) : base (CreateNew (path), true)` | partial | `SKFileStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsValid` | partial | `SKFileStream.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool IsPathSupported (string path)` | partial | `SKFileStream.IsPathSupported` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKStreamAsset OpenStream (string path)` | partial | `SKFileStream.OpenStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKMemoryStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKMemoryStream () : this (SkiaApi.sk_memorystream_new (), true)` | partial | `SKMemoryStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKMemoryStream (ulong length) : this(SkiaApi.sk_memorystream_new_with_length ((IntPtr)length), true)` | not-applicable | `SKMemoryStream(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKMemoryStream (SKData data) : this(SkiaApi.sk_memorystream_new_with_skdata (data.Handle), true)` | partial | `SKMemoryStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKMemoryStream (byte[] data) : this ()` | partial | `SKMemoryStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void SetMemory (byte[] data)` | partial | `SKMemoryStream.SetMemory` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKWStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public virtual int BytesWritten` | partial | `SKWStream.BytesWritten` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public virtual bool Write (byte[] buffer, int size)` | implemented | `SKWStream.Write` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool NewLine ()` | partial | `SKWStream.NewLine` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public virtual void Flush ()` | partial | `SKWStream.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool Write8 (Byte value)` | implemented | `SKWStream.Write8` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool Write16 (UInt16 value)` | implemented | `SKWStream.Write16` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool Write32 (UInt32 value)` | implemented | `SKWStream.Write32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteText (string value)` | implemented | `SKWStream.WriteText` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteDecimalAsTest (Int32 value)` | implemented | `SKWStream.WriteDecimalAsTest` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteBigDecimalAsText (Int64 value, int digits)` | implemented | `SKWStream.WriteBigDecimalAsText` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteHexAsText (UInt32 value, int digits)` | implemented | `SKWStream.WriteHexAsText` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteScalarAsText (float value)` | implemented | `SKWStream.WriteScalarAsText` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteBool (bool value)` | implemented | `SKWStream.WriteBool` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteScalar (float value)` | implemented | `SKWStream.WriteScalar` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WritePackedUInt32 (UInt32 value)` | implemented | `SKWStream.WritePackedUInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool WriteStream (SKStream input, int length)` | implemented | `SKWStream.WriteStream` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static int GetSizeOfPackedUInt32 (UInt32 value)` | implemented | `SKWStream.GetSizeOfPackedUInt32` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKFileWStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFileWStream (string path) : base (CreateNew (path), true)` | partial | `SKFileWStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsValid` | partial | `SKFileWStream.IsValid` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool IsPathSupported (string path)` | partial | `SKFileWStream.IsPathSupported` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKWStream OpenStream (string path)` | partial | `SKFileWStream.OpenStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKDynamicMemoryWStream

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKDynamicMemoryWStream () : base (SkiaApi.sk_dynamicmemorywstream_new (), true)` | partial | `SKDynamicMemoryWStream(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData CopyToData ()` | partial | `SKDynamicMemoryWStream.CopyToData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKStreamAsset DetachAsStream ()` | partial | `SKDynamicMemoryWStream.DetachAsStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKData DetachAsData ()` | partial | `SKDynamicMemoryWStream.DetachAsData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void CopyTo (IntPtr data)` | not-applicable | `SKDynamicMemoryWStream.CopyTo` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void CopyTo (Span<byte> data)` | implemented | `SKDynamicMemoryWStream.CopyTo` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool CopyTo (SKWStream dst)` | implemented | `SKDynamicMemoryWStream.CopyTo` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public bool CopyTo (Stream dst)` | implemented | `SKDynamicMemoryWStream.CopyTo` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKSurface

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKSurface Create (SKImageInfo info)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKImageInfo info, int rowBytes)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKImageInfo info, SKSurfaceProperties props)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKImageInfo info, int rowBytes, SKSurfaceProperties props)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKPixmap pixmap)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKPixmap pixmap, SKSurfaceProperties props)` | implemented | `SKSurface.Create` — Native raster surface creation covers real color/alpha/color-space/props, row bytes and pixmap overloads; F16/F32/extended color types, stride and lifetime tests pass. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels, int rowBytes)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels, int rowBytes, SKSurfaceReleaseDelegate releaseProc, object context)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels, SKSurfaceProperties props)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels, int rowBytes, SKSurfaceProperties props)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (SKImageInfo info, IntPtr pixels, int rowBytes, SKSurfaceReleaseDelegate releaseProc, object context, SKSurfaceProperties props)` | not-applicable | `SKSurface.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKColorSpace colorspace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKColorSpace colorspace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, GRBackendTexture texture, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info, int sampleCount)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info, int sampleCount, GRSurfaceOrigin origin)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info, int sampleCount, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, bool budgeted, SKImageInfo info, int sampleCount, GRSurfaceOrigin origin, SKSurfaceProperties props, bool shouldCreateWithMips)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info, int sampleCount)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info, int sampleCount, GRSurfaceOrigin origin)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info, int sampleCount, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, bool budgeted, SKImageInfo info, int sampleCount, GRSurfaceOrigin origin, SKSurfaceProperties props, bool shouldCreateWithMips)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKImageInfo info)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKImageInfo info, bool mipmapped)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKImageInfo info, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKImageInfo info, bool mipmapped, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKColorSpace colorSpace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKColorSpace colorSpace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (SKGraphiteRecorder recorder, SKGraphiteBackendTexture backendTexture, SKColorType colorType, SKColorSpace colorSpace, SKSurfaceProperties props, SKGraphiteReleaseDelegate releaseProc)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, CoreAnimation.CAMetalLayer layer, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props, out CoreAnimation.ICAMetalDrawable drawable)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, MetalKit.MTKView view, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, MetalKit.MTKView view, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface Create (GRRecordingContext context, MetalKit.MTKView view, GRSurfaceOrigin origin, int sampleCount, SKColorType colorType, SKColorSpace colorspace, SKSurfaceProperties props)` | partial | `SKSurface.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKSurface CreateNull (int width, int height)` | implemented | `SKSurface.CreateNull` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public SKCanvas Canvas` | partial | `SKSurface.Canvas` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKSurfaceProperties SurfaceProperties` | implemented | `SKSurface.SurfaceProperties` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public GRRecordingContext Context` | partial | `SKSurface.Context` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage Snapshot ()` | partial | `SKSurface.Snapshot` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKImage Snapshot (SKRectI bounds)` | partial | `SKSurface.Snapshot` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Draw (SKCanvas canvas, float x, float y, SKPaint paint)` | partial | `SKSurface.Draw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Draw (SKCanvas canvas, SKPoint p, SKSamplingOptions sampling, SKPaint paint = null)` | partial | `SKSurface.Draw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Draw (SKCanvas canvas, float x, float y, SKSamplingOptions sampling, SKPaint paint = null)` | partial | `SKSurface.Draw` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKPixmap PeekPixels ()` | implemented | `SKSurface.PeekPixels` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public bool PeekPixels (SKPixmap pixmap)` | implemented | `SKSurface.PeekPixels` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public bool ReadPixels (SKImageInfo dstInfo, IntPtr dstPixels, int dstRowBytes, int srcX, int srcY)` | not-applicable | `SKSurface.ReadPixels` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public void RequestReadPixels (SKImageInfo info, SKRectI srcRect, Action<SKImageReadPixelsResult> callback)` | implemented | `SKSurface.RequestReadPixels` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public void RequestReadPixels (SKImageInfo info, SKRectI srcRect, SKImageRescaleGamma rescaleGamma, SKImageRescaleMode rescaleMode, Action<SKImageReadPixelsResult> callback)` | implemented | `SKSurface.RequestReadPixels` — Compiled Skia null-surface and picture-count hooks plus ownership-preserving drawable/readback adapters. Native null pixels, operation count, recording and async image lifetime tests pass. |
| `public void Flush ()` | partial | `SKSurface.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void Flush (bool submit, bool synchronous = false)` | partial | `SKSurface.Flush` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKSurfaceProperties

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKSurfaceProperties (SKPixelGeometry pixelGeometry) : this ((uint)0, pixelGeometry)` | implemented | `SKSurfaceProperties(...)` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public SKSurfaceProperties (uint flags, SKPixelGeometry pixelGeometry) : this (SkiaApi.sk_surfaceprops_new (flags, pixelGeometry), true)` | implemented | `SKSurfaceProperties(...)` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public SKSurfaceProperties (SKSurfacePropsFlags flags, SKPixelGeometry pixelGeometry) : this (SkiaApi.sk_surfaceprops_new ((uint)flags, pixelGeometry), true)` | implemented | `SKSurfaceProperties(...)` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public SKSurfacePropsFlags Flags` | implemented | `SKSurfaceProperties.Flags` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public SKPixelGeometry PixelGeometry` | implemented | `SKSurfaceProperties.PixelGeometry` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |
| `public bool IsUseDeviceIndependentFonts` | implemented | `SKSurfaceProperties.IsUseDeviceIndependentFonts` — Actual native raster surface properties and borrowed pixel descriptors; all extended color types preserve strides and ownership in surface-formats tests. |

### SkiaSharp.SKSwizzle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void SwapRedBlue (IntPtr pixels, int count)` | not-applicable | `SKSwizzle.SwapRedBlue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static void SwapRedBlue (IntPtr dest, IntPtr src, int count)` | not-applicable | `SKSwizzle.SwapRedBlue` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static void SwapRedBlue (Span<byte> pixels)` | implemented | `SKSwizzle.SwapRedBlue` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static void SwapRedBlue (ReadOnlySpan<byte> pixels, int count)` | implemented | `SKSwizzle.SwapRedBlue` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static void SwapRedBlue (ReadOnlySpan<byte> dest, ReadOnlySpan<byte> src, int count)` | implemented | `SKSwizzle.SwapRedBlue` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |

### SkiaSharp.SKTextBlob

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKRect Bounds` | partial | `SKTextBlob.Bounds` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public uint UniqueId` | partial | `SKTextBlob.UniqueId` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? Create (string text, SKFont font, SKPoint origin = default)` | partial | `SKTextBlob.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? Create (ReadOnlySpan<char> text, SKFont font, SKPoint origin = default)` | partial | `SKTextBlob.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? Create (IntPtr text, int length, SKTextEncoding encoding, SKFont font, SKPoint origin = default)` | not-applicable | `SKTextBlob.Create` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKTextBlob? Create (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKFont font, SKPoint origin = default)` | partial | `SKTextBlob.Create` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateHorizontal (string text, SKFont font, ReadOnlySpan<float> positions, float y)` | partial | `SKTextBlob.CreateHorizontal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateHorizontal (ReadOnlySpan<char> text, SKFont font, ReadOnlySpan<float> positions, float y)` | partial | `SKTextBlob.CreateHorizontal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateHorizontal (IntPtr text, int length, SKTextEncoding encoding, SKFont font, ReadOnlySpan<float> positions, float y)` | not-applicable | `SKTextBlob.CreateHorizontal` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKTextBlob? CreateHorizontal (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKFont font, ReadOnlySpan<float> positions, float y)` | partial | `SKTextBlob.CreateHorizontal` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreatePositioned (string text, SKFont font, ReadOnlySpan<SKPoint> positions)` | partial | `SKTextBlob.CreatePositioned` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreatePositioned (ReadOnlySpan<char> text, SKFont font, ReadOnlySpan<SKPoint> positions)` | partial | `SKTextBlob.CreatePositioned` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreatePositioned (IntPtr text, int length, SKTextEncoding encoding, SKFont font, ReadOnlySpan<SKPoint> positions)` | not-applicable | `SKTextBlob.CreatePositioned` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKTextBlob? CreatePositioned (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKFont font, ReadOnlySpan<SKPoint> positions)` | partial | `SKTextBlob.CreatePositioned` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateRotationScale (string text, SKFont font, ReadOnlySpan<SKRotationScaleMatrix> positions)` | partial | `SKTextBlob.CreateRotationScale` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateRotationScale (ReadOnlySpan<char> text, SKFont font, ReadOnlySpan<SKRotationScaleMatrix> positions)` | partial | `SKTextBlob.CreateRotationScale` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreateRotationScale (IntPtr text, int length, SKTextEncoding encoding, SKFont font, ReadOnlySpan<SKRotationScaleMatrix> positions)` | not-applicable | `SKTextBlob.CreateRotationScale` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKTextBlob? CreateRotationScale (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKFont font, ReadOnlySpan<SKRotationScaleMatrix> positions)` | partial | `SKTextBlob.CreateRotationScale` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTextBlob? CreatePathPositioned (string text, SKFont font, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | implemented | `SKTextBlob.CreatePathPositioned` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKTextBlob? CreatePathPositioned (ReadOnlySpan<char> text, SKFont font, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | implemented | `SKTextBlob.CreatePathPositioned` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public static SKTextBlob? CreatePathPositioned (IntPtr text, int length, SKTextEncoding encoding, SKFont font, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | not-applicable | `SKTextBlob.CreatePathPositioned` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static SKTextBlob? CreatePathPositioned (ReadOnlySpan<byte> text, SKTextEncoding encoding, SKFont font, SKPath path, SKTextAlign textAlign = SKTextAlign.Left, SKPoint origin = default)` | implemented | `SKTextBlob.CreatePathPositioned` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public float[] GetIntercepts (float upperBounds, float lowerBounds, SKPaint? paint = null)` | implemented | `SKTextBlob.GetIntercepts` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public void GetIntercepts (float upperBounds, float lowerBounds, Span<float> intervals, SKPaint? paint = null)` | implemented | `SKTextBlob.GetIntercepts` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public int CountIntercepts (float upperBounds, float lowerBounds, SKPaint? paint = null)` | implemented | `SKTextBlob.CountIntercepts` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKTextBlobBuilder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKTextBlobBuilder () : this (SkiaApi.sk_textblob_builder_new (), true)` | partial | `SKTextBlobBuilder(...)` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTextBlob? Build ()` | partial | `SKTextBlobBuilder.Build` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddRun (ReadOnlySpan<ushort> glyphs, SKFont font, SKPoint origin = default)` | partial | `SKTextBlobBuilder.AddRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddHorizontalRun (ReadOnlySpan<ushort> glyphs, SKFont font, ReadOnlySpan<float> positions, float y)` | partial | `SKTextBlobBuilder.AddHorizontalRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddPositionedRun (ReadOnlySpan<ushort> glyphs, SKFont font, ReadOnlySpan<SKPoint> positions)` | partial | `SKTextBlobBuilder.AddPositionedRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddRotationScaleRun (ReadOnlySpan<ushort> glyphs, SKFont font, ReadOnlySpan<SKRotationScaleMatrix> positions)` | partial | `SKTextBlobBuilder.AddRotationScaleRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void AddPathPositionedRun (ReadOnlySpan<ushort> glyphs, SKFont font, ReadOnlySpan<float> glyphWidths, ReadOnlySpan<SKPoint> glyphOffsets, SKPath path, SKTextAlign textAlign = SKTextAlign.Left)` | implemented | `SKTextBlobBuilder.AddPathPositionedRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRunBuffer AllocateRun (SKFont font, int count, float x, float y, SKRect? bounds = null)` | partial | `SKTextBlobBuilder.AllocateRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRawRunBuffer<float> AllocateRawRun (SKFont font, int count, float x, float y, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKTextRunBuffer AllocateTextRun (SKFont font, int count, float x, float y, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRawRunBuffer<float> AllocateRawTextRun (SKFont font, int count, float x, float y, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKHorizontalRunBuffer AllocateHorizontalRun (SKFont font, int count, float y, SKRect? bounds = null)` | partial | `SKTextBlobBuilder.AllocateHorizontalRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRawRunBuffer<float> AllocateRawHorizontalRun (SKFont font, int count, float y, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawHorizontalRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKHorizontalTextRunBuffer AllocateHorizontalTextRun (SKFont font, int count, float y, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateHorizontalTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRawRunBuffer<float> AllocateRawHorizontalTextRun (SKFont font, int count, float y, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawHorizontalTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKPositionedRunBuffer AllocatePositionedRun (SKFont font, int count, SKRect? bounds = null)` | partial | `SKTextBlobBuilder.AllocatePositionedRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRawRunBuffer<SKPoint> AllocateRawPositionedRun (SKFont font, int count, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawPositionedRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKPositionedTextRunBuffer AllocatePositionedTextRun (SKFont font, int count, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocatePositionedTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRawRunBuffer<SKPoint> AllocateRawPositionedTextRun (SKFont font, int count, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawPositionedTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRotationScaleRunBuffer AllocateRotationScaleRun (SKFont font, int count, SKRect? bounds = null)` | partial | `SKTextBlobBuilder.AllocateRotationScaleRun` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKRawRunBuffer<SKRotationScaleMatrix> AllocateRawRotationScaleRun (SKFont font, int count, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawRotationScaleRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRotationScaleTextRunBuffer AllocateRotationScaleTextRun (SKFont font, int count, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRotationScaleTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |
| `public SKRawRunBuffer<SKRotationScaleMatrix> AllocateRawRotationScaleTextRun (SKFont font, int count, int textByteCount, SKRect? bounds = null)` | implemented | `SKTextBlobBuilder.AllocateRawRotationScaleTextRun` — Concrete native codec/image/text-blob or managed stream/span overload; native readback gamma, ownership, destination bounds and encoding tests pass in assets.test.mjs. |

### SkiaSharp.SKTypeface

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKTypeface Default` | partial | `SKTypeface.Default` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface Empty` | implemented | `SKTypeface.Empty` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |
| `public bool IsEmpty` | implemented | `SKTypeface.IsEmpty` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |
| `public static SKTypeface CreateDefault ()` | implemented | `SKTypeface.CreateDefault` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |
| `public static SKTypeface FromFamilyName (string familyName, int weight, int width, SKFontStyleSlant slant)` | partial | `SKTypeface.FromFamilyName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromFamilyName (string familyName)` | partial | `SKTypeface.FromFamilyName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromFamilyName (string familyName, SKFontStyle style)` | partial | `SKTypeface.FromFamilyName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromFamilyName (string familyName, SKFontStyleWeight weight, SKFontStyleWidth width, SKFontStyleSlant slant)` | partial | `SKTypeface.FromFamilyName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromFile (string path, int index = 0)` | partial | `SKTypeface.FromFile` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromStream (Stream stream, int index = 0)` | partial | `SKTypeface.FromStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromStream (SKStreamAsset stream, int index = 0)` | partial | `SKTypeface.FromStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKTypeface FromData (SKData data, int index = 0)` | partial | `SKTypeface.FromData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public string FamilyName` | partial | `SKTypeface.FamilyName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFontStyle FontStyle` | partial | `SKTypeface.FontStyle` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int FontWeight` | partial | `SKTypeface.FontWeight` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int FontWidth` | partial | `SKTypeface.FontWidth` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFontStyleSlant FontSlant` | partial | `SKTypeface.FontSlant` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsBold` | partial | `SKTypeface.IsBold` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsItalic` | partial | `SKTypeface.IsItalic` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool IsFixedPitch` | partial | `SKTypeface.IsFixedPitch` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int UnitsPerEm` | partial | `SKTypeface.UnitsPerEm` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GlyphCount` | partial | `SKTypeface.GlyphCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public string PostScriptName` | partial | `SKTypeface.PostScriptName` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int TableCount` | partial | `SKTypeface.TableCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public UInt32[] GetTableTags ()` | partial | `SKTypeface.GetTableTags` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryGetTableTags (out UInt32[] tags)` | implemented | `SKTypeface.TryGetTableTags` — Managed out array is populated from validated SFNT table directory with explicit success result. |
| `public int GetTableSize (UInt32 tag)` | partial | `SKTypeface.GetTableSize` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public byte[] GetTableData (UInt32 tag)` | partial | `SKTypeface.GetTableData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryGetTableData (UInt32 tag, out byte[] tableData)` | partial | `SKTypeface.TryGetTableData` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool TryGetTableData (UInt32 tag, int offset, int length, IntPtr tableData)` | not-applicable | `SKTypeface.TryGetTableData` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public int CountGlyphs (string str)` | partial | `SKTypeface.CountGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int CountGlyphs (ReadOnlySpan<char> str)` | partial | `SKTypeface.CountGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int CountGlyphs (byte[] str, SKTextEncoding encoding)` | partial | `SKTypeface.CountGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int CountGlyphs (ReadOnlySpan<byte> str, SKTextEncoding encoding)` | partial | `SKTypeface.CountGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int CountGlyphs (IntPtr str, int strLen, SKTextEncoding encoding)` | not-applicable | `SKTypeface.CountGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public ushort GetGlyph (int codepoint)` | partial | `SKTypeface.GetGlyph` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ushort[] GetGlyphs (ReadOnlySpan<int> codepoints)` | partial | `SKTypeface.GetGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ushort[] GetGlyphs (string text)` | partial | `SKTypeface.GetGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ushort[] GetGlyphs (ReadOnlySpan<char> text)` | partial | `SKTypeface.GetGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ushort[] GetGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding)` | partial | `SKTypeface.GetGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public ushort[] GetGlyphs (IntPtr text, int length, SKTextEncoding encoding)` | not-applicable | `SKTypeface.GetGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public bool ContainsGlyph (int codepoint)` | partial | `SKTypeface.ContainsGlyph` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ContainsGlyphs (ReadOnlySpan<int> codepoints)` | partial | `SKTypeface.ContainsGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ContainsGlyphs (string text)` | partial | `SKTypeface.ContainsGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ContainsGlyphs (ReadOnlySpan<char> text)` | partial | `SKTypeface.ContainsGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ContainsGlyphs (ReadOnlySpan<byte> text, SKTextEncoding encoding)` | partial | `SKTypeface.ContainsGlyphs` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool ContainsGlyphs (IntPtr text, int length, SKTextEncoding encoding)` | not-applicable | `SKTypeface.ContainsGlyphs` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public SKFont ToFont ()` | partial | `SKTypeface.ToFont` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFont ToFont (float size, float scaleX = SKFont.DefaultScaleX, float skewX = SKFont.DefaultSkewX)` | partial | `SKTypeface.ToFont` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKStreamAsset OpenStream ()` | partial | `SKTypeface.OpenStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKStreamAsset OpenStream (out int ttcIndex)` | partial | `SKTypeface.OpenStream` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool HasGetKerningPairAdjustments` | partial | `SKTypeface.HasGetKerningPairAdjustments` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public int[] GetKerningPairAdjustments (ReadOnlySpan<ushort> glyphs)` | partial | `SKTypeface.GetKerningPairAdjustments` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool GetKerningPairAdjustments (ReadOnlySpan<ushort> glyphs, Span<int> adjustments)` | partial | `SKTypeface.GetKerningPairAdjustments` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int VariationDesignParameterCount` | partial | `SKTypeface.VariationDesignParameterCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFontVariationAxis[] VariationDesignParameters` | partial | `SKTypeface.VariationDesignParameters` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GetVariationDesignParameters (Span<SKFontVariationAxis> axes)` | partial | `SKTypeface.GetVariationDesignParameters` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int VariationDesignPositionCount` | partial | `SKTypeface.VariationDesignPositionCount` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKFontVariationPositionCoordinate[] VariationDesignPosition` | partial | `SKTypeface.VariationDesignPosition` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public int GetVariationDesignPosition (Span<SKFontVariationPositionCoordinate> coordinates)` | partial | `SKTypeface.GetVariationDesignPosition` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public SKTypeface Clone (ReadOnlySpan<SKFontVariationPositionCoordinate> position)` | implemented | `SKTypeface.Clone` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |
| `public SKTypeface Clone (int paletteIndex)` | implemented | `SKTypeface.Clone` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |
| `public SKTypeface Clone (SKFontArguments args)` | implemented | `SKTypeface.Clone` — Font bytes, native variable-font instancing and parameter/position array/destination overloads are explicitly dispatched and checked against native font fixtures. |

### SkiaSharp.SKVertices

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKVertices CreateCopy (SKVertexMode vmode, SKPoint[] positions, SKColor[] colors)` | partial | `SKVertices.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKVertices CreateCopy (SKVertexMode vmode, SKPoint[] positions, SKPoint[] texs, SKColor[] colors)` | partial | `SKVertices.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static SKVertices CreateCopy (SKVertexMode vmode, SKPoint[] positions, SKPoint[] texs, SKColor[] colors, UInt16[] indices)` | partial | `SKVertices.CreateCopy` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKWebpEncoder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static bool Encode (SKWStream dst, SKPixmap src, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.Encode` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |
| `public static bool Encode (Stream dst, SKPixmap src, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.Encode` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |
| `public static SKData? Encode (SKPixmap src, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.Encode` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |
| `public static bool EncodeAnimated (SKWStream dst, ReadOnlySpan<SKWebpEncoderFrame> frames, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.EncodeAnimated` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |
| `public static bool EncodeAnimated (Stream dst, ReadOnlySpan<SKWebpEncoderFrame> frames, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.EncodeAnimated` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |
| `public static SKData? EncodeAnimated (ReadOnlySpan<SKWebpEncoderFrame> frames, SKWebpEncoderOptions options)` | implemented | `SKWebpEncoder.EncodeAnimated` — Compiled native SkWebpEncoder supports bitmap/pixmap/image inputs and stream/data return forms; animated encoding decodes into verified frames. |

### SkiaSharp.GRVkAlloc

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt64 Memory` | missing | `GRVkAlloc.Memory` — No exported browser type with this declaration identity. |
| `public UInt64 Offset` | missing | `GRVkAlloc.Offset` — No exported browser type with this declaration identity. |
| `public UInt64 Size` | missing | `GRVkAlloc.Size` — No exported browser type with this declaration identity. |
| `public UInt32 Flags` | missing | `GRVkAlloc.Flags` — No exported browser type with this declaration identity. |
| `public IntPtr BackendMemory` | not-applicable | `GRVkAlloc.BackendMemory` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public readonly bool Equals (GRVkAlloc obj)` | missing | `GRVkAlloc.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `GRVkAlloc.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (GRVkAlloc left, GRVkAlloc right)` | not-applicable | `GRVkAlloc.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRVkAlloc left, GRVkAlloc right)` | not-applicable | `GRVkAlloc.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `GRVkAlloc.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkImageInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt64 Image` | missing | `GRVkImageInfo.Image` — No exported browser type with this declaration identity. |
| `public GRVkAlloc Alloc` | missing | `GRVkImageInfo.Alloc` — No exported browser type with this declaration identity. |
| `public UInt32 ImageTiling` | missing | `GRVkImageInfo.ImageTiling` — No exported browser type with this declaration identity. |
| `public UInt32 ImageLayout` | missing | `GRVkImageInfo.ImageLayout` — No exported browser type with this declaration identity. |
| `public UInt32 Format` | missing | `GRVkImageInfo.Format` — No exported browser type with this declaration identity. |
| `public UInt32 ImageUsageFlags` | missing | `GRVkImageInfo.ImageUsageFlags` — No exported browser type with this declaration identity. |
| `public UInt32 SampleCount` | missing | `GRVkImageInfo.SampleCount` — No exported browser type with this declaration identity. |
| `public UInt32 LevelCount` | missing | `GRVkImageInfo.LevelCount` — No exported browser type with this declaration identity. |
| `public UInt32 CurrentQueueFamily` | missing | `GRVkImageInfo.CurrentQueueFamily` — No exported browser type with this declaration identity. |
| `public bool Protected` | missing | `GRVkImageInfo.Protected` — No exported browser type with this declaration identity. |
| `public GRVkYcbcrConversionInfo YcbcrConversionInfo` | missing | `GRVkImageInfo.YcbcrConversionInfo` — No exported browser type with this declaration identity. |
| `public UInt32 SharingMode` | missing | `GRVkImageInfo.SharingMode` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (GRVkImageInfo obj)` | missing | `GRVkImageInfo.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `GRVkImageInfo.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (GRVkImageInfo left, GRVkImageInfo right)` | not-applicable | `GRVkImageInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRVkImageInfo left, GRVkImageInfo right)` | not-applicable | `GRVkImageInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `GRVkImageInfo.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkYcbcrComponents

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt32 R` | missing | `GRVkYcbcrComponents.R` — No exported browser type with this declaration identity. |
| `public UInt32 G` | missing | `GRVkYcbcrComponents.G` — No exported browser type with this declaration identity. |
| `public UInt32 B` | missing | `GRVkYcbcrComponents.B` — No exported browser type with this declaration identity. |
| `public UInt32 A` | missing | `GRVkYcbcrComponents.A` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (GRVkYcbcrComponents obj)` | missing | `GRVkYcbcrComponents.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `GRVkYcbcrComponents.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (GRVkYcbcrComponents left, GRVkYcbcrComponents right)` | not-applicable | `GRVkYcbcrComponents.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRVkYcbcrComponents left, GRVkYcbcrComponents right)` | not-applicable | `GRVkYcbcrComponents.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `GRVkYcbcrComponents.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkYcbcrConversionInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt32 Format` | missing | `GRVkYcbcrConversionInfo.Format` — No exported browser type with this declaration identity. |
| `public UInt64 ExternalFormat` | missing | `GRVkYcbcrConversionInfo.ExternalFormat` — No exported browser type with this declaration identity. |
| `public UInt32 YcbcrModel` | missing | `GRVkYcbcrConversionInfo.YcbcrModel` — No exported browser type with this declaration identity. |
| `public UInt32 YcbcrRange` | missing | `GRVkYcbcrConversionInfo.YcbcrRange` — No exported browser type with this declaration identity. |
| `public UInt32 XChromaOffset` | missing | `GRVkYcbcrConversionInfo.XChromaOffset` — No exported browser type with this declaration identity. |
| `public UInt32 YChromaOffset` | missing | `GRVkYcbcrConversionInfo.YChromaOffset` — No exported browser type with this declaration identity. |
| `public UInt32 ChromaFilter` | missing | `GRVkYcbcrConversionInfo.ChromaFilter` — No exported browser type with this declaration identity. |
| `public UInt32 ForceExplicitReconstruction` | missing | `GRVkYcbcrConversionInfo.ForceExplicitReconstruction` — No exported browser type with this declaration identity. |
| `public GRVkYcbcrComponents Components` | missing | `GRVkYcbcrConversionInfo.Components` — No exported browser type with this declaration identity. |
| `public bool SamplerFilterMustMatchChromaFilter` | missing | `GRVkYcbcrConversionInfo.SamplerFilterMustMatchChromaFilter` — No exported browser type with this declaration identity. |
| `public bool SupportsLinearFilter` | missing | `GRVkYcbcrConversionInfo.SupportsLinearFilter` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (GRVkYcbcrConversionInfo obj)` | missing | `GRVkYcbcrConversionInfo.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `GRVkYcbcrConversionInfo.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (GRVkYcbcrConversionInfo left, GRVkYcbcrConversionInfo right)` | not-applicable | `GRVkYcbcrConversionInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (GRVkYcbcrConversionInfo left, GRVkYcbcrConversionInfo right)` | not-applicable | `GRVkYcbcrConversionInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `GRVkYcbcrConversionInfo.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.SKCodecFrameInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Int32 RequiredFrame` | implemented | `SKCodecFrameInfo.RequiredFrame` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public Int32 Duration` | implemented | `SKCodecFrameInfo.Duration` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public bool FullyRecieved` | implemented | `SKCodecFrameInfo.FullyRecieved` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKAlphaType AlphaType` | implemented | `SKCodecFrameInfo.AlphaType` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public bool HasAlphaWithinBounds` | implemented | `SKCodecFrameInfo.HasAlphaWithinBounds` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecAnimationDisposalMethod DisposalMethod` | implemented | `SKCodecFrameInfo.DisposalMethod` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKCodecAnimationBlend Blend` | implemented | `SKCodecFrameInfo.Blend` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public SKRectI FrameRect` | implemented | `SKCodecFrameInfo.FrameRect` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly bool Equals (SKCodecFrameInfo obj)` | implemented | `SKCodecFrameInfo.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public readonly override bool Equals (object obj)` | implemented | `SKCodecFrameInfo.Equals` — Encoder/codec value constructors, mutable fields and equality are explicitly implemented and exercised by native codec/animation roundtrip tests. |
| `public static bool operator == (SKCodecFrameInfo left, SKCodecFrameInfo right)` | not-applicable | `SKCodecFrameInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKCodecFrameInfo left, SKCodecFrameInfo right)` | not-applicable | `SKCodecFrameInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKCodecFrameInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKDocumentXpsOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Single Dpi` | implemented | `SKDocumentXpsOptions.Dpi` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public bool AllowNoPngs` | implemented | `SKDocumentXpsOptions.AllowNoPngs` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public readonly bool Equals (SKDocumentXpsOptions obj)` | implemented | `SKDocumentXpsOptions.Equals` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public readonly override bool Equals (object obj)` | implemented | `SKDocumentXpsOptions.Equals` — Concrete metadata/option fields, constructor defaults and value equality are used by native PDF/XPS output; native/default metadata and lifetime document tests pass. |
| `public static bool operator == (SKDocumentXpsOptions left, SKDocumentXpsOptions right)` | not-applicable | `SKDocumentXpsOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKDocumentXpsOptions left, SKDocumentXpsOptions right)` | not-applicable | `SKDocumentXpsOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKDocumentXpsOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFontPaletteOverride

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public UInt16 Index` | implemented | `SKFontPaletteOverride.Index` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public UInt32 Color` | implemented | `SKFontPaletteOverride.Color` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly bool Equals (SKFontPaletteOverride obj)` | implemented | `SKFontPaletteOverride.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly override bool Equals (object obj)` | implemented | `SKFontPaletteOverride.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static bool operator == (SKFontPaletteOverride left, SKFontPaletteOverride right)` | not-applicable | `SKFontPaletteOverride.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKFontPaletteOverride left, SKFontPaletteOverride right)` | not-applicable | `SKFontPaletteOverride.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKFontPaletteOverride.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFontVariationAxis

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFourByteTag Tag` | implemented | `SKFontVariationAxis.Tag` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public Single Min` | implemented | `SKFontVariationAxis.Min` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public Single Default` | implemented | `SKFontVariationAxis.Default` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public Single Max` | implemented | `SKFontVariationAxis.Max` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public bool IsHidden` | implemented | `SKFontVariationAxis.IsHidden` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly bool Equals (SKFontVariationAxis obj)` | implemented | `SKFontVariationAxis.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly override bool Equals (object obj)` | implemented | `SKFontVariationAxis.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static bool operator == (SKFontVariationAxis left, SKFontVariationAxis right)` | not-applicable | `SKFontVariationAxis.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKFontVariationAxis left, SKFontVariationAxis right)` | not-applicable | `SKFontVariationAxis.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKFontVariationAxis.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKFontVariationPositionCoordinate

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKFourByteTag Axis` | implemented | `SKFontVariationPositionCoordinate.Axis` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public Single Value` | implemented | `SKFontVariationPositionCoordinate.Value` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly bool Equals (SKFontVariationPositionCoordinate obj)` | implemented | `SKFontVariationPositionCoordinate.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public readonly override bool Equals (object obj)` | implemented | `SKFontVariationPositionCoordinate.Equals` — Font metric/style/variation/palette record constructors and scalar/equality forms are implemented. Metrics and style clamps are compared with both SkiaSharp3.119 and4.154 native references. |
| `public static bool operator == (SKFontVariationPositionCoordinate left, SKFontVariationPositionCoordinate right)` | not-applicable | `SKFontVariationPositionCoordinate.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKFontVariationPositionCoordinate left, SKFontVariationPositionCoordinate right)` | not-applicable | `SKFontVariationPositionCoordinate.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKFontVariationPositionCoordinate.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteContextOptions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool DisableDriverCorrectnessWorkarounds` | partial | `SKGraphiteContextOptions.DisableDriverCorrectnessWorkarounds` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Int32 InternalMultisampleCount` | partial | `SKGraphiteContextOptions.InternalMultisampleCount` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Int64 GpuBudgetInBytes` | partial | `SKGraphiteContextOptions.GpuBudgetInBytes` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool RequireOrderedRecordings` | partial | `SKGraphiteContextOptions.RequireOrderedRecordings` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool SetBackendLabels` | partial | `SKGraphiteContextOptions.SetBackendLabels` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (SKGraphiteContextOptions obj)` | partial | `SKGraphiteContextOptions.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKGraphiteContextOptions.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKGraphiteContextOptions left, SKGraphiteContextOptions right)` | not-applicable | `SKGraphiteContextOptions.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteContextOptions left, SKGraphiteContextOptions right)` | not-applicable | `SKGraphiteContextOptions.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKGraphiteContextOptions.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteDawnBackendContextInit

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public void* Instance` | partial | `SKGraphiteDawnBackendContextInit.Instance` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void* Device` | partial | `SKGraphiteDawnBackendContextInit.Device` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public void* Queue` | partial | `SKGraphiteDawnBackendContextInit.Queue` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public bool NonYielding` | partial | `SKGraphiteDawnBackendContextInit.NonYielding` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly bool Equals (SKGraphiteDawnBackendContextInit obj)` | partial | `SKGraphiteDawnBackendContextInit.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKGraphiteDawnBackendContextInit.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKGraphiteDawnBackendContextInit left, SKGraphiteDawnBackendContextInit right)` | not-applicable | `SKGraphiteDawnBackendContextInit.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteDawnBackendContextInit left, SKGraphiteDawnBackendContextInit right)` | not-applicable | `SKGraphiteDawnBackendContextInit.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKGraphiteDawnBackendContextInit.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteInsertRecordingInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public sk_graphite_recording_t Recording` | partial | `SKGraphiteInsertRecordingInfo.Recording` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public sk_surface_t TargetSurface` | partial | `SKGraphiteInsertRecordingInfo.TargetSurface` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Int32 TargetTranslationX` | partial | `SKGraphiteInsertRecordingInfo.TargetTranslationX` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public Int32 TargetTranslationY` | partial | `SKGraphiteInsertRecordingInfo.TargetTranslationY` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public SKRectI TargetClip` | partial | `SKGraphiteInsertRecordingInfo.TargetClip` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (SKGraphiteInsertRecordingInfo obj)` | partial | `SKGraphiteInsertRecordingInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKGraphiteInsertRecordingInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKGraphiteInsertRecordingInfo left, SKGraphiteInsertRecordingInfo right)` | not-applicable | `SKGraphiteInsertRecordingInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteInsertRecordingInfo left, SKGraphiteInsertRecordingInfo right)` | not-applicable | `SKGraphiteInsertRecordingInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKGraphiteInsertRecordingInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteMtlBackendContextInit

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public void* Device` | missing | `SKGraphiteMtlBackendContextInit.Device` — No exported browser type with this declaration identity. |
| `public void* Queue` | missing | `SKGraphiteMtlBackendContextInit.Queue` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (SKGraphiteMtlBackendContextInit obj)` | missing | `SKGraphiteMtlBackendContextInit.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `SKGraphiteMtlBackendContextInit.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (SKGraphiteMtlBackendContextInit left, SKGraphiteMtlBackendContextInit right)` | not-applicable | `SKGraphiteMtlBackendContextInit.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteMtlBackendContextInit left, SKGraphiteMtlBackendContextInit right)` | not-applicable | `SKGraphiteMtlBackendContextInit.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `SKGraphiteMtlBackendContextInit.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.SKGraphiteSubmitInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public bool Sync` | partial | `SKGraphiteSubmitInfo.Sync` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public bool MarkBoundary` | partial | `SKGraphiteSubmitInfo.MarkBoundary` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public UInt64 FrameID` | partial | `SKGraphiteSubmitInfo.FrameID` — Type exists; instance field/property and all behavioral invariants require a type-specific audit. No inferred completeness from names. |
| `public readonly bool Equals (SKGraphiteSubmitInfo obj)` | partial | `SKGraphiteSubmitInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public readonly override bool Equals (object obj)` | partial | `SKGraphiteSubmitInfo.Equals` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |
| `public static bool operator == (SKGraphiteSubmitInfo left, SKGraphiteSubmitInfo right)` | not-applicable | `SKGraphiteSubmitInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteSubmitInfo left, SKGraphiteSubmitInfo right)` | not-applicable | `SKGraphiteSubmitInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | partial | `SKGraphiteSubmitInfo.GetHashCode` — A member is exported, but this declaration is not certified by the signature-level audit rules. Member-name presence is deliberately not counted as full overload or behavioral conformance. |

### SkiaSharp.SKGraphiteVkTextureInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Int32 SampleCount` | missing | `SKGraphiteVkTextureInfo.SampleCount` — No exported browser type with this declaration identity. |
| `public bool Mipmapped` | missing | `SKGraphiteVkTextureInfo.Mipmapped` — No exported browser type with this declaration identity. |
| `public UInt32 Flags` | missing | `SKGraphiteVkTextureInfo.Flags` — No exported browser type with this declaration identity. |
| `public Int32 Format` | missing | `SKGraphiteVkTextureInfo.Format` — No exported browser type with this declaration identity. |
| `public Int32 ImageTiling` | missing | `SKGraphiteVkTextureInfo.ImageTiling` — No exported browser type with this declaration identity. |
| `public UInt32 ImageUsageFlags` | missing | `SKGraphiteVkTextureInfo.ImageUsageFlags` — No exported browser type with this declaration identity. |
| `public Int32 SharingMode` | missing | `SKGraphiteVkTextureInfo.SharingMode` — No exported browser type with this declaration identity. |
| `public UInt32 AspectMask` | missing | `SKGraphiteVkTextureInfo.AspectMask` — No exported browser type with this declaration identity. |
| `public readonly bool Equals (SKGraphiteVkTextureInfo obj)` | missing | `SKGraphiteVkTextureInfo.Equals` — No exported browser type with this declaration identity. |
| `public readonly override bool Equals (object obj)` | missing | `SKGraphiteVkTextureInfo.Equals` — No exported browser type with this declaration identity. |
| `public static bool operator == (SKGraphiteVkTextureInfo left, SKGraphiteVkTextureInfo right)` | not-applicable | `SKGraphiteVkTextureInfo.Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static bool operator != (SKGraphiteVkTextureInfo left, SKGraphiteVkTextureInfo right)` | not-applicable | `SKGraphiteVkTextureInfo.!Equals(...)` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public readonly override int GetHashCode ()` | missing | `SKGraphiteVkTextureInfo.GetHashCode` — No exported browser type with this declaration identity. |

### SkiaSharp.GRSurfaceOrigin

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// TOP_LEFT_GR_SURFACE_ORIGIN = 0 TopLeft = 0, // BOTTOM_LEFT_GR_SURFACE_ORIGIN = 1` | implemented | `GRSurfaceOrigin.TopLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BottomLeft = 1` | implemented | `GRSurfaceOrigin.BottomLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKAlphaType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UNKNOWN_SK_ALPHATYPE = 0 Unknown = 0, // OPAQUE_SK_ALPHATYPE = 1` | implemented | `SKAlphaType.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Opaque = 1, // PREMUL_SK_ALPHATYPE = 2` | implemented | `SKAlphaType.Opaque` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Premul = 2, // UNPREMUL_SK_ALPHATYPE = 3` | implemented | `SKAlphaType.Premul` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Unpremul = 3` | implemented | `SKAlphaType.Unpremul` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKBitmapAllocFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SK_BITMAP_ALLOC_FLAGS = 0 None = 0, // ZERO_PIXELS_SK_BITMAP_ALLOC_FLAGS = 1 << 0` | implemented | `SKBitmapAllocFlags.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ZeroPixels = 1` | implemented | `SKBitmapAllocFlags.ZeroPixels` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKBlendMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// CLEAR_SK_BLENDMODE = 0 Clear = 0, // SRC_SK_BLENDMODE = 1` | implemented | `SKBlendMode.Clear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Src = 1, // DST_SK_BLENDMODE = 2` | implemented | `SKBlendMode.Src` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Dst = 2, // SRCOVER_SK_BLENDMODE = 3` | implemented | `SKBlendMode.Dst` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SrcOver = 3, // DSTOVER_SK_BLENDMODE = 4` | implemented | `SKBlendMode.SrcOver` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DstOver = 4, // SRCIN_SK_BLENDMODE = 5` | implemented | `SKBlendMode.DstOver` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SrcIn = 5, // DSTIN_SK_BLENDMODE = 6` | implemented | `SKBlendMode.SrcIn` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DstIn = 6, // SRCOUT_SK_BLENDMODE = 7` | implemented | `SKBlendMode.DstIn` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SrcOut = 7, // DSTOUT_SK_BLENDMODE = 8` | implemented | `SKBlendMode.SrcOut` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DstOut = 8, // SRCATOP_SK_BLENDMODE = 9` | implemented | `SKBlendMode.DstOut` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SrcATop = 9, // DSTATOP_SK_BLENDMODE = 10` | implemented | `SKBlendMode.SrcATop` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `DstATop = 10, // XOR_SK_BLENDMODE = 11` | implemented | `SKBlendMode.DstATop` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Xor = 11, // PLUS_SK_BLENDMODE = 12` | implemented | `SKBlendMode.Xor` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Plus = 12, // MODULATE_SK_BLENDMODE = 13` | implemented | `SKBlendMode.Plus` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Modulate = 13, // SCREEN_SK_BLENDMODE = 14` | implemented | `SKBlendMode.Modulate` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Screen = 14, // OVERLAY_SK_BLENDMODE = 15` | implemented | `SKBlendMode.Screen` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Overlay = 15, // DARKEN_SK_BLENDMODE = 16` | implemented | `SKBlendMode.Overlay` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Darken = 16, // LIGHTEN_SK_BLENDMODE = 17` | implemented | `SKBlendMode.Darken` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Lighten = 17, // COLORDODGE_SK_BLENDMODE = 18` | implemented | `SKBlendMode.Lighten` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ColorDodge = 18, // COLORBURN_SK_BLENDMODE = 19` | implemented | `SKBlendMode.ColorDodge` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ColorBurn = 19, // HARDLIGHT_SK_BLENDMODE = 20` | implemented | `SKBlendMode.ColorBurn` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `HardLight = 20, // SOFTLIGHT_SK_BLENDMODE = 21` | implemented | `SKBlendMode.HardLight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SoftLight = 21, // DIFFERENCE_SK_BLENDMODE = 22` | implemented | `SKBlendMode.SoftLight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Difference = 22, // EXCLUSION_SK_BLENDMODE = 23` | implemented | `SKBlendMode.Difference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Exclusion = 23, // MULTIPLY_SK_BLENDMODE = 24` | implemented | `SKBlendMode.Exclusion` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Multiply = 24, // HUE_SK_BLENDMODE = 25` | implemented | `SKBlendMode.Multiply` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Hue = 25, // SATURATION_SK_BLENDMODE = 26` | implemented | `SKBlendMode.Hue` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Saturation = 26, // COLOR_SK_BLENDMODE = 27` | implemented | `SKBlendMode.Saturation` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Color = 27, // LUMINOSITY_SK_BLENDMODE = 28` | implemented | `SKBlendMode.Color` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Luminosity = 28` | implemented | `SKBlendMode.Luminosity` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKBlurStyle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NORMAL_SK_BLUR_STYLE = 0 Normal = 0, // SOLID_SK_BLUR_STYLE = 1` | implemented | `SKBlurStyle.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Solid = 1, // OUTER_SK_BLUR_STYLE = 2` | implemented | `SKBlurStyle.Solid` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Outer = 2, // INNER_SK_BLUR_STYLE = 3` | implemented | `SKBlurStyle.Outer` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Inner = 3` | implemented | `SKBlurStyle.Inner` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKCanvasSaveLayerRecFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SK_CANVAS_SAVELAYERREC_FLAGS = 0 None = 0, // PRESERVE_LCD_TEXT_SK_CANVAS_SAVELAYERREC_FLAGS = 1 << 1` | implemented | `SKCanvasSaveLayerRecFlags.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `PreserveLcdText = 2, // INITIALIZE_WITH_PREVIOUS_SK_CANVAS_SAVELAYERREC_FLAGS = 1 << 2` | implemented | `SKCanvasSaveLayerRecFlags.PreserveLcdText` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InitializeWithPrevious = 4, // F16_COLOR_TYPE_SK_CANVAS_SAVELAYERREC_FLAGS = 1 << 4` | implemented | `SKCanvasSaveLayerRecFlags.InitializeWithPrevious` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `F16ColorType = 16` | implemented | `SKCanvasSaveLayerRecFlags.F16ColorType` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKClipOperation

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DIFFERENCE_SK_CLIPOP = 0 Difference = 0, // INTERSECT_SK_CLIPOP = 1` | implemented | `SKClipOperation.Difference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Intersect = 1` | implemented | `SKClipOperation.Intersect` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKCodecResult

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SUCCESS_SK_CODEC_RESULT = 0 Success = 0, // INCOMPLETE_INPUT_SK_CODEC_RESULT = 1` | implemented | `SKCodecResult.Success` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `IncompleteInput = 1, // ERROR_IN_INPUT_SK_CODEC_RESULT = 2` | implemented | `SKCodecResult.IncompleteInput` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ErrorInInput = 2, // INVALID_CONVERSION_SK_CODEC_RESULT = 3` | implemented | `SKCodecResult.ErrorInInput` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvalidConversion = 3, // INVALID_SCALE_SK_CODEC_RESULT = 4` | implemented | `SKCodecResult.InvalidConversion` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvalidScale = 4, // INVALID_PARAMETERS_SK_CODEC_RESULT = 5` | implemented | `SKCodecResult.InvalidScale` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvalidParameters = 5, // INVALID_INPUT_SK_CODEC_RESULT = 6` | implemented | `SKCodecResult.InvalidParameters` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvalidInput = 6, // COULD_NOT_REWIND_SK_CODEC_RESULT = 7` | implemented | `SKCodecResult.InvalidInput` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `CouldNotRewind = 7, // INTERNAL_ERROR_SK_CODEC_RESULT = 8` | implemented | `SKCodecResult.CouldNotRewind` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InternalError = 8, // UNIMPLEMENTED_SK_CODEC_RESULT = 9` | implemented | `SKCodecResult.InternalError` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Unimplemented = 9` | implemented | `SKCodecResult.Unimplemented` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKCodecScanlineOrder

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// TOP_DOWN_SK_CODEC_SCANLINE_ORDER = 0 TopDown = 0, // BOTTOM_UP_SK_CODEC_SCANLINE_ORDER = 1` | implemented | `SKCodecScanlineOrder.TopDown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BottomUp = 1` | implemented | `SKCodecScanlineOrder.BottomUp` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKZeroInitialized

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// YES_SK_CODEC_ZERO_INITIALIZED = 0 Yes = 0, // NO_SK_CODEC_ZERO_INITIALIZED = 1` | implemented | `SKZeroInitialized.Yes` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `No = 1` | implemented | `SKZeroInitialized.No` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKCodecAnimationBlend

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SRC_OVER_SK_CODEC_ANIMATION_BLEND = 0 SrcOver = 0, // SRC_SK_CODEC_ANIMATION_BLEND = 1` | implemented | `SKCodecAnimationBlend.SrcOver` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Src = 1` | implemented | `SKCodecAnimationBlend.Src` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKCodecAnimationDisposalMethod

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// KEEP_SK_CODEC_ANIMATION_DISPOSAL_METHOD = 1 Keep = 1, // RESTORE_BG_COLOR_SK_CODEC_ANIMATION_DISPOSAL_METHOD = 2` | implemented | `SKCodecAnimationDisposalMethod.Keep` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RestoreBackgroundColor = 2, // RESTORE_PREVIOUS_SK_CODEC_ANIMATION_DISPOSAL_METHOD = 3` | implemented | `SKCodecAnimationDisposalMethod.RestoreBackgroundColor` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RestorePrevious = 3` | implemented | `SKCodecAnimationDisposalMethod.RestorePrevious` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKColorChannel

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// R_SK_COLOR_CHANNEL = 0 R = 0, // G_SK_COLOR_CHANNEL = 1` | implemented | `SKColorChannel.R` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `G = 1, // B_SK_COLOR_CHANNEL = 2` | implemented | `SKColorChannel.G` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `B = 2, // A_SK_COLOR_CHANNEL = 3` | implemented | `SKColorChannel.B` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `A = 3` | implemented | `SKColorChannel.A` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKColorspacePrimariesCicp

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UNKNOWN_SK_COLORSPACE_PRIMARIES_CICP = 0 Unknown = 0, // REC709_SK_COLORSPACE_PRIMARIES_CICP = 1` | implemented | `SKColorspacePrimariesCicp.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec709 = 1, // REC470_SYSTEM_M_SK_COLORSPACE_PRIMARIES_CICP = 4` | implemented | `SKColorspacePrimariesCicp.Rec709` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec470SystemM = 4, // REC470_SYSTEM_BG_SK_COLORSPACE_PRIMARIES_CICP = 5` | implemented | `SKColorspacePrimariesCicp.Rec470SystemM` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec470SystemBg = 5, // REC601_SK_COLORSPACE_PRIMARIES_CICP = 6` | implemented | `SKColorspacePrimariesCicp.Rec470SystemBg` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec601 = 6, // SMPTE_ST240_SK_COLORSPACE_PRIMARIES_CICP = 7` | implemented | `SKColorspacePrimariesCicp.Rec601` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteSt240 = 7, // GENERIC_FILM_SK_COLORSPACE_PRIMARIES_CICP = 8` | implemented | `SKColorspacePrimariesCicp.SmpteSt240` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `GenericFilm = 8, // REC2020_SK_COLORSPACE_PRIMARIES_CICP = 9` | implemented | `SKColorspacePrimariesCicp.GenericFilm` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec2020 = 9, // SMPTE_ST428_1_SK_COLORSPACE_PRIMARIES_CICP = 10` | implemented | `SKColorspacePrimariesCicp.Rec2020` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteSt4281 = 10, // SMPTE_RP431_2_SK_COLORSPACE_PRIMARIES_CICP = 11` | implemented | `SKColorspacePrimariesCicp.SmpteSt4281` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteRp4312 = 11, // SMPTE_EG432_1_SK_COLORSPACE_PRIMARIES_CICP = 12` | implemented | `SKColorspacePrimariesCicp.SmpteRp4312` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteEg4321 = 12, // ITU_T_H273_VALUE22_SK_COLORSPACE_PRIMARIES_CICP = 22` | implemented | `SKColorspacePrimariesCicp.SmpteEg4321` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ItuTH273Value22 = 22` | implemented | `SKColorspacePrimariesCicp.ItuTH273Value22` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKColorspaceTransferFnCicp

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UNKNOWN_SK_COLORSPACE_TRANSFER_FN_CICP = 0 Unknown = 0, // REC709_SK_COLORSPACE_TRANSFER_FN_CICP = 1` | implemented | `SKColorspaceTransferFnCicp.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec709 = 1, // REC470_SYSTEM_M_SK_COLORSPACE_TRANSFER_FN_CICP = 4` | implemented | `SKColorspaceTransferFnCicp.Rec709` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec470SystemM = 4, // REC470_SYSTEM_BG_SK_COLORSPACE_TRANSFER_FN_CICP = 5` | implemented | `SKColorspaceTransferFnCicp.Rec470SystemM` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec470SystemBg = 5, // REC601_SK_COLORSPACE_TRANSFER_FN_CICP = 6` | implemented | `SKColorspaceTransferFnCicp.Rec470SystemBg` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec601 = 6, // SMPTE_ST240_SK_COLORSPACE_TRANSFER_FN_CICP = 7` | implemented | `SKColorspaceTransferFnCicp.Rec601` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteSt240 = 7, // LINEAR_SK_COLORSPACE_TRANSFER_FN_CICP = 8` | implemented | `SKColorspaceTransferFnCicp.SmpteSt240` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Linear = 8, // IEC61966_2_4_SK_COLORSPACE_TRANSFER_FN_CICP = 11` | implemented | `SKColorspaceTransferFnCicp.Linear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Iec6196624 = 11, // IEC61966_2_1_SK_COLORSPACE_TRANSFER_FN_CICP = 13` | implemented | `SKColorspaceTransferFnCicp.Iec6196624` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Iec6196621 = 13, // REC2020_10BIT_SK_COLORSPACE_TRANSFER_FN_CICP = 14` | implemented | `SKColorspaceTransferFnCicp.Iec6196621` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec202010bit = 14, // REC2020_12BIT_SK_COLORSPACE_TRANSFER_FN_CICP = 15` | implemented | `SKColorspaceTransferFnCicp.Rec202010bit` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rec202012bit = 15, // PQ_SK_COLORSPACE_TRANSFER_FN_CICP = 16` | implemented | `SKColorspaceTransferFnCicp.Rec202012bit` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Pq = 16, // SMPTE_ST428_1_SK_COLORSPACE_TRANSFER_FN_CICP = 17` | implemented | `SKColorspaceTransferFnCicp.Pq` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SmpteSt4281 = 17, // HLG_SK_COLORSPACE_TRANSFER_FN_CICP = 18` | implemented | `SKColorspaceTransferFnCicp.SmpteSt4281` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Hlg = 18` | implemented | `SKColorspaceTransferFnCicp.Hlg` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKEncodedImageFormat

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// BMP_SK_ENCODED_FORMAT = 0 Bmp = 0, // GIF_SK_ENCODED_FORMAT = 1` | implemented | `SKEncodedImageFormat.Bmp` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Gif = 1, // ICO_SK_ENCODED_FORMAT = 2` | implemented | `SKEncodedImageFormat.Gif` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Ico = 2, // JPEG_SK_ENCODED_FORMAT = 3` | implemented | `SKEncodedImageFormat.Ico` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Jpeg = 3, // PNG_SK_ENCODED_FORMAT = 4` | implemented | `SKEncodedImageFormat.Jpeg` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Png = 4, // WBMP_SK_ENCODED_FORMAT = 5` | implemented | `SKEncodedImageFormat.Png` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Wbmp = 5, // WEBP_SK_ENCODED_FORMAT = 6` | implemented | `SKEncodedImageFormat.Wbmp` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Webp = 6, // PKM_SK_ENCODED_FORMAT = 7` | implemented | `SKEncodedImageFormat.Webp` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Pkm = 7, // KTX_SK_ENCODED_FORMAT = 8` | implemented | `SKEncodedImageFormat.Pkm` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Ktx = 8, // ASTC_SK_ENCODED_FORMAT = 9` | implemented | `SKEncodedImageFormat.Ktx` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Astc = 9, // DNG_SK_ENCODED_FORMAT = 10` | implemented | `SKEncodedImageFormat.Astc` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Dng = 10, // HEIF_SK_ENCODED_FORMAT = 11` | implemented | `SKEncodedImageFormat.Dng` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Heif = 11, // AVIF_SK_ENCODED_FORMAT = 12` | implemented | `SKEncodedImageFormat.Heif` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Avif = 12, // JPEGXL_SK_ENCODED_FORMAT = 13` | implemented | `SKEncodedImageFormat.Avif` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Jpegxl = 13` | implemented | `SKEncodedImageFormat.Jpegxl` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKEncodedOrigin

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// TOP_LEFT_SK_ENCODED_ORIGIN = 1 TopLeft = 1, // TOP_RIGHT_SK_ENCODED_ORIGIN = 2` | implemented | `SKEncodedOrigin.TopLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `TopRight = 2, // BOTTOM_RIGHT_SK_ENCODED_ORIGIN = 3` | implemented | `SKEncodedOrigin.TopRight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BottomRight = 3, // BOTTOM_LEFT_SK_ENCODED_ORIGIN = 4` | implemented | `SKEncodedOrigin.BottomRight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BottomLeft = 4, // LEFT_TOP_SK_ENCODED_ORIGIN = 5` | implemented | `SKEncodedOrigin.BottomLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `LeftTop = 5, // RIGHT_TOP_SK_ENCODED_ORIGIN = 6` | implemented | `SKEncodedOrigin.LeftTop` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RightTop = 6, // RIGHT_BOTTOM_SK_ENCODED_ORIGIN = 7` | implemented | `SKEncodedOrigin.RightTop` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RightBottom = 7, // LEFT_BOTTOM_SK_ENCODED_ORIGIN = 8` | implemented | `SKEncodedOrigin.RightBottom` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `LeftBottom = 8, // DEFAULT_SK_ENCODED_ORIGIN = TOP_LEFT_SK_ENCODED_ORIGIN` | implemented | `SKEncodedOrigin.LeftBottom` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Default = 1` | implemented | `SKEncodedOrigin.Default` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFilterMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NEAREST_SK_FILTER_MODE = 0 Nearest = 0, // LINEAR_SK_FILTER_MODE = 1` | implemented | `SKFilterMode.Nearest` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Linear = 1` | implemented | `SKFilterMode.Linear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFontEdging

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// ALIAS_SK_FONT_EDGING = 0 Alias = 0, // ANTIALIAS_SK_FONT_EDGING = 1` | implemented | `SKFontEdging.Alias` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Antialias = 1, // SUBPIXEL_ANTIALIAS_SK_FONT_EDGING = 2` | implemented | `SKFontEdging.Antialias` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `SubpixelAntialias = 2` | implemented | `SKFontEdging.SubpixelAntialias` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFontHinting

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SK_FONT_HINTING = 0 None = 0, // SLIGHT_SK_FONT_HINTING = 1` | implemented | `SKFontHinting.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Slight = 1, // NORMAL_SK_FONT_HINTING = 2` | implemented | `SKFontHinting.Slight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Normal = 2, // FULL_SK_FONT_HINTING = 3` | implemented | `SKFontHinting.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Full = 3` | implemented | `SKFontHinting.Full` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKFontStyleSlant

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UPRIGHT_SK_FONT_STYLE_SLANT = 0 Upright = 0, // ITALIC_SK_FONT_STYLE_SLANT = 1` | implemented | `SKFontStyleSlant.Upright` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Italic = 1, // OBLIQUE_SK_FONT_STYLE_SLANT = 2` | implemented | `SKFontStyleSlant.Italic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Oblique = 2` | implemented | `SKFontStyleSlant.Oblique` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKGraphiteBackend

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DAWN_SK_GRAPHITE_BACKEND = 0 Dawn = 0, // METAL_SK_GRAPHITE_BACKEND = 1` | implemented | `SKGraphiteBackend.Dawn` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Metal = 1, // VULKAN_SK_GRAPHITE_BACKEND = 2` | implemented | `SKGraphiteBackend.Metal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Vulkan = 2, // UNKNOWN_SK_GRAPHITE_BACKEND = -1` | implemented | `SKGraphiteBackend.Vulkan` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Unknown = -1` | implemented | `SKGraphiteBackend.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKGraphiteInsertStatus

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SUCCESS_SK_GRAPHITE_INSERT_STATUS = 0 Success = 0, // INVALID_RECORDING_SK_GRAPHITE_INSERT_STATUS = 1` | implemented | `SKGraphiteInsertStatus.Success` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvalidRecording = 1, // PROMISE_INSTANTIATION_FAILED_SK_GRAPHITE_INSERT_STATUS = 2` | implemented | `SKGraphiteInsertStatus.InvalidRecording` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `PromiseInstantiationFailed = 2, // ADD_COMMANDS_FAILED_SK_GRAPHITE_INSERT_STATUS = 3` | implemented | `SKGraphiteInsertStatus.PromiseInstantiationFailed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `AddCommandsFailed = 3, // ASYNC_SHADER_COMPILES_FAILED_SK_GRAPHITE_INSERT_STATUS = 4` | implemented | `SKGraphiteInsertStatus.AddCommandsFailed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `AsyncShaderCompilesFailed = 4, // OUT_OF_ORDER_RECORDING_SK_GRAPHITE_INSERT_STATUS = 5` | implemented | `SKGraphiteInsertStatus.AsyncShaderCompilesFailed` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `OutOfOrderRecording = 5` | implemented | `SKGraphiteInsertStatus.OutOfOrderRecording` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKHighContrastConfigInvertStyle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NO_INVERT_SK_HIGH_CONTRAST_CONFIG_INVERT_STYLE = 0 NoInvert = 0, // INVERT_BRIGHTNESS_SK_HIGH_CONTRAST_CONFIG_INVERT_STYLE = 1` | implemented | `SKHighContrastConfigInvertStyle.NoInvert` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvertBrightness = 1, // INVERT_LIGHTNESS_SK_HIGH_CONTRAST_CONFIG_INVERT_STYLE = 2` | implemented | `SKHighContrastConfigInvertStyle.InvertBrightness` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InvertLightness = 2` | implemented | `SKHighContrastConfigInvertStyle.InvertLightness` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKImageCachingHint

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// ALLOW_SK_IMAGE_CACHING_HINT = 0 Allow = 0, // DISALLOW_SK_IMAGE_CACHING_HINT = 1` | implemented | `SKImageCachingHint.Allow` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Disallow = 1` | implemented | `SKImageCachingHint.Disallow` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKImageRescaleGamma

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SRC_SK_IMAGE_RESCALE_GAMMA = 0 Src = 0, // LINEAR_SK_IMAGE_RESCALE_GAMMA = 1` | implemented | `SKImageRescaleGamma.Src` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Linear = 1` | implemented | `SKImageRescaleGamma.Linear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKImageRescaleMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NEAREST_SK_IMAGE_RESCALE_MODE = 0 Nearest = 0, // LINEAR_SK_IMAGE_RESCALE_MODE = 1` | implemented | `SKImageRescaleMode.Nearest` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Linear = 1, // REPEATED_LINEAR_SK_IMAGE_RESCALE_MODE = 2` | implemented | `SKImageRescaleMode.Linear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RepeatedLinear = 2, // REPEATED_CUBIC_SK_IMAGE_RESCALE_MODE = 3` | implemented | `SKImageRescaleMode.RepeatedLinear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RepeatedCubic = 3` | implemented | `SKImageRescaleMode.RepeatedCubic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKJpegEncoderAlphaOption

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// IGNORE_SK_JPEGENCODER_ALPHA_OPTION = 0 Ignore = 0, // BLEND_ON_BLACK_SK_JPEGENCODER_ALPHA_OPTION = 1` | implemented | `SKJpegEncoderAlphaOption.Ignore` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BlendOnBlack = 1` | implemented | `SKJpegEncoderAlphaOption.BlendOnBlack` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKJpegEncoderDownsample

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DOWNSAMPLE_420_SK_JPEGENCODER_DOWNSAMPLE = 0 Downsample420 = 0, // DOWNSAMPLE_422_SK_JPEGENCODER_DOWNSAMPLE = 1` | implemented | `SKJpegEncoderDownsample.Downsample420` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Downsample422 = 1, // DOWNSAMPLE_444_SK_JPEGENCODER_DOWNSAMPLE = 2` | implemented | `SKJpegEncoderDownsample.Downsample422` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Downsample444 = 2` | implemented | `SKJpegEncoderDownsample.Downsample444` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKLatticeRectType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DEFAULT_SK_LATTICE_RECT_TYPE = 0 Default = 0, // TRANSPARENT_SK_LATTICE_RECT_TYPE = 1` | implemented | `SKLatticeRectType.Default` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Transparent = 1, // FIXED_COLOR_SK_LATTICE_RECT_TYPE = 2` | implemented | `SKLatticeRectType.Transparent` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `FixedColor = 2` | implemented | `SKLatticeRectType.FixedColor` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKMipmapMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SK_MIPMAP_MODE = 0 None = 0, // NEAREST_SK_MIPMAP_MODE = 1` | implemented | `SKMipmapMode.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Nearest = 1, // LINEAR_SK_MIPMAP_MODE = 2` | implemented | `SKMipmapMode.Nearest` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Linear = 2` | implemented | `SKMipmapMode.Linear` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPaintStyle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// FILL_SK_PAINT_STYLE = 0 Fill = 0, // STROKE_SK_PAINT_STYLE = 1` | implemented | `SKPaintStyle.Fill` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Stroke = 1, // STROKE_AND_FILL_SK_PAINT_STYLE = 2` | implemented | `SKPaintStyle.Stroke` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `StrokeAndFill = 2` | implemented | `SKPaintStyle.StrokeAndFill` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathAddMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// APPEND_SK_PATH_ADD_MODE = 0 Append = 0, // EXTEND_SK_PATH_ADD_MODE = 1` | implemented | `SKPathAddMode.Append` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Extend = 1` | implemented | `SKPathAddMode.Extend` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathArcSize

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// SMALL_SK_PATH_ARC_SIZE = 0 Small = 0, // LARGE_SK_PATH_ARC_SIZE = 1` | implemented | `SKPathArcSize.Small` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Large = 1` | implemented | `SKPathArcSize.Large` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathDirection

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// CW_SK_PATH_DIRECTION = 0 Clockwise = 0, // CCW_SK_PATH_DIRECTION = 1` | implemented | `SKPathDirection.Clockwise` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `CounterClockwise = 1` | implemented | `SKPathDirection.CounterClockwise` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPath1DPathEffectStyle

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// TRANSLATE_SK_PATH_EFFECT_1D_STYLE = 0 Translate = 0, // ROTATE_SK_PATH_EFFECT_1D_STYLE = 1` | implemented | `SKPath1DPathEffectStyle.Translate` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rotate = 1, // MORPH_SK_PATH_EFFECT_1D_STYLE = 2` | implemented | `SKPath1DPathEffectStyle.Rotate` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Morph = 2` | implemented | `SKPath1DPathEffectStyle.Morph` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKTrimPathEffectMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NORMAL_SK_PATH_EFFECT_TRIM_MODE = 0 Normal = 0, // INVERTED_SK_PATH_EFFECT_TRIM_MODE = 1` | implemented | `SKTrimPathEffectMode.Normal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Inverted = 1` | implemented | `SKTrimPathEffectMode.Inverted` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathFillType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// WINDING_SK_PATH_FILLTYPE = 0 Winding = 0, // EVENODD_SK_PATH_FILLTYPE = 1` | implemented | `SKPathFillType.Winding` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `EvenOdd = 1, // INVERSE_WINDING_SK_PATH_FILLTYPE = 2` | implemented | `SKPathFillType.EvenOdd` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InverseWinding = 2, // INVERSE_EVENODD_SK_PATH_FILLTYPE = 3` | implemented | `SKPathFillType.InverseWinding` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `InverseEvenOdd = 3` | implemented | `SKPathFillType.InverseEvenOdd` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathSegmentMask

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// LINE_SK_PATH_SEGMENT_MASK = 1 << 0 Line = 1, // QUAD_SK_PATH_SEGMENT_MASK = 1 << 1` | implemented | `SKPathSegmentMask.Line` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Quad = 2, // CONIC_SK_PATH_SEGMENT_MASK = 1 << 2` | implemented | `SKPathSegmentMask.Quad` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Conic = 4, // CUBIC_SK_PATH_SEGMENT_MASK = 1 << 3` | implemented | `SKPathSegmentMask.Conic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Cubic = 8` | implemented | `SKPathSegmentMask.Cubic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathVerb

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// MOVE_SK_PATH_VERB = 0 Move = 0, // LINE_SK_PATH_VERB = 1` | implemented | `SKPathVerb.Move` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Line = 1, // QUAD_SK_PATH_VERB = 2` | implemented | `SKPathVerb.Line` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Quad = 2, // CONIC_SK_PATH_VERB = 3` | implemented | `SKPathVerb.Quad` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Conic = 3, // CUBIC_SK_PATH_VERB = 4` | implemented | `SKPathVerb.Conic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Cubic = 4, // CLOSE_SK_PATH_VERB = 5` | implemented | `SKPathVerb.Cubic` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Close = 5, // DONE_SK_PATH_VERB = 6` | implemented | `SKPathVerb.Close` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Done = 6` | implemented | `SKPathVerb.Done` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathMeasureMatrixFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// GET_POSITION_SK_PATHMEASURE_MATRIXFLAGS = 0x01 GetPosition = 1, // GET_TANGENT_SK_PATHMEASURE_MATRIXFLAGS = 0x02` | implemented | `SKPathMeasureMatrixFlags.GetPosition` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `GetTangent = 2, // GET_POS_AND_TAN_SK_PATHMEASURE_MATRIXFLAGS = GET_POSITION_SK_PATHMEASURE_MATRIXFLAGS \| GET_TANGENT_SK_PATHMEASURE_MATRIXFLAGS` | implemented | `SKPathMeasureMatrixFlags.GetTangent` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `GetPositionAndTangent = 3` | implemented | `SKPathMeasureMatrixFlags.GetPositionAndTangent` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPathOp

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DIFFERENCE_SK_PATHOP = 0 Difference = 0, // INTERSECT_SK_PATHOP = 1` | implemented | `SKPathOp.Difference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Intersect = 1, // UNION_SK_PATHOP = 2` | implemented | `SKPathOp.Intersect` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Union = 2, // XOR_SK_PATHOP = 3` | implemented | `SKPathOp.Union` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Xor = 3, // REVERSE_DIFFERENCE_SK_PATHOP = 4` | implemented | `SKPathOp.Xor` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ReverseDifference = 4` | implemented | `SKPathOp.ReverseDifference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPixelGeometry

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UNKNOWN_SK_PIXELGEOMETRY = 0 Unknown = 0, // RGB_H_SK_PIXELGEOMETRY = 1` | implemented | `SKPixelGeometry.Unknown` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbHorizontal = 1, // BGR_H_SK_PIXELGEOMETRY = 2` | implemented | `SKPixelGeometry.RgbHorizontal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BgrHorizontal = 2, // RGB_V_SK_PIXELGEOMETRY = 3` | implemented | `SKPixelGeometry.BgrHorizontal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `RgbVertical = 3, // BGR_V_SK_PIXELGEOMETRY = 4` | implemented | `SKPixelGeometry.RgbVertical` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `BgrVertical = 4` | implemented | `SKPixelGeometry.BgrVertical` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPngEncoderFilterFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// ZERO_SK_PNGENCODER_FILTER_FLAGS = 0x00 NoFilters = 0, // NONE_SK_PNGENCODER_FILTER_FLAGS = 0x08` | implemented | `SKPngEncoderFilterFlags.NoFilters` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `None = 8, // SUB_SK_PNGENCODER_FILTER_FLAGS = 0x10` | implemented | `SKPngEncoderFilterFlags.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Sub = 16, // UP_SK_PNGENCODER_FILTER_FLAGS = 0x20` | implemented | `SKPngEncoderFilterFlags.Sub` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Up = 32, // AVG_SK_PNGENCODER_FILTER_FLAGS = 0x40` | implemented | `SKPngEncoderFilterFlags.Up` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Avg = 64, // PAETH_SK_PNGENCODER_FILTER_FLAGS = 0x80` | implemented | `SKPngEncoderFilterFlags.Avg` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Paeth = 128, // ALL_SK_PNGENCODER_FILTER_FLAGS = NONE_SK_PNGENCODER_FILTER_FLAGS \| SUB_SK_PNGENCODER_FILTER_FLAGS \| UP_SK_PNGENCODER_FILTER_FLAGS \| AVG_SK_PNGENCODER_FILTER_FLAGS \| PAETH_SK_PNGENCODER_FILTER_FLAGS` | implemented | `SKPngEncoderFilterFlags.Paeth` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `AllFilters = 248` | implemented | `SKPngEncoderFilterFlags.AllFilters` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKPointMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// POINTS_SK_POINT_MODE = 0 Points = 0, // LINES_SK_POINT_MODE = 1` | implemented | `SKPointMode.Points` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Lines = 1, // POLYGON_SK_POINT_MODE = 2` | implemented | `SKPointMode.Lines` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Polygon = 2` | implemented | `SKPointMode.Polygon` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKRegionOperation

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// DIFFERENCE_SK_REGION_OP = 0 Difference = 0, // INTERSECT_SK_REGION_OP = 1` | implemented | `SKRegionOperation.Difference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Intersect = 1, // UNION_SK_REGION_OP = 2` | implemented | `SKRegionOperation.Intersect` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Union = 2, // XOR_SK_REGION_OP = 3` | implemented | `SKRegionOperation.Union` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `XOR = 3, // REVERSE_DIFFERENCE_SK_REGION_OP = 4` | implemented | `SKRegionOperation.XOR` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `ReverseDifference = 4, // REPLACE_SK_REGION_OP = 5` | implemented | `SKRegionOperation.ReverseDifference` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Replace = 5` | implemented | `SKRegionOperation.Replace` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKRoundRectCorner

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UPPER_LEFT_SK_RRECT_CORNER = 0 UpperLeft = 0, // UPPER_RIGHT_SK_RRECT_CORNER = 1` | implemented | `SKRoundRectCorner.UpperLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `UpperRight = 1, // LOWER_RIGHT_SK_RRECT_CORNER = 2` | implemented | `SKRoundRectCorner.UpperRight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `LowerRight = 2, // LOWER_LEFT_SK_RRECT_CORNER = 3` | implemented | `SKRoundRectCorner.LowerRight` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `LowerLeft = 3` | implemented | `SKRoundRectCorner.LowerLeft` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKRoundRectType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// EMPTY_SK_RRECT_TYPE = 0 Empty = 0, // RECT_SK_RRECT_TYPE = 1` | implemented | `SKRoundRectType.Empty` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Rect = 1, // OVAL_SK_RRECT_TYPE = 2` | implemented | `SKRoundRectType.Rect` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Oval = 2, // SIMPLE_SK_RRECT_TYPE = 3` | implemented | `SKRoundRectType.Oval` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Simple = 3, // NINE_PATCH_SK_RRECT_TYPE = 4` | implemented | `SKRoundRectType.Simple` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `NinePatch = 4, // COMPLEX_SK_RRECT_TYPE = 5` | implemented | `SKRoundRectType.NinePatch` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Complex = 5` | implemented | `SKRoundRectType.Complex` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKShaderTileMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// CLAMP_SK_SHADER_TILEMODE = 0 Clamp = 0, // REPEAT_SK_SHADER_TILEMODE = 1` | implemented | `SKShaderTileMode.Clamp` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Repeat = 1, // MIRROR_SK_SHADER_TILEMODE = 2` | implemented | `SKShaderTileMode.Repeat` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Mirror = 2, // DECAL_SK_SHADER_TILEMODE = 3` | implemented | `SKShaderTileMode.Mirror` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Decal = 3` | implemented | `SKShaderTileMode.Decal` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKStrokeCap

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// BUTT_SK_STROKE_CAP = 0 Butt = 0, // ROUND_SK_STROKE_CAP = 1` | implemented | `SKStrokeCap.Butt` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Round = 1, // SQUARE_SK_STROKE_CAP = 2` | implemented | `SKStrokeCap.Round` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Square = 2` | implemented | `SKStrokeCap.Square` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKStrokeJoin

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// MITER_SK_STROKE_JOIN = 0 Miter = 0, // ROUND_SK_STROKE_JOIN = 1` | implemented | `SKStrokeJoin.Miter` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Round = 1, // BEVEL_SK_STROKE_JOIN = 2` | implemented | `SKStrokeJoin.Round` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Bevel = 2` | implemented | `SKStrokeJoin.Bevel` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKSurfacePropsFlags

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// NONE_SK_SURFACE_PROPS_FLAGS = 0 None = 0, // USE_DEVICE_INDEPENDENT_FONTS_SK_SURFACE_PROPS_FLAGS = 1 << 0` | implemented | `SKSurfacePropsFlags.None` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `UseDeviceIndependentFonts = 1` | implemented | `SKSurfacePropsFlags.UseDeviceIndependentFonts` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKTextAlign

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// LEFT_SK_TEXT_ALIGN = 0 Left = 0, // CENTER_SK_TEXT_ALIGN = 1` | implemented | `SKTextAlign.Left` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Center = 1, // RIGHT_SK_TEXT_ALIGN = 2` | implemented | `SKTextAlign.Center` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Right = 2` | implemented | `SKTextAlign.Right` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKTextEncoding

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// UTF8_SK_TEXT_ENCODING = 0 Utf8 = 0, // UTF16_SK_TEXT_ENCODING = 1` | implemented | `SKTextEncoding.Utf8` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Utf16 = 1, // UTF32_SK_TEXT_ENCODING = 2` | implemented | `SKTextEncoding.Utf16` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Utf32 = 2, // GLYPH_ID_SK_TEXT_ENCODING = 3` | implemented | `SKTextEncoding.Utf32` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `GlyphId = 3` | implemented | `SKTextEncoding.GlyphId` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKVertexMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// TRIANGLES_SK_VERTICES_VERTEX_MODE = 0 Triangles = 0, // TRIANGLE_STRIP_SK_VERTICES_VERTEX_MODE = 1` | implemented | `SKVertexMode.Triangles` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `TriangleStrip = 1, // TRIANGLE_FAN_SK_VERTICES_VERTEX_MODE = 2` | implemented | `SKVertexMode.TriangleStrip` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `TriangleFan = 2` | implemented | `SKVertexMode.TriangleFan` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SKWebpEncoderCompression

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `// LOSSY_SK_WEBPENCODER_COMPTRESSION = 0 Lossy = 0, // LOSSLESS_SK_WEBPENCODER_COMPTRESSION = 1` | implemented | `SKWebpEncoderCompression.Lossy` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |
| `Lossless = 1` | implemented | `SKWebpEncoderCompression.Lossless` — Explicit pinned enum member or documented native enum-value adapter; values are verified against source declarations/runtime metadata. |

### SkiaSharp.SkiaSharpVersion

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static Version NativeMinimum` | partial | `SkiaSharpVersion.NativeMinimum` — Browser version object exposes native Skia milestone and validates required WASM symbols; CLR native-library version identity is replaced by the browser ABI check. |
| `public static Version Native` | partial | `SkiaSharpVersion.Native` — Browser version object exposes native Skia milestone and validates required WASM symbols; CLR native-library version identity is replaced by the browser ABI check. |
| `public static bool CheckNativeLibraryCompatible (bool throwIfIncompatible = false)` | partial | `SkiaSharpVersion.CheckNativeLibraryCompatible` — Browser version object exposes native Skia milestone and validates required WASM symbols; CLR native-library version identity is replaced by the browser ABI check. |

### SkiaSharp.StringUtilities

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static int GetUnicodeCharacterCode (string character, SKTextEncoding encoding)` | implemented | `StringUtilities.GetUnicodeCharacterCode` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static byte[] GetEncodedText (string text, SKTextEncoding encoding)` | implemented | `StringUtilities.GetEncodedText` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static byte[] GetEncodedText (ReadOnlySpan<char> text, SKTextEncoding encoding)` | implemented | `StringUtilities.GetEncodedText` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static string GetString (IntPtr data, int dataLength, SKTextEncoding encoding)` | not-applicable | `StringUtilities.GetString` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public static string GetString (byte[] data, SKTextEncoding encoding)` | implemented | `StringUtilities.GetString` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static string GetString (byte[] data, int index, int count, SKTextEncoding encoding)` | implemented | `StringUtilities.GetString` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static string GetString (ReadOnlySpan<byte> data, SKTextEncoding encoding)` | implemented | `StringUtilities.GetString` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |
| `public static string GetString (ReadOnlySpan<byte> data, int index, int count, SKTextEncoding encoding)` | implemented | `StringUtilities.GetString` — UTF8/16/32 encode/decode/slice and character-code adapters; BOM and malformed surrogate cases plus overlapping red/blue swizzle spans pass conformance tests. |

### SkiaSharp.GRVorticeD3DBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public new IDXGIAdapter1? Adapter` | missing | `GRVorticeD3DBackendContext.Adapter` — No exported browser type with this declaration identity. |
| `public new ID3D12Device2? Device` | missing | `GRVorticeD3DBackendContext.Device` — No exported browser type with this declaration identity. |
| `public new ID3D12CommandQueue? Queue` | missing | `GRVorticeD3DBackendContext.Queue` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVorticeD3DTextureResourceInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public new ID3D12Resource? Resource` | missing | `GRVorticeD3DTextureResourceInfo.Resource` — No exported browser type with this declaration identity. |
| `public new ResourceStates ResourceState` | missing | `GRVorticeD3DTextureResourceInfo.ResourceState` — No exported browser type with this declaration identity. |
| `public new Format Format` | missing | `GRVorticeD3DTextureResourceInfo.Format` — No exported browser type with this declaration identity. |

### SkiaSharp.HarfBuzz.BlobExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static Blob ToHarfBuzzBlob(this SKStreamAsset asset)` | implemented | `HarfBuzz.BlobExtensions.ToHarfBuzzBlob` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |

### SkiaSharp.HarfBuzz.CanvasExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void DrawShapedText(this SKCanvas canvas, string text, SKPoint p, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, string text, SKPoint p, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, string text, SKPoint p, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, string text, float x, float y, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, string text, float x, float y, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, string text, float x, float y, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, SKPoint p, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, SKPoint p, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, SKPoint p, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, float x, float y, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, float x, float y, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |
| `public static void DrawShapedText(this SKCanvas canvas, SKShaper shaper, string text, float x, float y, SKTextAlign textAlign, SKFont font, SKPaint paint)` | implemented | `HarfBuzz.CanvasExtensions.DrawShapedText` — All twelve shaped-text point/scalar/font/paint/alignment/explicit-shaper forms produce equivalent native glyph calls; shaper typeface assignment and .NET glyph/cluster geometry are verified. |

### SkiaSharp.HarfBuzz.ColorExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColor ToSKColor (this HBColor hbColor)` | implemented | `HarfBuzz.ColorExtensions.ToSKColor` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |
| `public static SKColorF ToSKColorF (this HBColor hbColor)` | implemented | `HarfBuzz.ColorExtensions.ToSKColorF` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |
| `public static SKColor[] ToSKColors (this HBColor[] hbColors)` | implemented | `HarfBuzz.ColorExtensions.ToSKColors` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |
| `public static HBColor ToHBColor (this SKColor color)` | implemented | `HarfBuzz.ColorExtensions.ToHBColor` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |
| `public static HBColor ToHBColor (this SKColorF color)` | implemented | `HarfBuzz.ColorExtensions.ToHBColor` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |

### SkiaSharp.HarfBuzz.FontExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKSizeI GetScale(this Font font)` | implemented | `HarfBuzz.FontExtensions.GetScale` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |
| `public static void SetScale(this Font font, SKSizeI scale)` | implemented | `HarfBuzz.FontExtensions.SetScale` — Explicit HarfBuzz color conversion, scale value/out forwarding and immutable blob ownership adapters; encoded font data remains owned until blob disposal. |

### SkiaSharp.HarfBuzz.SKShaper

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKShaper(SKTypeface typeface)` | implemented | `SKShaper(...)` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public SKTypeface Typeface` | implemented | `SKShaper.Typeface` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public void Dispose()` | implemented | `SKShaper.Dispose` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(Buffer buffer, SKPaint paint)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(Buffer buffer, float xOffset, float yOffset, SKPaint paint)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(Buffer buffer, SKFont font)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(Buffer buffer, float xOffset, float yOffset, SKFont font)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(string text, SKPaint paint)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(string text, float xOffset, float yOffset, SKPaint paint)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(string text, SKFont font)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result Shape(string text, float xOffset, float yOffset, SKFont font)` | implemented | `SKShaper.Shape` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |

### SkiaSharp.HarfBuzz.SKShaper.Result

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Result()` | implemented | `HarfBuzz.SKShaper.Result(...)` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result(uint[] codepoints, uint[] clusters, SKPoint[] points)` | implemented | `HarfBuzz.SKShaper.Result(...)` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public Result(uint[] codepoints, uint[] clusters, SKPoint[] points, float width)` | implemented | `HarfBuzz.SKShaper.Result(...)` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public uint[] Codepoints` | implemented | `HarfBuzz.SKShaper.Result.Codepoints` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public uint[] Clusters` | implemented | `HarfBuzz.SKShaper.Result.Clusters` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public SKPoint[] Points` | implemented | `HarfBuzz.SKShaper.Result.Points` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |
| `public float Width` | implemented | `HarfBuzz.SKShaper.Result.Width` — Stateful native HarfBuzz font/buffer shapes matching .NET size512 convention. Actual .NET/HarfBuzz14 ligature, Arabic, combining-mark and non-BMP glyph, byte-cluster and position references pass; string/buffer/font/legacy paint/offset forms dispatch explicitly. |

### SkiaSharp.Views.Maui.Controls.Hosting.AppHostBuilderExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static MauiAppBuilder UseSkiaSharp(this MauiAppBuilder builder)` | not-applicable | `Views.Maui.Controls.Hosting.AppHostBuilderExtensions.UseSkiaSharp` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Controls.GetPropertyValueEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public T Value` | not-applicable | `Views.Maui.Controls.GetPropertyValueEventArgs.Value` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Controls.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty IgnorePixelScalingProperty = BindableProperty.Create(nameof(IgnorePixelScaling), typeof(bool), typeof(SKCanvasView), false)` | not-applicable | `Views.Maui.Controls.SKCanvasView.IgnorePixelScalingProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static readonly BindableProperty EnableTouchEventsProperty = BindableProperty.Create(nameof(EnableTouchEvents), typeof(bool), typeof(SKCanvasView), false)` | not-applicable | `Views.Maui.Controls.SKCanvasView.EnableTouchEventsProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView()` | not-applicable | `Views.Maui.Controls.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs>? PaintSurface` | not-applicable | `Views.Maui.Controls.SKCanvasView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKTouchEventArgs>? Touch` | not-applicable | `Views.Maui.Controls.SKCanvasView.Touch` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Maui.Controls.SKCanvasView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Maui.Controls.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableTouchEvents` | not-applicable | `Views.Maui.Controls.SKCanvasView.EnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void InvalidateSurface()` | not-applicable | `Views.Maui.Controls.SKCanvasView.InvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Controls.SKGLView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty IgnorePixelScalingProperty = BindableProperty.Create(nameof(IgnorePixelScaling), typeof(bool), typeof(SKGLView), false)` | not-applicable | `Views.Maui.Controls.SKGLView.IgnorePixelScalingProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static readonly BindableProperty HasRenderLoopProperty = BindableProperty.Create(nameof(HasRenderLoop), typeof(bool), typeof(SKGLView), false)` | not-applicable | `Views.Maui.Controls.SKGLView.HasRenderLoopProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static readonly BindableProperty EnableTouchEventsProperty = BindableProperty.Create(nameof(EnableTouchEvents), typeof(bool), typeof(SKGLView), false)` | not-applicable | `Views.Maui.Controls.SKGLView.EnableTouchEventsProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLView()` | not-applicable | `Views.Maui.Controls.SKGLView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Maui.Controls.SKGLView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool HasRenderLoop` | not-applicable | `Views.Maui.Controls.SKGLView.HasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableTouchEvents` | not-applicable | `Views.Maui.Controls.SKGLView.EnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs>? PaintSurface` | not-applicable | `Views.Maui.Controls.SKGLView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKTouchEventArgs>? Touch` | not-applicable | `Views.Maui.Controls.SKGLView.Touch` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Maui.Controls.SKGLView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext? GRContext` | not-applicable | `Views.Maui.Controls.SKGLView.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void InvalidateSurface()` | not-applicable | `Views.Maui.Controls.SKGLView.InvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Controls.SKImageImageSource

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty ImageProperty = BindableProperty.Create(nameof(Image), typeof(SKImage), typeof(SKImageImageSource), default(SKImage))` | not-applicable | `Views.Maui.Controls.SKImageImageSource.ImageProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImage Image` | not-applicable | `Views.Maui.Controls.SKImageImageSource.Image` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<bool> Cancel()` | not-applicable | `Views.Maui.Controls.SKImageImageSource.Cancel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static implicit operator SKImageImageSource(SKImage image)` | not-applicable | `new Views.Maui.Controls.SKImageImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKImage(SKImageImageSource source)` | not-applicable | `new Views.Maui.Controls.SKImageImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.Views.Maui.Controls.SKBitmapImageSource

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty BitmapProperty = BindableProperty.Create(nameof(Bitmap), typeof(SKBitmap), typeof(SKBitmapImageSource), default(SKBitmap))` | not-applicable | `Views.Maui.Controls.SKBitmapImageSource.BitmapProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKBitmap Bitmap` | not-applicable | `Views.Maui.Controls.SKBitmapImageSource.Bitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<bool> Cancel()` | not-applicable | `Views.Maui.Controls.SKBitmapImageSource.Cancel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static implicit operator SKBitmapImageSource(SKBitmap bitmap)` | not-applicable | `new Views.Maui.Controls.SKBitmapImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKBitmap(SKBitmapImageSource source)` | not-applicable | `new Views.Maui.Controls.SKBitmapImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.Views.Maui.Controls.SKPixmapImageSource

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty PixmapProperty = BindableProperty.Create(nameof(Pixmap), typeof(SKPixmap), typeof(SKPixmapImageSource), default(SKPixmap))` | not-applicable | `Views.Maui.Controls.SKPixmapImageSource.PixmapProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPixmap Pixmap` | not-applicable | `Views.Maui.Controls.SKPixmapImageSource.Pixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<bool> Cancel()` | not-applicable | `Views.Maui.Controls.SKPixmapImageSource.Cancel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static implicit operator SKPixmapImageSource(SKPixmap pixmap)` | not-applicable | `new Views.Maui.Controls.SKPixmapImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |
| `public static implicit operator SKPixmap(SKPixmapImageSource source)` | not-applicable | `new Views.Maui.Controls.SKPixmapImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.Views.Maui.Controls.SKPictureImageSource

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static readonly BindableProperty PictureProperty = BindableProperty.Create(nameof(Picture), typeof(SKPicture), typeof(SKPictureImageSource), default(SKPicture))` | not-applicable | `Views.Maui.Controls.SKPictureImageSource.PictureProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static readonly BindableProperty DimensionsProperty = BindableProperty.Create(nameof(Dimensions), typeof(SKSizeI), typeof(SKPictureImageSource), default(SKSizeI))` | not-applicable | `Views.Maui.Controls.SKPictureImageSource.DimensionsProperty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPicture Picture` | not-applicable | `Views.Maui.Controls.SKPictureImageSource.Picture` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSizeI Dimensions` | not-applicable | `Views.Maui.Controls.SKPictureImageSource.Dimensions` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<bool> Cancel()` | not-applicable | `Views.Maui.Controls.SKPictureImageSource.Cancel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static explicit operator SKPicture(SKPictureImageSource source)` | not-applicable | `new Views.Maui.Controls.SKPictureImageSource(...) / explicit conversion` — C# operator/conversion syntax is not available in JavaScript; use explicit value methods or constructors. |

### SkiaSharp.Views.Maui.Extensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static Point ToMauiPoint(this SKPointI point)` | not-applicable | `Views.Maui.Extensions.ToMauiPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static PointF ToMauiPointF(this SKPointI point)` | not-applicable | `Views.Maui.Extensions.ToMauiPointF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToMauiPoint(this SKPoint point)` | not-applicable | `Views.Maui.Extensions.ToMauiPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static PointF ToMauiPointF(this SKPoint point)` | not-applicable | `Views.Maui.Extensions.ToMauiPointF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPoint ToSKPoint(this Point point)` | not-applicable | `Views.Maui.Extensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPoint ToSKPoint(this PointF point)` | not-applicable | `Views.Maui.Extensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToMauiSize(this SKSizeI size)` | not-applicable | `Views.Maui.Extensions.ToMauiSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SizeF ToMauiSizeF(this SKSizeI size)` | not-applicable | `Views.Maui.Extensions.ToMauiSizeF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToMauiSize(this SKSize size)` | not-applicable | `Views.Maui.Extensions.ToMauiSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SizeF ToMauiSizeF(this SKSize size)` | not-applicable | `Views.Maui.Extensions.ToMauiSizeF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this Size size)` | not-applicable | `Views.Maui.Extensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this SizeF size)` | not-applicable | `Views.Maui.Extensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToMauiRectangle(this SKRectI rect)` | not-applicable | `Views.Maui.Extensions.ToMauiRectangle` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static RectF ToMauiRectangleF(this SKRectI rect)` | not-applicable | `Views.Maui.Extensions.ToMauiRectangleF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToMauiRectangle(this SKRect rect)` | not-applicable | `Views.Maui.Extensions.ToMauiRectangle` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static RectF ToMauiRectangleF(this SKRect rect)` | not-applicable | `Views.Maui.Extensions.ToMauiRectangleF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this Rect rect)` | not-applicable | `Views.Maui.Extensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this RectF rect)` | not-applicable | `Views.Maui.Extensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToMauiColor(this SKColor color)` | not-applicable | `Views.Maui.Extensions.ToMauiColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToMauiColor(this SKColorF color)` | not-applicable | `Views.Maui.Extensions.ToMauiColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.Maui.Extensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this Color color)` | not-applicable | `Views.Maui.Extensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Handlers.SKCanvasViewHandler

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void OnInvalidateSurface(SKCanvasViewHandler handler, ISKCanvasView canvasView, object? args)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKCanvasViewHandler handler, ISKCanvasView canvasView, object? args)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKCanvasViewHandler handler, ISKCanvasView canvasView, object? args)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKCanvasViewHandler handler, ISKCanvasView canvasView, object? args)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKCanvasViewHandler handler, ISKCanvasView canvasView, object? args)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKCanvasViewHandler handler, ISKCanvasView canvasView)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static PropertyMapper<ISKCanvasView, SKCanvasViewHandler> SKCanvasViewMapper = new PropertyMapper<ISKCanvasView, SKCanvasViewHandler>(ViewHandler.ViewMapper)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.SKCanvasViewHandler>` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CommandMapper<ISKCanvasView, SKCanvasViewHandler> SKCanvasViewCommandMapper = new CommandMapper<ISKCanvasView, SKCanvasViewHandler>(ViewHandler.ViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler.SKCanvasViewHandler>` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasViewHandler() : base(SKCanvasViewMapper, SKCanvasViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasViewHandler(PropertyMapper? mapper, CommandMapper? commands) : base(mapper ?? SKCanvasViewMapper, commands ?? SKCanvasViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKCanvasViewHandler(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Handlers.SKGLViewHandler

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapBackground(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapBackground` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static PropertyMapper<ISKGLView, SKGLViewHandler> SKGLViewMapper = new PropertyMapper<ISKGLView, SKGLViewHandler>(ViewHandler.ViewMapper)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.SKGLViewHandler>` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CommandMapper<ISKGLView, SKGLViewHandler> SKGLViewCommandMapper = new CommandMapper<ISKGLView, SKGLViewHandler>(ViewHandler.ViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.SKGLViewHandler>` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLViewHandler() : base(SKGLViewMapper, SKGLViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLViewHandler(PropertyMapper? mapper, CommandMapper? commands) : base(mapper ?? SKGLViewMapper, commands ?? SKGLViewCommandMapper)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void OnInvalidateSurface(SKGLViewHandler handler, ISKGLView view, object? args)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.OnInvalidateSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapIgnorePixelScaling(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapIgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapHasRenderLoop(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapHasRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void MapEnableTouchEvents(SKGLViewHandler handler, ISKGLView view)` | not-applicable | `Views.Maui.Handlers.SKGLViewHandler.MapEnableTouchEvents` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.Handlers.SKImageSourceService

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public override Task<IImageSourceServiceResult<Drawable>?> GetDrawableAsync(IImageSource imageSource, Context context, CancellationToken cancellationToken = default)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService.GetDrawableAsync` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<IImageSourceServiceResult<UIImage>?> GetImageAsync(IImageSource imageSource, float scale = 1, CancellationToken cancellationToken = default)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService.GetImageAsync` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<IImageSourceServiceResult<MauiImageSource>?> GetImageAsync( IImageSource imageSource, CancellationToken cancellationToken = default)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService.GetImageAsync` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public async Task<IImageSourceServiceResult<MauiImageSource>?> GetImageAsync(IStreamImageSource imageSource, CancellationToken cancellationToken = default)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService.GetImageAsync` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override Task<IImageSourceServiceResult<WImageSource>?> GetImageSourceAsync(IImageSource imageSource, float scale = 1, CancellationToken cancellationToken = default)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService.GetImageSourceAsync` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageSourceService() : this(null)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageSourceService(ILogger? logger) : base(logger)` | not-applicable | `Views.Maui.Handlers.SKImageSourceService(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKPaintGLSurfaceEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget) : this(surface, renderTarget, GRSurfaceOrigin.BottomLeft, SKColorType.Rgba8888)` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType)` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info) : this(surface, renderTarget, origin, info, info)` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info, SKImageInfo rawInfo)` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRBackendRenderTarget BackendRenderTarget` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.BackendRenderTarget` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKColorType ColorType` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.ColorType` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRSurfaceOrigin Origin` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.Origin` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo Info` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.Info` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo RawInfo` | not-applicable | `Views.Maui.SKPaintGLSurfaceEventArgs.RawInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKPaintSurfaceEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaintSurfaceEventArgs(SKSurface surface, SKImageInfo info) : this(surface, info, info)` | not-applicable | `Views.Maui.SKPaintSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintSurfaceEventArgs(SKSurface surface, SKImageInfo info, SKImageInfo rawInfo)` | not-applicable | `Views.Maui.SKPaintSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.Maui.SKPaintSurfaceEventArgs.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo Info` | not-applicable | `Views.Maui.SKPaintSurfaceEventArgs.Info` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo RawInfo` | not-applicable | `Views.Maui.SKPaintSurfaceEventArgs.RawInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKTouchEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKTouchEventArgs(long id, SKTouchAction type, SKPoint location, bool inContact) : this(id, type, SKMouseButton.Left, SKTouchDeviceType.Touch, location, inContact, 0, 1)` | not-applicable | `Views.Maui.SKTouchEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKTouchEventArgs(long id, SKTouchAction type, SKMouseButton mouseButton, SKTouchDeviceType deviceType, SKPoint location, bool inContact) : this(id, type, mouseButton, deviceType, location, inContact, 0, 1)` | not-applicable | `Views.Maui.SKTouchEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKTouchEventArgs(long id, SKTouchAction type, SKMouseButton mouseButton, SKTouchDeviceType deviceType, SKPoint location, bool inContact, int wheelDelta) : this(id, type, mouseButton, deviceType, location, inContact, wheelDelta, 1)` | not-applicable | `Views.Maui.SKTouchEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKTouchEventArgs(long id, SKTouchAction type, SKMouseButton mouseButton, SKTouchDeviceType deviceType, SKPoint location, bool inContact, int wheelDelta, float pressure)` | not-applicable | `Views.Maui.SKTouchEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool Handled` | not-applicable | `Views.Maui.SKTouchEventArgs.Handled` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public long Id` | not-applicable | `Views.Maui.SKTouchEventArgs.Id` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKTouchAction ActionType` | not-applicable | `Views.Maui.SKTouchEventArgs.ActionType` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKTouchDeviceType DeviceType` | not-applicable | `Views.Maui.SKTouchEventArgs.DeviceType` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKMouseButton MouseButton` | not-applicable | `Views.Maui.SKTouchEventArgs.MouseButton` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPoint Location` | not-applicable | `Views.Maui.SKTouchEventArgs.Location` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool InContact` | not-applicable | `Views.Maui.SKTouchEventArgs.InContact` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public int WheelDelta` | not-applicable | `Views.Maui.SKTouchEventArgs.WheelDelta` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public float Pressure` | not-applicable | `Views.Maui.SKTouchEventArgs.Pressure` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override string ToString()` | not-applicable | `Views.Maui.SKTouchEventArgs.ToString` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKTouchAction

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Entered` | not-applicable | `Views.Maui.SKTouchAction.Entered` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Pressed` | not-applicable | `Views.Maui.SKTouchAction.Pressed` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Moved` | not-applicable | `Views.Maui.SKTouchAction.Moved` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Released` | not-applicable | `Views.Maui.SKTouchAction.Released` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Cancelled` | not-applicable | `Views.Maui.SKTouchAction.Cancelled` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Exited` | not-applicable | `Views.Maui.SKTouchAction.Exited` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `WheelChanged` | not-applicable | `Views.Maui.SKTouchAction.WheelChanged` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKTouchDeviceType

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Touch` | not-applicable | `Views.Maui.SKTouchDeviceType.Touch` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Mouse` | not-applicable | `Views.Maui.SKTouchDeviceType.Mouse` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Pen` | not-applicable | `Views.Maui.SKTouchDeviceType.Pen` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Maui.SKMouseButton

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Unknown` | not-applicable | `Views.Maui.SKMouseButton.Unknown` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Left` | not-applicable | `Views.Maui.SKMouseButton.Left` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Middle` | not-applicable | `Views.Maui.SKMouseButton.Middle` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `Right` | not-applicable | `Views.Maui.SKMouseButton.Right` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.SKSwapChainPanel

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static bool RaiseOnUnsupported` | not-applicable | `Views.Windows.SKSwapChainPanel.RaiseOnUnsupported` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Windows.SKSwapChainPanel.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Windows.SKSwapChainPanel.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double ContentsScale` | not-applicable | `Views.Windows.SKSwapChainPanel.ContentsScale` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool DrawInBackground` | not-applicable | `Views.Windows.SKSwapChainPanel.DrawInBackground` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableRenderLoop` | not-applicable | `Views.Windows.SKSwapChainPanel.EnableRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public new void Invalidate()` | not-applicable | `Views.Windows.SKSwapChainPanel.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Windows.SKSwapChainPanel.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSwapChainPanel()` | not-applicable | `Views.Windows.SKSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Windows.SKSwapChainPanel.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Windows.SKSwapChainPanel.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Windows.SKSwapChainPanel.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.SKXamlCanvas

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKSize CanvasSize` | not-applicable | `Views.Windows.SKXamlCanvas.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Windows.SKXamlCanvas.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double Dpi` | not-applicable | `Views.Windows.SKXamlCanvas.Dpi` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Windows.SKXamlCanvas.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public new void Invalidate()` | not-applicable | `Views.Windows.SKXamlCanvas.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void Draw(CGRect dirtyRect)` | not-applicable | `Views.Windows.SKXamlCanvas.Draw` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void WillMoveToWindow(UIWindow window)` | not-applicable | `Views.Windows.SKXamlCanvas.WillMoveToWindow` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override bool IsOpaque` | not-applicable | `Views.Windows.SKXamlCanvas.IsOpaque` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void DrawRect(CGRect dirtyRect)` | not-applicable | `Views.Windows.SKXamlCanvas.DrawRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKXamlCanvas()` | not-applicable | `Views.Windows.SKXamlCanvas(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Windows.SKXamlCanvas.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Windows.SKXamlCanvas.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double Dpi` | not-applicable | `Views.Windows.SKXamlCanvas.Dpi` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Windows.SKXamlCanvas.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Windows.SKXamlCanvas.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Blazor.Internal.ActionHelper

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public ActionHelper(Action action)` | not-applicable | `Views.Blazor.Internal.ActionHelper(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invoke()` | not-applicable | `Views.Blazor.Internal.ActionHelper.Invoke` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Blazor.Internal.FloatFloatActionHelper

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public FloatFloatActionHelper(Action<float, float> action)` | not-applicable | `Views.Blazor.Internal.FloatFloatActionHelper(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invoke(float width, float height)` | not-applicable | `Views.Blazor.Internal.FloatFloatActionHelper.Invoke` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Blazor.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Action<SKPaintSurfaceEventArgs>? OnPaintSurface` | not-applicable | `Views.Blazor.SKCanvasView.OnPaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableRenderLoop` | not-applicable | `Views.Blazor.SKCanvasView.EnableRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Blazor.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public IReadOnlyDictionary<string, object>? AdditionalAttributes` | not-applicable | `Views.Blazor.SKCanvasView.AdditionalAttributes` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double Dpi` | not-applicable | `Views.Blazor.SKCanvasView.Dpi` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Blazor.SKCanvasView.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Dispose()` | not-applicable | `Views.Blazor.SKCanvasView.Dispose` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Blazor.SKGLView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public Action<SKPaintGLSurfaceEventArgs>? OnPaintSurface` | not-applicable | `Views.Blazor.SKGLView.OnPaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableRenderLoop` | not-applicable | `Views.Blazor.SKGLView.EnableRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Blazor.SKGLView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public IReadOnlyDictionary<string, object>? AdditionalAttributes` | not-applicable | `Views.Blazor.SKGLView.AdditionalAttributes` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double Dpi` | not-applicable | `Views.Blazor.SKGLView.Dpi` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Blazor.SKGLView.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Dispose()` | not-applicable | `Views.Blazor.SKGLView.Dispose` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Gtk.GTKExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPointI ToSKPointI(this Point point)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKPointI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPointI point)` | not-applicable | `Views.Gtk.GTKExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRectI(this Rectangle rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKRectI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rectangle ToRect(this SKRectI rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSizeI ToSKSizeI(this Size size)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKSizeI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToSize(this SKSizeI size)` | not-applicable | `Views.Gtk.GTKExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToColor(this SKColor color)` | not-applicable | `Views.Gtk.GTKExtensions.ToColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Pixbuf ToPixbuf(this SKPicture picture, SKSizeI dimensions)` | not-applicable | `Views.Gtk.GTKExtensions.ToPixbuf` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Pixbuf ToPixbuf(this SKImage skiaImage)` | not-applicable | `Views.Gtk.GTKExtensions.ToPixbuf` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Pixbuf ToPixbuf(this SKBitmap skiaBitmap)` | not-applicable | `Views.Gtk.GTKExtensions.ToPixbuf` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Pixbuf ToPixbuf(this SKPixmap pixmap)` | not-applicable | `Views.Gtk.GTKExtensions.ToPixbuf` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this Pixbuf pixbuf)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this Pixbuf pixbuf)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this Pixbuf pixbuf, SKPixmap pixmap)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPoint ToSKPoint(this Graphene.Point point)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Graphene.Point ToGraphenePoint(this SKPoint point)` | not-applicable | `Views.Gtk.GTKExtensions.ToGraphenePoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this Graphene.Size size)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Graphene.Size ToGrapheneSize(this SKSize size)` | not-applicable | `Views.Gtk.GTKExtensions.ToGrapheneSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this Graphene.Rect rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Graphene.Rect ToGrapheneRect(this SKRect rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToGrapheneRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRectI(this Gdk.Rectangle rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKRectI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Gdk.Rectangle ToGdkRectangle(this SKRectI rect)` | not-applicable | `Views.Gtk.GTKExtensions.ToGdkRectangle` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPoint3 ToSKPoint3(this Graphene.Point3D point)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKPoint3` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Graphene.Point3D ToGraphenePoint3D(this SKPoint3 point)` | not-applicable | `Views.Gtk.GTKExtensions.ToGraphenePoint3D` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Gdk.RGBA color)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Gdk.RGBA ToGdkRGBA(this SKColor color)` | not-applicable | `Views.Gtk.GTKExtensions.ToGdkRGBA` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this Gdk.RGBA color)` | not-applicable | `Views.Gtk.GTKExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Gdk.RGBA ToGdkRGBA(this SKColorF color)` | not-applicable | `Views.Gtk.GTKExtensions.ToGdkRGBA` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Gtk.SKDrawingArea

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKDrawingArea()` | not-applicable | `Views.Gtk.SKDrawingArea(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Gtk.SKDrawingArea.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Gtk.SKDrawingArea.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKDrawingArea() : base(new GObject.ConstructArgument[] { })` | not-applicable | `Views.Gtk.SKDrawingArea(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Gtk.SKDrawingArea.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs>? PaintSurface` | not-applicable | `Views.Gtk.SKDrawingArea.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void Dispose()` | not-applicable | `Views.Gtk.SKDrawingArea.Dispose` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.SKPaintGLSurfaceEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget) : this(surface, renderTarget, GRSurfaceOrigin.BottomLeft, SKColorType.Rgba8888)` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType)` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info) : this(surface, renderTarget, origin, info, info)` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintGLSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info, SKImageInfo rawInfo)` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRBackendRenderTarget BackendRenderTarget` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.BackendRenderTarget` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKColorType ColorType` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.ColorType` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRSurfaceOrigin Origin` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.Origin` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo Info` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.Info` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo RawInfo` | not-applicable | `Views.Windows.SKPaintGLSurfaceEventArgs.RawInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.SKPaintSurfaceEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaintSurfaceEventArgs(SKSurface surface, SKImageInfo info) : this(surface, info, info)` | not-applicable | `Views.Windows.SKPaintSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintSurfaceEventArgs(SKSurface surface, SKImageInfo info, SKImageInfo rawInfo)` | not-applicable | `Views.Windows.SKPaintSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.Windows.SKPaintSurfaceEventArgs.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo Info` | not-applicable | `Views.Windows.SKPaintSurfaceEventArgs.Info` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo RawInfo` | not-applicable | `Views.Windows.SKPaintSurfaceEventArgs.RawInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.WPF.SKElement

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKElement()` | not-applicable | `Views.WPF.SKElement(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.WPF.SKElement.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.WPF.SKElement.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.WPF.SKElement.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.WPF.SKGLElement

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLElement() : base()` | not-applicable | `Views.WPF.SKGLElement(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.WPF.SKGLElement.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.WPF.SKGLElement.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.WPF.SKGLElement.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Dispose()` | not-applicable | `Views.WPF.SKGLElement.Dispose` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.WPF.WPFExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this Point point)` | not-applicable | `Views.WPF.WPFExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPoint point)` | not-applicable | `Views.WPF.WPFExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this Rect rect)` | not-applicable | `Views.WPF.WPFExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToRect(this SKRect rect)` | not-applicable | `Views.WPF.WPFExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this Size size)` | not-applicable | `Views.WPF.WPFExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToSize(this SKSize size)` | not-applicable | `Views.WPF.WPFExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.WPF.WPFExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToColor(this SKColor color)` | not-applicable | `Views.WPF.WPFExtensions.ToColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKPicture picture, SKSizeI dimensions)` | not-applicable | `Views.WPF.WPFExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKImage skiaImage)` | not-applicable | `Views.WPF.WPFExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKBitmap skiaBitmap)` | not-applicable | `Views.WPF.WPFExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKPixmap pixmap)` | not-applicable | `Views.WPF.WPFExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this BitmapSource bitmap)` | not-applicable | `Views.WPF.WPFExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this BitmapSource bitmap)` | not-applicable | `Views.WPF.WPFExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this BitmapSource bitmap, SKPixmap pixmap)` | not-applicable | `Views.WPF.WPFExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.AngleSwapChainPanel

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public AngleSwapChainPanel()` | not-applicable | `Views.Windows.AngleSwapChainPanel(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool DrawInBackground` | not-applicable | `Views.Windows.AngleSwapChainPanel.DrawInBackground` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public double ContentsScale` | not-applicable | `Views.Windows.AngleSwapChainPanel.ContentsScale` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool EnableRenderLoop` | not-applicable | `Views.Windows.AngleSwapChainPanel.EnableRenderLoop` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Windows.AngleSwapChainPanel.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Windows.UWPExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this Point point)` | not-applicable | `Views.Windows.UWPExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPoint point)` | not-applicable | `Views.Windows.UWPExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this Rect rect)` | not-applicable | `Views.Windows.UWPExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToRect(this SKRect rect)` | not-applicable | `Views.Windows.UWPExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this Size size)` | not-applicable | `Views.Windows.UWPExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToSize(this SKSize size)` | not-applicable | `Views.Windows.UWPExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.Windows.UWPExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToColor(this SKColor color)` | not-applicable | `Views.Windows.UWPExtensions.ToColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKPicture picture, SKSizeI dimensions)` | not-applicable | `Views.Windows.UWPExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKImage skiaImage)` | not-applicable | `Views.Windows.UWPExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKBitmap skiaBitmap)` | not-applicable | `Views.Windows.UWPExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static WriteableBitmap ToWriteableBitmap(this SKPixmap pixmap)` | not-applicable | `Views.Windows.UWPExtensions.ToWriteableBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this WriteableBitmap bitmap)` | not-applicable | `Views.Windows.UWPExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this WriteableBitmap bitmap)` | not-applicable | `Views.Windows.UWPExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static bool ToSKPixmap(this WriteableBitmap bitmap, SKPixmap pixmap)` | not-applicable | `Views.Windows.UWPExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Desktop.Extensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this System.Drawing.PointF point)` | not-applicable | `Views.Desktop.Extensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPointI ToSKPoint(this System.Drawing.Point point)` | not-applicable | `Views.Desktop.Extensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.PointF ToDrawingPoint(this SKPoint point)` | not-applicable | `Views.Desktop.Extensions.ToDrawingPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Point ToDrawingPoint(this SKPointI point)` | not-applicable | `Views.Desktop.Extensions.ToDrawingPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this System.Drawing.RectangleF rect)` | not-applicable | `Views.Desktop.Extensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRect(this System.Drawing.Rectangle rect)` | not-applicable | `Views.Desktop.Extensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.RectangleF ToDrawingRect(this SKRect rect)` | not-applicable | `Views.Desktop.Extensions.ToDrawingRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Rectangle ToDrawingRect(this SKRectI rect)` | not-applicable | `Views.Desktop.Extensions.ToDrawingRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this System.Drawing.SizeF size)` | not-applicable | `Views.Desktop.Extensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSizeI ToSKSize(this System.Drawing.Size size)` | not-applicable | `Views.Desktop.Extensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.SizeF ToDrawingSize(this SKSize size)` | not-applicable | `Views.Desktop.Extensions.ToDrawingSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Size ToDrawingSize(this SKSizeI size)` | not-applicable | `Views.Desktop.Extensions.ToDrawingSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Bitmap ToBitmap(this SKPicture picture, SKSizeI dimensions)` | not-applicable | `Views.Desktop.Extensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Bitmap ToBitmap(this SKImage skiaImage)` | not-applicable | `Views.Desktop.Extensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Bitmap ToBitmap(this SKBitmap skiaBitmap)` | not-applicable | `Views.Desktop.Extensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Bitmap ToBitmap(this SKPixmap pixmap)` | not-applicable | `Views.Desktop.Extensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this System.Drawing.Bitmap bitmap)` | not-applicable | `Views.Desktop.Extensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this System.Drawing.Bitmap bitmap)` | not-applicable | `Views.Desktop.Extensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this System.Drawing.Bitmap bitmap, SKPixmap pixmap)` | not-applicable | `Views.Desktop.Extensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this System.Drawing.Color color)` | not-applicable | `Views.Desktop.Extensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static System.Drawing.Color ToDrawingColor(this SKColor color)` | not-applicable | `Views.Desktop.Extensions.ToDrawingColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Desktop.SKControl

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKControl()` | not-applicable | `Views.Desktop.SKControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Desktop.SKControl.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Desktop.SKControl.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Desktop.SKGLControl

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLControl() : base(new GLControlSettings { AlphaBits = 8, RedBits = 8, GreenBits = 8, BlueBits = 8, DepthBits = 24, StencilBits = 8 })` | not-applicable | `Views.Desktop.SKGLControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLControl(GLControlSettings settings) : base(settings)` | not-applicable | `Views.Desktop.SKGLControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLControl() : base(new GraphicsMode(new ColorFormat(8, 8, 8, 8), 24, 8))` | not-applicable | `Views.Desktop.SKGLControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLControl(GraphicsMode mode) : base(mode)` | not-applicable | `Views.Desktop.SKGLControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLControl(GraphicsMode mode, int major, int minor, GraphicsContextFlags flags) : base(mode, major, minor, flags)` | not-applicable | `Views.Desktop.SKGLControl(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Desktop.SKGLControl.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Desktop.SKGLControl.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Desktop.SKGLControl.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.AndroidExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this PointF point)` | not-applicable | `Views.Android.AndroidExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPointI ToSKPoint(this Point point)` | not-applicable | `Views.Android.AndroidExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static PointF ToPoint(this SKPoint point)` | not-applicable | `Views.Android.AndroidExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPointI point)` | not-applicable | `Views.Android.AndroidExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this RectF rect)` | not-applicable | `Views.Android.AndroidExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRect(this Rect rect)` | not-applicable | `Views.Android.AndroidExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static RectF ToRect(this SKRect rect)` | not-applicable | `Views.Android.AndroidExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToRect(this SKRectI rect)` | not-applicable | `Views.Android.AndroidExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.Android.AndroidExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToColor(this SKColor color)` | not-applicable | `Views.Android.AndroidExtensions.ToColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKMatrix ToSKMatrix(this Matrix matrix)` | not-applicable | `Views.Android.AndroidExtensions.ToSKMatrix` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Matrix ToMatrix(this SKMatrix matrix)` | not-applicable | `Views.Android.AndroidExtensions.ToMatrix` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this Bitmap bitmap)` | not-applicable | `Views.Android.AndroidExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this Bitmap bitmap, SKPixmap pixmap)` | not-applicable | `Views.Android.AndroidExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this Bitmap bitmap)` | not-applicable | `Views.Android.AndroidExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Bitmap ToBitmap(this SKBitmap skiaBitmap)` | not-applicable | `Views.Android.AndroidExtensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Bitmap ToBitmap(this SKImage skiaImage)` | not-applicable | `Views.Android.AndroidExtensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Bitmap ToBitmap(this SKPixmap skiaPixmap)` | not-applicable | `Views.Android.AndroidExtensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Bitmap ToBitmap(this SKPicture skiaPicture, SKSizeI dimensions)` | not-applicable | `Views.Android.AndroidExtensions.ToBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasView(Context context) : base(context)` | not-applicable | `Views.Android.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(Context context, IAttributeSet attrs) : base(context, attrs)` | not-applicable | `Views.Android.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(Context context, IAttributeSet attrs, int defStyleAttr) : base(context, attrs, defStyleAttr)` | not-applicable | `Views.Android.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Android.SKCanvasView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Android.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Android.SKCanvasView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.SKGLSurfaceView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLSurfaceView(Context context) : base(context)` | not-applicable | `Views.Android.SKGLSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKGLSurfaceView(Context context, IAttributeSet attrs) : base(context, attrs)` | not-applicable | `Views.Android.SKGLSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Android.SKGLSurfaceView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Android.SKGLSurfaceView.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Android.SKGLSurfaceView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.SKGLSurfaceViewRenderer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKSize CanvasSize` | not-applicable | `Views.Android.SKGLSurfaceViewRenderer.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Android.SKGLSurfaceViewRenderer.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void OnDrawFrame(IGL10 gl)` | not-applicable | `Views.Android.SKGLSurfaceViewRenderer.OnDrawFrame` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void OnSurfaceChanged(IGL10 gl, int width, int height)` | not-applicable | `Views.Android.SKGLSurfaceViewRenderer.OnSurfaceChanged` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void OnSurfaceCreated(IGL10 gl, EGLConfig config)` | not-applicable | `Views.Android.SKGLSurfaceViewRenderer.OnSurfaceCreated` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.SKLockedSurface

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKImageInfo ImageInfo` | not-applicable | `Views.Android.SKLockedSurface.ImageInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.Android.SKLockedSurface.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvas Canvas` | not-applicable | `Views.Android.SKLockedSurface.Canvas` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Android.SKSurfaceView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKSurfaceView(Context context) : base(context)` | not-applicable | `Views.Android.SKSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurfaceView(Context context, IAttributeSet attrs) : base(context, attrs)` | not-applicable | `Views.Android.SKSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurfaceView(Context context, IAttributeSet attrs, int defStyle) : base(context, attrs, defStyle)` | not-applicable | `Views.Android.SKSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Android.SKSurfaceView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public virtual void SurfaceChanged(ISurfaceHolder holder, Format format, int width, int height)` | not-applicable | `Views.Android.SKSurfaceView.SurfaceChanged` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public virtual void SurfaceCreated(ISurfaceHolder holder)` | not-applicable | `Views.Android.SKSurfaceView.SurfaceCreated` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public virtual void SurfaceDestroyed(ISurfaceHolder holder)` | not-applicable | `Views.Android.SKSurfaceView.SurfaceDestroyed` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKLockedSurface LockSurface()` | not-applicable | `Views.Android.SKSurfaceView.LockSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void UnlockSurfaceAndPost(SKLockedSurface surface)` | not-applicable | `Views.Android.SKSurfaceView.UnlockSurfaceAndPost` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.tvOS.AppleExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this CGPoint point)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGPoint ToPoint(this SKPoint point)` | not-applicable | `Views.tvOS.AppleExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this CGRect rect)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGRect ToRect(this SKRect rect)` | not-applicable | `Views.tvOS.AppleExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this CGSize size)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGSize ToSize(this SKSize size)` | not-applicable | `Views.tvOS.AppleExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this CGColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this CGColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGColor ToCGColor(this SKColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGColor ToCGColor(this SKColorF color)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this CIColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this CIColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIColor ToCIColor(this SKColor color)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIColor ToCIColor(this SKColorF color)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this CGImage cgImage, SKPixmap pixmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this CGImage cgImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this CGImage cgImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGImage ToCGImage(this SKPicture skiaPicture, SKSizeI dimensions)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGImage ToCGImage(this SKImage skiaImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGImage ToCGImage(this SKPixmap skiaPixmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CGImage ToCGImage(this SKBitmap skiaBitmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToCGImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void ToSKPixmap(this CIImage ciImage, SKPixmap pixmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this CIImage ciImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this CIImage ciImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIImage ToCIImage(this SKPicture skiaPicture, SKSizeI dimensions)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIImage ToCIImage(this SKImage skiaImage)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIImage ToCIImage(this SKPixmap skiaPixmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static CIImage ToCIImage(this SKBitmap skiaBitmap)` | not-applicable | `Views.tvOS.AppleExtensions.ToCIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSData ToNSData(this SKData skiaData)` | not-applicable | `Views.tvOS.AppleExtensions.ToNSData` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKData ToSKData(this NSData nsData)` | not-applicable | `Views.tvOS.AppleExtensions.ToSKData` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.tvOS.SKCanvasLayer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasLayer()` | not-applicable | `Views.tvOS.SKCanvasLayer(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.tvOS.SKCanvasLayer.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.tvOS.SKCanvasLayer.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void DrawInContext(CGContext ctx)` | not-applicable | `Views.tvOS.SKCanvasLayer.DrawInContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.tvOS.SKCanvasLayer.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.iOS.SKMetalView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKMetalView() : this(CGRect.Empty)` | not-applicable | `Views.iOS.SKMetalView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKMetalView(CGRect frame) : base(frame, null)` | not-applicable | `Views.iOS.SKMetalView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKMetalView(CGRect frame, IMTLDevice device) : base(frame, device)` | not-applicable | `Views.iOS.SKMetalView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKMetalView(IntPtr p) : base(p)` | not-applicable | `Views.iOS.SKMetalView(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public override void AwakeFromNib()` | not-applicable | `Views.iOS.SKMetalView.AwakeFromNib` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.iOS.SKMetalView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.iOS.SKMetalView.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintMetalSurfaceEventArgs> PaintSurface` | not-applicable | `Views.iOS.SKMetalView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.iOS.SKPaintMetalSurfaceEventArgs

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKPaintMetalSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget) : this(surface, renderTarget, GRSurfaceOrigin.TopLeft, SKColorType.Rgba8888)` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintMetalSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKColorType colorType)` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintMetalSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info) : this(surface, renderTarget, origin, info, info)` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKPaintMetalSurfaceEventArgs(SKSurface surface, GRBackendRenderTarget renderTarget, GRSurfaceOrigin origin, SKImageInfo info, SKImageInfo rawInfo)` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSurface Surface` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.Surface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRBackendRenderTarget BackendRenderTarget` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.BackendRenderTarget` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKColorType ColorType` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.ColorType` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRSurfaceOrigin Origin` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.Origin` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo Info` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.Info` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKImageInfo RawInfo` | not-applicable | `Views.iOS.SKPaintMetalSurfaceEventArgs.RawInfo` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.CustomRenderingView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public CustomRenderingView(EvasObject parent) : base(parent)` | not-applicable | `Views.Tizen.CustomRenderingView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Tizen.CustomRenderingView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public RenderingMode RenderingMode` | not-applicable | `Views.Tizen.CustomRenderingView.RenderingMode` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Tizen.CustomRenderingView.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.NUI.CustomRenderingView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKSize CanvasSize` | not-applicable | `Views.Tizen.NUI.CustomRenderingView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs>? PaintSurface` | not-applicable | `Views.Tizen.NUI.CustomRenderingView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public void Invalidate()` | not-applicable | `Views.Tizen.NUI.CustomRenderingView.Invalidate` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public Action? SizeUpdated` | not-applicable | `Views.Tizen.NUI.CustomRenderingView.SizeUpdated` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.RenderingMode

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `Continuously` | not-applicable | `Views.Tizen.RenderingMode.Continuously` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `WhenDirty` | not-applicable | `Views.Tizen.RenderingMode.WhenDirty` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasView(EvasObject parent) : base(parent)` | not-applicable | `Views.Tizen.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Tizen.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Tizen.SKCanvasView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.NUI.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasView()` | not-applicable | `Views.Tizen.NUI.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Tizen.NUI.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.SKGLSurfaceView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLSurfaceView(EvasObject parent) : base(parent)` | not-applicable | `Views.Tizen.SKGLSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Tizen.SKGLSurfaceView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Tizen.SKGLSurfaceView.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.NUI.SKGLSurfaceView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLSurfaceView()` | not-applicable | `Views.Tizen.NUI.SKGLSurfaceView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public Vec2 position` | not-applicable | `Views.Tizen.NUI.SKGLSurfaceView.position` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public Vec2(float xIn, float yIn)` | not-applicable | `Views.Tizen.NUI.SKGLSurfaceView.Vec2` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.ScalingInfo

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static string Profile` | not-applicable | `Views.Tizen.ScalingInfo.Profile` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static int Dpi` | not-applicable | `Views.Tizen.ScalingInfo.Dpi` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static double ScalingFactor` | not-applicable | `Views.Tizen.ScalingInfo.ScalingFactor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static double FromPixel(double v)` | not-applicable | `Views.Tizen.ScalingInfo.FromPixel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static double ToPixel(double v)` | not-applicable | `Views.Tizen.ScalingInfo.ToPixel` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static void SetScalingFactor(double? scalingFactor)` | not-applicable | `Views.Tizen.ScalingInfo.SetScalingFactor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Tizen.TizenExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKPoint ToSKPoint(this Point point)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPointI ToSKPointI(this Point point)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKPointI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPoint ToSKPoint(this global::Tizen.NUI.Position point)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKPointI ToSKPointI(this global::Tizen.NUI.Position2D point)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKPointI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPoint point)` | not-applicable | `Views.Tizen.TizenExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Point ToPoint(this SKPointI point)` | not-applicable | `Views.Tizen.TizenExtensions.ToPoint` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this Size size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSizeI ToSKSizeI(this Size size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKSizeI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSize ToSKSize(this global::Tizen.NUI.Size size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKSizeI ToSKSizeI(this global::Tizen.NUI.Size2D size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKSizeI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToSize(this SKSize size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Size ToSize(this SKSizeI size)` | not-applicable | `Views.Tizen.TizenExtensions.ToSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this Rect rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRectI(this Rect rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKRectI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRect ToSKRect(this global::Tizen.NUI.Rectangle rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKRectI ToSKRectI(this global::Tizen.NUI.Rectangle rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKRectI` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToRect(this SKRect rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Rect ToRect(this SKRectI rect)` | not-applicable | `Views.Tizen.TizenExtensions.ToRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColor ToSKColor(this Color color)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this global::Tizen.NUI.Color color)` | not-applicable | `Views.Tizen.TizenExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static Color ToColor(this SKColor color)` | not-applicable | `Views.Tizen.TizenExtensions.ToColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.tvOS.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasView()` | not-applicable | `Views.tvOS.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(CGRect frame) : base(frame)` | not-applicable | `Views.tvOS.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(IntPtr p) : base(p)` | not-applicable | `Views.tvOS.SKCanvasView(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public override void AwakeFromNib()` | not-applicable | `Views.tvOS.SKCanvasView.AwakeFromNib` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.tvOS.SKCanvasView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.tvOS.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void Draw(CGRect rect)` | not-applicable | `Views.tvOS.SKCanvasView.Draw` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void WillMoveToWindow(UIWindow window)` | not-applicable | `Views.tvOS.SKCanvasView.WillMoveToWindow` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.tvOS.SKCanvasView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void LayoutSubviews()` | not-applicable | `Views.tvOS.SKCanvasView.LayoutSubviews` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.tvOS.SKGLLayer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLLayer()` | not-applicable | `Views.tvOS.SKGLLayer(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.tvOS.SKGLLayer.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.tvOS.SKGLLayer.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public virtual void Render()` | not-applicable | `Views.tvOS.SKGLLayer.Render` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override CGRect Frame` | not-applicable | `Views.tvOS.SKGLLayer.Frame` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.tvOS.SKGLLayer.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.tvOS.iOSExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColor ToSKColor(this UIColor color)` | not-applicable | `Views.tvOS.iOSExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this UIColor color)` | not-applicable | `Views.tvOS.iOSExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIColor ToUIColor(this SKColor color)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIColor ToUIColor(this SKColorF color)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this UIImage uiImage)` | not-applicable | `Views.tvOS.iOSExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this UIImage uiImage)` | not-applicable | `Views.tvOS.iOSExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static bool ToSKPixmap(this UIImage uiImage, SKPixmap pixmap)` | not-applicable | `Views.tvOS.iOSExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKPicture skiaPicture, SKSizeI dimensions, nfloat scale, UIImageOrientation orientation)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKPicture skiaPicture, SKSizeI dimensions)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKImage skiaImage)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKPixmap skiaPixmap, nfloat scale, UIImageOrientation orientation)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKPixmap skiaPixmap)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKBitmap skiaBitmap, nfloat scale, UIImageOrientation orientation)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static UIImage ToUIImage(this SKBitmap skiaBitmap)` | not-applicable | `Views.tvOS.iOSExtensions.ToUIImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Mac.MacExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static SKColor ToSKColor(this NSColor color)` | not-applicable | `Views.Mac.MacExtensions.ToSKColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKColorF ToSKColorF(this NSColor color)` | not-applicable | `Views.Mac.MacExtensions.ToSKColorF` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSColor ToNSColor(this SKColor color)` | not-applicable | `Views.Mac.MacExtensions.ToNSColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSColor ToNSColor(this SKColorF color)` | not-applicable | `Views.Mac.MacExtensions.ToNSColor` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKImage ToSKImage(this NSImage nsImage)` | not-applicable | `Views.Mac.MacExtensions.ToSKImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static SKBitmap ToSKBitmap(this NSImage nsImage)` | not-applicable | `Views.Mac.MacExtensions.ToSKBitmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static bool ToSKPixmap(this NSImage nsImage, SKPixmap pixmap)` | not-applicable | `Views.Mac.MacExtensions.ToSKPixmap` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSImage ToNSImage(this SKPicture skiaPicture, SKSizeI dimensions)` | not-applicable | `Views.Mac.MacExtensions.ToNSImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSImage ToNSImage(this SKImage skiaImage)` | not-applicable | `Views.Mac.MacExtensions.ToNSImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSImage ToNSImage(this SKPixmap skiaPixmap)` | not-applicable | `Views.Mac.MacExtensions.ToNSImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public static NSImage ToNSImage(this SKBitmap skiaBitmap)` | not-applicable | `Views.Mac.MacExtensions.ToNSImage` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Mac.SKCanvasView

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKCanvasView()` | not-applicable | `Views.Mac.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(CGRect frame) : base(frame)` | not-applicable | `Views.Mac.SKCanvasView(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKCanvasView(IntPtr p) : base(p)` | not-applicable | `Views.Mac.SKCanvasView(...)` — Raw process addresses and native object handles cannot cross the browser sandbox. Typed-array and managed wrapper overloads are audited separately. |
| `public override void AwakeFromNib()` | not-applicable | `Views.Mac.SKCanvasView.AwakeFromNib` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Mac.SKCanvasView.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public bool IgnorePixelScaling` | not-applicable | `Views.Mac.SKCanvasView.IgnorePixelScaling` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Mac.SKCanvasView.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void DrawRect(CGRect dirtyRect)` | not-applicable | `Views.Mac.SKCanvasView.DrawRect` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.Views.Mac.SKGLLayer

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public SKGLLayer()` | not-applicable | `Views.Mac.SKGLLayer(...)` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public SKSize CanvasSize` | not-applicable | `Views.Mac.SKGLLayer.CanvasSize` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public GRContext GRContext` | not-applicable | `Views.Mac.SKGLLayer.GRContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public event EventHandler<SKPaintGLSurfaceEventArgs> PaintSurface` | not-applicable | `Views.Mac.SKGLLayer.PaintSurface` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void DrawInCGLContext(CGLContext glContext, CGLPixelFormat pixelFormat, double timeInterval, ref CVTimeStamp timeStamp)` | not-applicable | `Views.Mac.SKGLLayer.DrawInCGLContext` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |
| `public override void Release(CGLContext glContext)` | not-applicable | `Views.Mac.SKGLLayer.Release` — Platform UI adapter is outside the reusable HTML component API; use <skia-canvas> and browser lifecycle events. |

### SkiaSharp.GRSharpVkBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public new Instance VkInstance` | missing | `GRSharpVkBackendContext.VkInstance` — No exported browser type with this declaration identity. |
| `public new PhysicalDevice VkPhysicalDevice` | missing | `GRSharpVkBackendContext.VkPhysicalDevice` — No exported browser type with this declaration identity. |
| `public new Device VkDevice` | missing | `GRSharpVkBackendContext.VkDevice` — No exported browser type with this declaration identity. |
| `public new Queue VkQueue` | missing | `GRSharpVkBackendContext.VkQueue` — No exported browser type with this declaration identity. |
| `public new PhysicalDeviceFeatures? VkPhysicalDeviceFeatures` | missing | `GRSharpVkBackendContext.VkPhysicalDeviceFeatures` — No exported browser type with this declaration identity. |
| `public new GRSharpVkGetProcedureAddressDelegate GetProcedureAddress` | missing | `GRSharpVkBackendContext.GetProcedureAddress` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkExtensionsSharpVkExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void Initialize(this GRVkExtensions extensions, GRSharpVkGetProcedureAddressDelegate getProc, Instance instance, PhysicalDevice physicalDevice)` | missing | `GRVkExtensionsSharpVkExtensions.Initialize` — No exported browser type with this declaration identity. |
| `public static void Initialize(this GRVkExtensions extensions, GRSharpVkGetProcedureAddressDelegate getProc, Instance instance, PhysicalDevice physicalDevice, string[] instanceExtensions, string[] deviceExtensions)` | missing | `GRVkExtensionsSharpVkExtensions.Initialize` — No exported browser type with this declaration identity. |

### SkiaSharp.GRSilkNetBackendContext

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public new Instance VkInstance` | missing | `GRSilkNetBackendContext.VkInstance` — No exported browser type with this declaration identity. |
| `public new PhysicalDevice VkPhysicalDevice` | missing | `GRSilkNetBackendContext.VkPhysicalDevice` — No exported browser type with this declaration identity. |
| `public new Device VkDevice` | missing | `GRSilkNetBackendContext.VkDevice` — No exported browser type with this declaration identity. |
| `public new Queue VkQueue` | missing | `GRSilkNetBackendContext.VkQueue` — No exported browser type with this declaration identity. |
| `public new PhysicalDeviceFeatures? VkPhysicalDeviceFeatures` | missing | `GRSilkNetBackendContext.VkPhysicalDeviceFeatures` — No exported browser type with this declaration identity. |
| `public new GRSilkNetGetProcedureAddressDelegate GetProcedureAddress` | missing | `GRSilkNetBackendContext.GetProcedureAddress` — No exported browser type with this declaration identity. |

### SkiaSharp.GRVkExtensionsSilkNetExtensions

| Declaration | Status | JavaScript mapping / reason |
| --- | --- | --- |
| `public static void Initialize(this GRVkExtensions extensions, GRSilkNetGetProcedureAddressDelegate getProc, Instance instance, PhysicalDevice physicalDevice)` | missing | `GRVkExtensionsSilkNetExtensions.Initialize` — No exported browser type with this declaration identity. |
| `public static void Initialize(this GRVkExtensions extensions, GRSilkNetGetProcedureAddressDelegate getProc, Instance instance, PhysicalDevice physicalDevice, string[] instanceExtensions, string[] deviceExtensions)` | missing | `GRVkExtensionsSilkNetExtensions.Initialize` — No exported browser type with this declaration identity. |

